---
# bundle-header — provenance for this document. Identical across the bundle.
bundle:    agent-guides
lineage:   g-8b5800/main
ancestry:  [g-c7344c, g-099a8a, g-8b5800]
version:   1
component: method-changelog
released:  2026-09-24
---

# Method changelog

Newest first. One row per method version, saying what a reader **does differently** now, not
what was edited. A release adds its row in the same change that bumps the version; rows are
never renumbered and never rewritten, because other copies cite them — with one exception: a
privacy scrub (`prompt-context.md`, principle 20) may rewrite or delete a row, and the release
that does it says so in its own row. The `version` column is
the method lineage's version (`method/prompt-*.md` headers), not this file's.

| Version | Date | What changed for the reader |
|---|---|---|
| 24 | 2026-09-24 | **Nothing private travels, and a tool checks it rather than a memory.** Principle 20: nothing in the bundle may let a reader identify a private repository, its owner, organisation, customers, users or infrastructure, or anyone who uses the bundle — directly or by putting harmless details together. Figures become orders of magnitude, exact constants ratios, quotes paraphrases, identifiers roles, narrowing domain nouns neutral kinds, personal context nothing; a public carrier may stay as it is. **Privacy wins over record-keeping**: changelogs and history are not exempt, and obsolete private detail is deleted when nothing depends on it. `bundle.py privacy` enforces it, inside `digest --check` and in the home repository's hooks, with a machine-local list of private terms that is never committed; the `privacy-allow: <reason>` override exists only on the user's explicit instruction and is listed on every run. The bundle README's conventions table opens with it; harvest and sync generalise before writing anything that travels and run the check, and a `lost` line removed for privacy is an approved removal; bootstrap puts a one-line reminder in the carrier's root file and the check in its gate; every paste block carries one line of it. **Carrier ids are random**, minted once by `bundle.py carrier-id --mint` and stored in the carrier's own header field `carrier:`: an id hashed from the remote was reversed by guessing the remote, so the ids of the carriers not reached were withdrawn. `bundle.py ids FILE...` is the host-side record-id check. Copilot's on-demand procedures verified: prompt files, custom agents and agent skills. **A privacy scrub rewrote every document in this release, including earlier rows of this table**: carrier-specific detail was removed or generalised, 2026-09-24. |
| 23 | 2026-09-24 | **Records written by parallel sessions are never numbered.** A decision row, a roadmap item and a changelog entry get `<kind>-<repo6>-<content6>` (`d-`, `i-`, `s-`), minted once by `bundle.py id` and frozen; two sessions on different branches cannot see each other's next number, and a counted id made one of them lie. Legacy ids stay as written; numbers remain only for what a release writes. **Every invocation carries a `Reads:` list** of files and exact section headings, which `bundle.py` reads, checks against the files and prices (`report`) — the lists are the only copy; a `consult` use reads the knowledge index and one note. **Bootstrap wires the knowledge base in**: the root file sends a session to `knowledge/INDEX.md` before a design decision and before claiming done, and the session loop does the same. One rule, one home: the repository's own header fields (`upstream` among them) in the bundle `README.md`, `incoming/` in `prompt-update.md`, the workspace scope in `prompt-context.md` §*Workspaces*, the precedence and set rules in §*The set*, the harvest in `prompt-harvest.md` (an update writes only candidates; a release admits them, in `prompt-sync.md`), the digest in `bundle.py digest --check`. Fixed contradictions: a carrier never bumps a version; a merge records a principle-number mapping here, not in `adapted`; bootstrap stops when `adopted` is this repository's; evaluation reports carry a content suffix. *Three agents, one source* re-verified against vendor docs: Cursor and Copilot now have hooks and path-scoped rules and read `AGENTS.md`, so no surface lacks machinery and none shares another's — the conclusion (guarantees on rung 3 or 4) stands for a new reason. This table moved out of `prompt-context.md` into this file. In the same release the knowledge base's notes moved into `notes/active/`, `notes/review/` and `notes/retired/`, where the folder is the note's state, moved by `bundle.py note-state`. |
| 22 | 2026-09-23 | **A retraction reaches the copy that is loaded, and the row of the decision it reverses.** Principle 6's retraction rule now covers standing rules and decision records, not only findings: a correction written where the mistake was found leaves the loaded copy wrong and every record written under it repeating the wrong step, and a superseded decision whose own row does not say so keeps reading as live to everyone who arrives from the index. Two repositories, both measured. In `prompt-update.md`, *Does not apply* is distinguished from `declined` and re-triaged when its mechanism appears, and `incoming/` is *empty except its own `README.md`*. `layout.md` says a single-assistant repository keeps one skills folder, and that Claude Code now reads `AGENTS.md` when no `CLAUDE.md` exists. Rows 17–21 below were missing from this changelog while the header declared 21: a reader updating from 16 had nothing to triage from, and one carrier had to diff artifact specs instead. They are written now, from the bundle roadmap's *Done* entries. The bundle gains `references.md`, the external work behind the rules on documents, and `bundle.py` runs on Python 3.9. |
| 21 | 2026-09-22 | **The tool refuses what it used to infer.** A writing command is given its repositories and never takes them from the machine's manifest, and the base of a meta-session is found by the content of everything that travels, not by the stamped digest, which does not cover `tracking/`. `stamp` is idempotent against the same base. `prompt-sync.md` describes what the tool does, not what it was assumed to do. |
| 20 | 2026-09-22 | **A group update is a cycle**: align every carrier, harvest each one, then one release. `prompt-sync.md` §*The cycle* gives the reason each step is where it is; when nothing diverged the first step is `align` plus `check-local`, a check and not a ceremony. |
| 19 | 2026-09-22 | **A harvest closes the open session first.** `prompt-harvest.md` phase 0 runs the session loop's closing review over whatever is in flight, because a harvest reads the record and an unclosed session's learnings are in a working tree and in nobody's record. |
| 18 | 2026-09-22 | **A session writes its workspace and nothing else.** The local step and the meta-session are scoped to the repositories the session has open; `bundle.py` refuses to splice into a carrier outside it and names every carrier it did not touch, and `stamp` refuses a second bump against the release it already followed. |
| 17 | — | **A number nobody holds.** The release that stamped it (bundle v13) failed before reaching any carrier; numbers are never reused, so it stays skipped. |
| 16 | 2026-09-22 | **The local step has its own invocation: `prompt-harvest.md`.** A repository's harvest used to be a section of `prompt-update.md`, reachable only while updating, and a meta-session assumed every carrier had somehow already written down what it learned. It is now the step each carrier runs before a meta-session — and it is defined by what it may **not** write: candidates and evidence go in `tracking/`, which merges line by line, and a note, an index row, a method rule, a header or a version belongs to the release, written once for every carrier. Extending a note is a candidate that names the note. `bundle.py check-local` enforces it against the carrier's committed bundle, so a fork is visible before a commit rather than at the next meta-session. |
| 15 | 2026-09-22 | **The meta-session over every carrier, and a tool that runs it.** `prompt-sync.md`: gather (every carrier verified, the base found by content in the carriers' histories and only then by a header, one N-way table, one approval), build the release once outside every carrier, apply it in each carrier by itself (its own fields and local files kept, its uncommitted work refused, its gate run there), and close only when `align` reports every carrier on one version. `tools/bundle.py` is that procedure's mechanical half — one standalone file, standard library only, tests inside — replacing three digest recipes and every script a session used to write for the occasion. `tracking/carriers.md` records every carrier by a derived `r-` id; this machine's paths stay in a local manifest. **Every file carries provenance**, a tool in a comment block, and a file type with no header form is refused. |
| 14 | 2026-09-22 | **Merge of the two version-13 lines, `m-bc50b0` and `m-a194bd`, forked from `m-bc50b0` v12.** Both rows 13 stay, each labelled with its lineage, because other copies cite them. From `m-bc50b0`: the insertion point of an append-only document is structural (artifact 5). From `m-a194bd`: everything in its row 13. The two lines had both removed the note cap on the same day and disagreed on one thing, **when a review happens**: one made it a separate pass, never inside a harvest, because a learning is understood least on the day it arrives; the other made a review with verdicts close every harvest, so duplicates do not accumulate. Merged as both: a review closes every harvest, and a note admitted in that same harvest can only be *kept* or *queued* by it — merging, superseding or retiring it waits for a later pass. Every digest recipe fixes the collation (`LC_ALL=C`); the `m-bc50b0` v13 digests had been computed under a locale's collation. |
| 13 (`m-a194bd`) | 2026-09-22 | **Fork `m-a194bd`, harvested from one carrier.** **No note is refused to make room**: the knowledge base's fixed cap of 30 is replaced by what a check can see — every note reachable from a topic, a phase and an *about to do* row, an index split by area when it stops being read in one pass, and a closing review that ends in recorded verdicts (merge, supersede, retire, downgrade), never in a count. The ladder: **a rule the agent had loaded and broke anyway is the signal to raise its rung.** Principle 2: the document audit covers every path any instruction document names, with exemptions listed. Principle 7 gains its second half, **observing after shipping** where the developers cannot observe the software run: a readout per silent failure showing what was asked beside what happened, a generated build identity, a standing brief of questions each with the readout that answers it, and unverified claims queued in it. Principle 15: **content the owner authored is not the agent's to complete** — an empty slot there is a choice. Principle 17's example names the right fix, a formatter that writes back only copies that kept every comment and every non-layout token. Artifact 5 gains *Deviation from the plan* and *Not verified*; artifact 8 takes a pointer to debt written anywhere else. The session loop: probes are found by the audit and cleaned up by their runner, pointing at the knowledge note that generalises it (step 3); **a change meant to change nothing is proved by its invariant, compared mechanically** (step 4); **each commit is offered when its piece passes**, and a split after the fact builds every tree from the starting commit, proves both ends, backs up, and gates each intermediate tree (step 7). Sixteen candidates recorded in `tracking/candidates.md`. Contradictions between the repository's practice and v12 were resolved by mechanism, not by date: commit timing (step 7), the formatter's fix (principle 17, *Working safely*), and rewriting the tree only by a tool that backs up first and proves the restore; the document-language default is left open as a candidate. This table is back in newest-first order. |
| 13 (`m-bc50b0`) | 2026-09-22 | **The insertion point of an append-only document is structural, not instructional.** "Newest on top" tells a writer *where* relative to something, so whatever sits directly under the title is where the next entry lands — and when that was the opening fence of the changelog's own format example, entries were swallowed into a code block that never closed. Invisible in a diff, so no reviewer catches it either. Observed **twice in one repository, by two different sessions**, and never where the format reference happened to sit at the bottom. The rule: the reference goes at the end, nothing that can capture text sits between the title and the first entry.  **Merged from the other line of this lineage**, which had reached its own version 12 in parallel — the collision this method predicts, resolved by taking both rather than either. |
| 12 | 2026-09-22 | **Harvested from two carriers**, applying the generality test to every method candidate and keeping under a third. Principle 6: **a number carries its date and its environment** (never bump a date without re-running; when two measurements disagree, find which binary produced each), and **a retraction is written everywhere the claim was**. Principle 18: when the red step could not be watched, **prove the test bites by a targeted mutation** that asserts its target exists; and when code, test and comment disagree, none of them is the specification. Engineering standards: **a comment describes the code, never the change**, and **durable documents never cite session-local artefacts**. The session loop, step 4: **verification is a step, not a reflex** — once per finished unit, scoped by measured cost, reporting which selection ran — and **measure the untouched base** from a clean export before calling a red check yours. *Working safely*: **the uncommitted diff is the work** — no checkout, restore, stash, reset or clean — and a formatter runs only on the files in the change. The knowledge base grew from 8 notes to 29, eleven of them `measured`, and gained a **lifecycle** — admission with the literature consulted first, falsification, retirement and a budget of 30 — with `INDEX.md` rewritten as a phase guide whose every row carries a *Verify by* check. `prompt-update.md` now documents the cases this release met: an old set in another place or shape, an incoming copy carrying another repository's fields or a stale digest, one release spliced into several repositories, and the harvest as a procedure. The bundle gained `roadmap.md` and `tracking/` (candidates, experiments, retired). |
| 11 | 2026-09-22 | The header is **yaml frontmatter**, not a fenced ```yaml block, in every document of the set and of the bundle around it. One shape instead of two: notes already used frontmatter and the check had grown to accept either, which is how a check ends up accepting whatever it finds. The digest recipe strips from the opening `---` to the closing one — **equally unambiguous**, which was the objection to the change and it was wrong. What is genuinely lost: a rendered preview hides frontmatter, so the version is no longer visible without opening the file. |
| 10 | 2026-09-22 | The set gains a fifth document, **`prompt-merge.md`**, for the case the others could not handle: two bundles whose ancestries **diverge**, where neither side is newer. Its premise is that a merge has no direction — an absence on the other side is checked against their `declined` before being treated as a gap, and **two contradictory knowledge notes are reconciled into one claim with a sharper boundary rather than by picking a winner**. Ties break on `confidence`, then on mechanism over authority, then by asking — never by date. The guides also became **one copyable folder**: `method/` and `knowledge/` under a single bundle header, with one `incoming/`. |
| 9 | 2026-09-22 | The header is explicitly **two kinds of field**: the ones describing the set (`lineage`, `ancestry`, `version`, `forked_at`, `digest`, `released`) travel with the body; the ones describing the repository (`adopted`, `adapted`, `declined`) stay put. Propagating a release is body **plus** the set fields, with the repository fields preserved — and the check is to print version, lineage and computed digest side by side afterwards, because **equal digests with different versions** is the signature of getting it backwards. |
| 8 | 2026-09-17 | **Fork `m-bc50b0`.** Two corrections and two additions. Corrected: Claude Code **does** have per-area glob auto-attachment (`.claude/rules/*.md` with `paths:`), so Cursor's targeting is mirrored rather than worked around; and file permissions are checked against `Edit(...)`/`Read(...)` **only** — a `Write(path)` deny is accepted, never consulted, and protects nothing. Added: principle 1 now names its own failure mode — **a rule citing an enforcer that does not do the job reads as rung 3 and behaves as rung 0** — and makes the instruction system itself part of what the audit checks; and *Workspaces* gains **derived decision-id prefixes**, because cross-repo citation makes a bare `D-002` ambiguous and a chosen prefix collides. `prompt-update.md` gains **the intake folder**: a newer set is copied to `.agents/incoming/`, triaged against the live one, and only then applied — never overwritten first. |
| 7 | 2026-09-17 | **Versioning survives divergence.** A monotone integer silently collides when two repositories on one lineage both improve the method and both bump — the naive comparison calls them identical and discards one side. Fixed by a rule and a check: **a lineage has exactly one writer**, so changing the method *is* forking (`forked_at` records where, and numbering continues rather than restarting); and **`digest`**, a content fingerprint of the set with headers stripped, catches the rule being broken. Matching went from four outcomes to six, three of which stop. Also: the method now assumes **three agents — Claude Code, Cursor and Copilot** — under one rule, *one source of truth, three surfaces*, with the asymmetry that decides the design: Copilot has one flat file and no enforcement, so **a guarantee that must hold cannot depend on any agent's machinery** and belongs on rung 3 or 4. Decisions, references, roadmap and changelog are agent-neutral by design, which is what makes changing tools a non-event. |
| 6 | 2026-09-17 | **Every executable prompt now asks before it executes.** Each opens with a *Before you start* pre-flight — three to five questions, in one message, each carrying the default it will take, answerable with the single word `defaults` — and each invocation orders it as its first act. `prompt-context.md` §*The pre-flight* holds the shared rule, whose whole design is one sentence: **assume nothing genuinely in doubt, ask as little as possible**, resolved by being strict about what counts as doubt — if it can be read, or tried reversibly, it is not a question. The prompt must also say in one line **what it is not asking**, so minimal never becomes silent. Paste blocks became **code fences instead of blockquotes**, because `>` markers are copied along with the text, and each invocation now appears **once**, at the top of its file. `prompt-context.md` absorbed the last of the shared prose — the set table, the language rule, the header mechanics and the adoption rules now exist once. |
| 5 | 2026-09-17 | The set is **four documents with one naming scheme**: `prompt-context.md` (new — this file: the shared reference the other three read from, and **not executable**), `prompt-evaluate.md`, `prompt-bootstrap.md`, `prompt-update.md`. Each executable prompt now opens with its **copy-paste block at the very top**. `prompt-update.md` gained **the prune**, which removes superseded method files only after verifying section by section that their content is covered, lifted or **moved out**, and after following every inbound link. `prompt-evaluate.md` became **set-aware**: it reads the `set` field, lists what is actually present, uses every prompt it finds as criteria, and reports whether the set is complete and consistent. Added *The platform's own mechanics*: what loads when, what does not exist (no unconditional glob auto-attach), a skill's frontmatter, and the mapping from other assistants' formats. |
| 4 | 2026-09-17 | The method **ships as three documents**: `evaluate-prompt.md` (new — read-only assessment of a repository's AI instruction system, producing one summarised Markdown report with a maturity table and a cheapest-first next-steps list) is now the front door; `update-prompt.md` (new — the upgrade job, extracted so it runs without loading the method); and this document. Lineage ids became **opaque tokens with a visible `ancestry` chain**, so a copy can be matched and forked without revealing where it came from. The worked example and every illustration were anonymised: the method carries no project, product, person or customer. |
| 3 | 2026-09-17 | The method is **distributable and complete**: a machine-readable **method header** (lineage, version, adapted, declined) replaces the version line; *The pair* makes bootstrap and update complementary and mutually non-destructive; **principle 18** puts the test before the code and says why that is sharper for an agent than for a human; **principle 19** adopts into a repository that already works instead of replacing it; *Engineering standards* covers tests, types, in-code documentation, the four layers of evaluation and the per-domain non-negotiables; *Workspaces* covers several repos at once; *Models, reasoning levels and cost* says which model each stage wants and how a smaller one self-limits; step 8 separates **capture — always** from **proposing — throttled**; and step 7 gains *What to show the user*. |
| 2 | 2026-09-17 | The method covers the **process itself**: principle 17 (friction is recorded, priced and promoted), the session's two bookends — the **opening brief** (step 0) and the **closing review** (step 8) — a `Process and tooling` area in the roadmap, and this versioning-and-distribution section with its update invocation. |
| 1 | 2026-09-17 | The method covers **the recurring session**, not only the bootstrap: *The session loop*, principles 14–16 (look at the output; sort the decisions; put the documents back), the roadmap as a five-state ledger, and a second invocation for normal work. |
| 0 | — | The bootstrap: the enforcement ladder, principles 1–13, ten artifacts, nine phases, stack adaptation, checklists, worked example. |
