---
# bundle-header — provenance for this document. Identical across the bundle.
bundle:    agent-guides
lineage:   g-8b5800/main
ancestry:  [g-c7344c, g-099a8a, g-8b5800]
version:   7
component: tracking
released:  2026-09-24
---

# Candidates — learnings that have not passed admission

**A learning offered by a harvest that is not yet a note or a method rule.** It waits here
until it passes the five admission steps in `../knowledge/README.md` (*The lifecycle of a
note*), is folded into an existing note, or is discarded with a reason. Nothing here is
followed as guidance — it is a queue, not a knowledge base.

**One table, one row per candidate**: slug and claim, kind (**K** knowledge, **M** method), what
it lacks, where the evidence lives (by the kind of repository in neutral words, never by name or id),
and when it was first offered. A candidate that has waited through three harvests without
gaining what it lacks is discarded with that reason, not kept forever: *First seen* is what
that rule counts from. Where a candidate went once it left this table — admitted, folded,
fixed, refused or discarded — is recorded in `history.md`, so a later harvest offering it
again can see it was answered rather than lost.

| Candidate | Kind | Lacks | Evidence | First seen |
|---|---|---|---|---|
| one-rule-two-readers-agree-on-the-verdict-not-the-refusal — a second implementation of one rule set is held to the same verdicts and not to the same refusals, so it rejects by crashing where the first names the problem | K | a generality statement, and it may be an occurrence of `same-answer-or-refuse` or of `one-path-for-every-source` rather than a claim of its own | an interactive client, measured 2026-09-22: a few forged inputs against the two implementations of one input-reader rule set. Both gave the same verdict on one; on the others the implementation that runs first crashed with an uncaught error and a traceback, where the other reader named the file and carried on | 2026-09-22 or earlier |
| rank-on-raw-probability-on-calibrated — a step-function calibrator ties scores; rank on the raw score | K | generality (ML-narrow); fits `metric-against-trivial-predictor` as a boundary | a data-analysis repository: tens of thousands of distinct scores collapsed to about a hundred after a step-function calibration | 2026-09-22 or earlier |
| append-only-at-a-fixed-schema — nulls from a schema change are indistinguishable from real missing values | K | an incident with a number; overlaps `no-simultaneous-deploy` | a data-analysis repository: a schema check raises on write | 2026-09-22 or earlier |
| split-by-time-neither-side-for-straddlers — a row created before the boundary and observed after it belongs to neither side | K | novelty (temporal splits are known); the "neither side" nuance could extend `population-by-outcome` | a data-analysis repository; literature: Roberts et al. 2017 (Ecography) | 2026-09-22 or earlier |
| distinct-event-kinds-stay-distinct — events that share a table are not the same kind of event | K | generality statement; an incident with a number | a data-analysis repository: an event of one kind counted as an attempt of another would inflate every rate | 2026-09-22 or earlier |
| batch-get-is-unordered — attach the key to each result, never zip by request order | K | likely already known to an agent | a data-analysis repository: a test double returning batches reversed; and a second form measured 2026-09-22 — the *fields* of one returned document come back in no order, so a frame built from it has arbitrary column order | 2026-09-22 or earlier |
| cache-proven-by-equality — verify a derived cache by frame equality with the uncached path | K | fits `same-answer-or-refuse` as an occurrence | a data-analysis repository: arrays read back as another type, truncated timestamps, a copy that preserved modification times | 2026-09-22 or earlier |
| numerator-needs-its-attempt-denominator — a rate peak can be the retry schedule | K | novelty (analytics craft) | a data-analysis repository: a strong negative correlation (about −0.9) with the retry schedule | 2026-09-22 or earlier |
| effective-support-before-pruning — zero contribution means redundant or structurally empty, which need opposite decisions | K | generality (ML-narrow) | a data-analysis repository: effective support differing by more than an order of magnitude across scopes | 2026-09-22 or earlier |
| write-audit-publish-by-pointer — immutable runs, a read-back audit, pointer flipped last | K | an occurrence against a real store (known pattern) | a data-analysis repository, not yet run against production | 2026-09-22 or earlier |
| shadow-mode-logs-the-counterfactual — a monitor phase logs the verdict it would have given, down the enforce path | K | a second occurrence | a transactional service | 2026-09-22 or earlier |
| detect-only-control-is-its-queue — an alert-only control is as good as the owned queue that drains it | K | a number; literature is SRE ch. 6 | a transactional service | 2026-09-22 or earlier |
| committed-secret-is-not-a-gitignore-fix — untracking a committed secret fixes neither exposure nor risk | K | overlaps `secrets-survive-rotation`; could extend it | a transactional service | 2026-09-22 or earlier |
| parallel-reads-ordered-verdicts — run independent checks concurrently, replay verdicts in policy order | K | a measured latency number | a transactional service | 2026-09-22 or earlier |
| derive-dependent-config — never configure a value derivable from others | K | a boundary is already known (derivation couples controls) | a transactional service | 2026-09-22 or earlier |
| append-only-collides-with-erasure — decide erasure (TTL, crypto-shredding) when adopting append-only | K | an incident; it is a recorded open collision | a transactional service | 2026-09-22 or earlier |
| obligations-accrue-across-versions — ask "did anything since the subject's version require X" | K | a second occurrence | a transactional service | 2026-09-22 or earlier |
| changelog-order-is-checked — newest-first is audited, not remembered | M | level 2 so far; one repository has several entries out of order | a transactional service | 2026-09-22 or earlier |
| suspect-the-harness-first — when a probe surprises you, the harness is a test double and the first suspect | M | overlaps `test-double-fidelity` and principle 14; could be one sentence there | an interactive client: several occurrences in two days, each a surprise traced to the harness rather than the code — its clock state, the timing of injected input, what a capture included, and a first measurement taken while still warming up | 2026-09-22 or earlier |
| privileged-call-inside-the-host-gesture — a host that grants a capability only during a user activation must receive the request from its own event dispatch; an application loop that handles input a frame later has already lost it, so arm on press and fire on the host's release event | K | a measurement on the target runtime (the design is unverified there); generality beyond browsers | an interactive client: a privileged request armed on press and fired from the host's own release event, with stale arms dropped after about a second; other gesture-gated capabilities requested only from a gesture. Literature: WHATWG HTML, transient activation (verified 2026-09-22) | 2026-09-22 or earlier |
| classify-client-storage-by-rebuild-cost — data kept on a client that may be evicted without notice is classed by whether the application can rebuild it; only what it cannot rebuild is kept verbatim, and loss is made visible | K | an eviction occurrence (the policy is still an open question there) | an interactive client: three kinds of client-side data each handled eviction with a different criterion; files the user imported are kept exactly as they arrived because nothing can rebuild them. Literature: WebKit 2020, a 7-day cap on script-writable storage in the browser, not applied to home-screen web apps (verified 2026-09-22) | 2026-09-22 or earlier |
| accept-only-what-can-win — when two sources can hold the same id, resolve by a version the content carries, send ties to whoever acted last, and refuse at write time what the resolver will never return | K | an incident | an interactive client: the higher content version wins and a tie goes to the user's copy; an import older than the built-in copy is refused at write time because it would be stored and never selected; removing an imported newer version brings the built-in one back | 2026-09-22 or earlier |
| filter-noise-at-parse-in-a-distribution-gap — drop sub-perceptual artefacts where the data is parsed, so every consumer agrees, with the threshold placed in an empty gap of the measured distribution | K | a generality statement; natural-breaks literature not yet consulted | an interactive client: over half of the blank intervals in its timed data were sub-perceptual noise; the largest noise value and the shortest real interval differed by about a factor of two, and the threshold was placed in that gap. Before the filter, two views reading the same data disagreed | 2026-09-22 or earlier |
| probe-compressibility-before-effort — spend compression effort only where a cheap trial shows gain; already-coded media does not compress | K | novelty (close to textbook) | an interactive client: precompression roughly halved the transfer size; the highest level bought about 1 % for tens of seconds of work on a file dominated by already-compressed content, which made up nearly all of the payload and did not shrink; an adaptive step cut the build time by about a third | 2026-09-22 or earlier |
| anchor-to-the-frame-that-does-not-move — a coordinate stored relative to a reference that normally moves predictably is invisible until something else drives that reference | K | a second domain | an interactive client: a handful of view-relative coordinates in one asset stayed invisible until the view became user-driven; the world frame was then made the semantics, with view-relative notation resolved to it once at load | 2026-09-22 or earlier |
| readout-per-silent-outcome — where the runtime cannot be observed, every silent outcome gets a readout with distinct states (not wired, refused, empty, ok), and each question for the remote tester is paired with the readout that answers it | K | an answer from the target runtime; overlaps `fail-closed-defaults` (fail visible) | an interactive client: each handler's registration reported as accepted or refused, a load/save status line per store, a diagram drawn crossed out instead of empty, a brief of open questions each naming its readout | 2026-09-22 or earlier |
| one-field-one-decision — a derived classification that drives two behaviours couples them, and changing its input for one silently changes the other | K | novelty (orthogonality is textbook) | an interactive client: adding one kind of data to an item changed its derived kind and so silently switched off an unrelated suggestion; after splitting the decision into its own field, a small sample showed all items but one unchanged and that one restored | 2026-09-22 or earlier |
| gate-reads-untracked-derived-state — on a checkout that has been away, rebuild the caches the gate reads before trusting its colour | M | overlaps `a-check-must-be-seen-to-fail` and `derived-copy-goes-stale-silently`; would be one bullet under step 0's staleness check | an interactive client: a gate green while its validator could not load; the next day, a validator that failed to parse until a reimport | 2026-09-22 or earlier |
| routine-tools-leave-the-tree-as-found — regenerate only when the source is newer; restore temporary writes in `finally` | M | a second repository; near the existing *Generated artefacts churn* bullet | an interactive client: a couple of hundred lines of id noise per regeneration; a generated configuration restored even on a failed export | 2026-09-22 or earlier |
| document-language-is-its-readers — a document's language follows its reader, not the conversation | M | a second repository; sits against the pre-flight's default of the conversation's language | one occurrence, detail removed for privacy | 2026-09-22 or earlier |
| changelog-ships-field — each entry says whether it changes the delivered artefact, pointing at the release step rather than restating it | M | evidence the field caught anything; the restated action went stale in the one repository that uses it | an interactive client: a dozen or so entries carry the field | 2026-09-22 or earlier |
| execute-the-procedure-to-review-it — reading a ritual with the intent to run it, or an adversarial "what if", finds what re-reading does not | M | a sharper statement; the two occurrences have different forms | an interactive client: a contradiction about where a readout sits survived every earlier read; a versioning defect present for a release was missed by several self-reviews and found by an outside question | 2026-09-22 or earlier |
| non-developer-guide-by-symptom — a guide for a non-developer is organised by symptom, with every command and message checked against the code | M | a second guide | one occurrence, detail removed for privacy | 2026-09-22 or earlier |
| parallel-agents-on-disjoint-files — split bulk work by disjoint file sets, then verify by the invariant | M | a second occurrence | an interactive client: several agents, one file set each, verified by script | 2026-09-22 or earlier |
| no-modification-time-no-window — a source whose records carry no modification time cannot be extracted incrementally by a re-read window alone, and the extractor's own maintenance writes invalidate any window | K | literature (CDC, change-data-capture); a second repository | a data-analysis repository, measured from store metadata: about 1 % of records last changed more than a week after creation and about 0.1 % more than a month after; most of another collection was last written on a single maintenance day | 2026-09-22 or earlier |
| identity-preserving-copy-is-a-no-op-mutation — a "copy" through a constructor that returns its argument (a tuple of a tuple) changes nothing, so the mutation survives for the wrong reason | M | a second occurrence; it would extend principle 18's formatter example | a data-analysis repository: an identity assertion looked like it did not bite until the copy was written as a literal | 2026-09-22 or earlier |
| refusal-must-not-read-like-an-answer — a function given an input outside its domain returns a well-formed value (the hash of nothing, an empty count) instead of refusing, and the caller cannot tell the two apart | K | a second occurrence; overlaps `fail-closed-defaults` (fail visible) and could be one form of it | a data-analysis repository, 2026-09-22: a fingerprint over a path that held no documents returned the hash of empty input, which looks like a valid digest; it now refuses with an exit code | 2026-09-22 or earlier |
| bumping-a-monotone-counter-is-not-idempotent — a command that raises a version must refuse a second run against the same predecessor, because the numbers it burns can never be reused | M | **the second occurrence arrived; what it still lacks is a second *tool***, which is the same gap `a-default-scope-is-the-widest-one` carries, and admitting a note with that gap twice would duplicate the weakness rather than the claim | a data-analysis repository, 2026-09-22, twice in one tool. (1) The same release bump run twice raised the version twice in silence; one number is now held by nobody, skipped with that reason written down and never reissued. (2) The guard covered the set versions and not the per-document ones, so re-stamping a release after a review raised every changed document a second time, for a correction to a release that had not shipped. Found by doing it; fixed with a test and a planted mutation, and the numbers put back | 2026-09-22 or earlier |
| split-a-checked-file-and-the-check-goes-blind — a structural check names the files it reads, so splitting one of them into several removes coverage without turning anything red | M | a second occurrence; overlaps `a-check-must-be-seen-to-fail` and `derived-copy-goes-stale-silently` | a data-analysis repository, 2026-09-22: an index split by area left the audit reading the original file alone — a dead link and a note removed from its area were both invisible; seen red on both after the check was taught the new files | 2026-09-22 or earlier |
| `an-append-only-log-cannot-be-grandfathered-by-line` — the exemption-by-line-number that makes a new rule shippable against pre-existing violations is unavailable in any document written newest-first, because every entry shifts every number | M | a second occurrence. It is a boundary on the existing grandfathering advice, not a new rule, so it may belong as one sentence there | a repository with rebuilt data artefacts, 2026-09-22: retiring a documented path turned 5 correct historical mentions in the changelog into path-check failures. Line grandfathering was unavailable for exactly this reason; the document was exempted whole, and the cost —the largest document in the repository loses path checking— written into the check | 2026-09-22 |
| `a-budgeted-root-file-needs-a-named-displacement-target` + `a-budget-at-its-ceiling-is-a-queue` — a root instruction file under a hard line budget cannot take a new rule without evicting one, and a file that sits **exactly at** its cap across many changes has already started evicting invisibly. Two halves of one claim: how to *detect* that eviction has begun (the cap is touched and held, not exceeded), and how to *choose* what leaves (first, a section already stated verbatim elsewhere) | M | a second repository; and whether it is a form of `a-second-source-of-truth-diverges-in-silence` rather than a claim of its own. Merged by the release of 2026-09-23, which found both rows describing one file | a repository with rebuilt data artefacts, measured 2026-09-23: applying six delta blocks to a 198-of-200-line file needed 10 lines. Nine came from two sections —Verification and Logging obligation— whose prose was **already verbatim in a verification skill and in the changelog's own header**. Evicting them removed a second source of truth rather than a rule, and nothing in the method points at that as the first place to look — and: the same measurement found the root instruction file at exactly its 200-line cap across many commits, dipping to 198 once and refilling in the next change. ⚠️ **The enforcer was also one line looser than the rule it enforced** — the docstring said *under 200*, the code tested *more than 200* — so the file had settled precisely in that gap, which is where a queue forms. The owner then raised the cap to 220, deliberately, which is the right response to the finding and not a fix for it: what the extra room buys is evicting on purpose | 2026-09-22 |
| `conformance-to-an-artefact-spec-is-swept-not-remembered` — adopting a method gives a repository a set of artefact specs, and a repository drifts from them **in the direction the spec did not measure**; so an update, and the meta-session, should each end by **comparing the living artefacts against their specs and reporting the gaps as suggestions**, never as edits. The valuable half is that the drift is usually not the one anybody suspects | M | a second repository, and a boundary on how much of the sweep belongs in the shared method versus in the carrier's own gate — a spec can only be compared against something the carrier knows how to measure, so most of the work is local by nature | a repository with rebuilt data artefacts, measured 2026-09-23: the owner asked for the decisions log to be brought to the new format. It was already in it — four columns, matching the current artifact spec, and the one delta that looked new had been **measured by an earlier session to be byte-identical to a version fourteen releases older**, and declined as a rename. ⚠️ **The real drift was the opposite and nobody had named it**: the spec says that file is *the index that finds the prose*, and its rows had become the prose — **over a third of them longer than a thousand characters, median about a thousand, longest about four thousand**. A one-line advisory in the carrier's gate now reports it on every run. The general claim is that a spec's guarantee and a spec's shape drift apart, and only the shape is easy to check | 2026-09-22 |
| `edit-tools-for-unrebuildable-artefacts-default-to-reporting` — a tool that edits an artefact nobody will rebuild to verify defaults to printing what it would change and writes only when asked; the dry run is not politeness, it is the only review the change will get | M | a second occurrence, and a boundary: for an artefact that IS cheaply rebuilt the default costs a step and buys nothing | a repository with rebuilt data artefacts, 2026-09-23: a tool repairing the metadata of prebuilt 300 MB database packs — an hour to rebuild, and the source dumps are not in the repository. **The default earned itself on its first run against real data**: the rule that a pack's name must end in its declared tier proposed renaming the bilingual pack, which declares a tier and deliberately carries none, because its builder skips that stamping for bilingual packs. Writing by default would have invented a defect out of a deliberate choice, in a file whose correctness nobody would have re-derived | 2026-09-22 |
| `address-by-content-coordinates-not-by-artefact-id` — a reference that must survive the regeneration of what it points into is keyed on **content-derived coordinates** (language, word, ordinal) and never on the artefact's own identity or row id, because both are reassigned by every rebuild | K | a second repository. It reads close to *natural keys over surrogate keys*, and the literature on that was not consulted; what may be new is the **direction** — the claim is not that natural keys are better in general, but that a reference **crossing an artefact boundary** has no choice | a repository with rebuilt data artefacts, measured 2026-09-23: entries are addressed by a code carrying language + word + sense ordinal rather than by file and row id. The same rebuild that changed every row id and renamed five of six artefacts left those references resolving. The one artefact whose id is derived from its **sources** rather than stamped also survived, and is now the only one whose stored history still resolves — the accidental control group | 2026-09-22 |
| `a-contentless-index-makes-row-identity-a-contract` — an index built without a copy of the text identifies its rows by position in the base table, so that correspondence becomes a cross-component contract that **nothing in the storage engine enforces**; drift produces confident wrong answers rather than an error | K | a second repository, and a check on whether this is specific to one engine's contentless mode or general to any positional index | a repository with rebuilt data artefacts: a full-text index is declared contentless to avoid storing the text twice, and its row id **is** the base table's primary key. If the two drift, free-text search returns the wrong records with no error at any layer. It is verified by a dedicated check rather than trusted, and the repository names it among the invariants that fail silently | 2026-09-22 |
| `the-narrower-artefact-declares-its-absorber` — when several artefacts of different breadth can be installed together, each narrower one **declares which wider one supersedes it**, and consumers drop it when that one is present; ranking them at the consumer instead makes coexistence order-dependent and puts the rule in every consumer | K | a second occurrence. The boundary is untested: it assumes containment is total, and a *mostly* narrower artefact would silently lose the rows it alone had | a repository with rebuilt data artefacts, read 2026-09-23: three of six artefacts declare a pointer to their wider sibling, and the query planner drops any whose absorber is loaded. Installing the narrow one before or after the wide one gives the same result, which makes the reverse order a non-question. ⚠️ **Two surfaces in that same repository disagree**: the query path honours the declaration and the attribution path ranks by size instead, landing correctly only because an absorber always happens to be larger | 2026-09-22 |
| `a-version-bump-spends-the-forward-compatibility-it-was-protecting` — a record format whose readers ignore unknown fields can gain fields forever without a version change; the moment a version **is** bumped and compared for equality, every older reader rejects the whole record, so the bump destroys exactly the tolerance that made the format extensible | K | a second repository. The mechanism is textbook (must-ignore semantics); what may be new is the accounting — treating the version field as a **budget that is spent**, and the observation that the cost is invisible at the moment of spending | a repository with rebuilt data artefacts: two new field types were added over time **without** a version bump, deliberately, on the strength of must-ignore. An earlier codec bump had been accepted on the reasoning that it cost nothing at the time — and it stopped costing nothing once the artefacts shipped inside the application, where an old reader meets a new record. The repository wrote that down as the decision aging badly rather than as a mistake | 2026-09-22 |
| `one-field-two-bands-puts-the-cut-in-the-data` — when a single numeric field carries two different semantics over disjoint ranges, the boundary between them travels **in the artefact's metadata**, never as a constant in a consumer: copying it into the consumer creates a cross-language contract that breaks in silence the day the producer re-cuts it | K | a second occurrence, and an argument for why two fields were not simply used instead | a repository with rebuilt data artefacts: a rank field is a frequency score below a boundary and a page-richness score above it, and the boundary is written into the artefact because a consumer needs it to decide which rows carry real signal. The repository already had three cross-language contracts that had each drifted once, and names avoiding a fourth as the reason | 2026-09-22 |
| `a-decoder-that-degrades-to-plausible-output-needs-an-out-of-band-check` — a decoder given the wrong parameters may not fail: it can return well-formed, entirely wrong output. Where that is possible, the parameters carry a hash that is verified before use, because there is no downstream symptom to catch | K | a second domain. Compression is the instance here; the claim is meant to cover any decoder with a shared side-channel (a dictionary, a key schedule, a codebook), and that breadth is untested | a repository with rebuilt data artefacts, measured: records are compressed against a dictionary shared by the whole artefact. Given the **wrong** dictionary the decompressor **does not error** — it returns corrupt text. So the dictionary's hash is stored and verified on open. The repository lists this among the guardrails that look redundant, on the assumption that the decompressor would fail by itself, and are not: it was measured not to | 2026-09-22 |
| `a-cache-key-names-its-invalidators` — a memo over an expensive verification encodes every input that could change the answer — including the **version of the code that produced it** — so invalidation is a string comparison and never a re-derivation; anything left out of the key is a stale answer nobody can detect | K | a second repository. Close to textbook cache-key hygiene; what is worth keeping is the **code version** in the key, which is the term most often omitted because it feels like it is not an input | a repository with rebuilt data artefacts, verified on a target device 2026-09-23: the key is size, modification time, and the versions of the normaliser, the schema, the codec, the check set and the application. Installing a new application version moved all fingerprints and forced re-verification **even for an artefact whose bytes and modification time were unchanged** — which is precisely the case a content hash alone would have missed | 2026-09-22 |
| `an-artefact-the-runtime-maps-cannot-be-compressed` — a payload the runtime memory-maps is stored uncompressed inside its container, so its size is paid in full and the only lever on it is producing less of it; a size estimate that assumed container compression will be wrong by the whole of that payload | K | generality beyond one platform's executable format, and it may be too platform-specific to be a claim at all | a repository with rebuilt data artefacts, measured 2026-09-23 by listing the container: 45.2 MB of executable payload appears with identical compressed and uncompressed sizes, while the data files beside it compress to 25–51 %. It was the larger half of a package that had been estimated at roughly half its real size | 2026-09-22 |
| **Documented default values drift from the code that defines them.** One audit corrected about a dozen drifted defaults in a configuration reference in a single pass — a number, a unit, a removed field, a renamed option | K | Plausible and well evidenced, but the remedy is the interesting part and it is unsettled: generate the reference, check it in the gate, or accept drift and date the document. Needs the remedy before it is a heuristic rather than an observation | a hardware-bound service | 2026-09-22 |
| **Class-level state written from a subclass is not shared with its peers.** A class-level setter wrote onto the calling subclass, so sibling components never saw the state and labels on remotely collected logs silently went missing | K | Language-specific in its current form. Either generalise it to "state whose scope is not where it is written" — which risks becoming a restatement of `in-process-guarantees` — or leave it to the type system. Not yet one claim | a hardware-bound service | 2026-09-22 |
| **A logging context must be owned by the operation observed, not by the component that opened it.** Clearing the logging context when one component finished dropped it from every log line of the operation that followed, which is what operators filter on | K | One occurrence, and it may be a special case of a wider claim about lifetimes crossing component boundaries. Worth a second occurrence before it earns a note | a hardware-bound service | 2026-09-22 |
| **A guardrail should state its real status** — confirmed, partial or gap — and name its assumptions inline, rather than reading as an absolute | M | This is about how a *method artefact* is written, not about building software, so it belongs in `method/` if anywhere. Raised there as a candidate; kept here so the origin is not lost | a hardware-bound service | 2026-09-22 |
