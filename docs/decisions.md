# Decisiones

Índice de todo lo que está cerrado. Una decisión se toma **una vez**: si se reabre sin un hecho
nuevo, la respuesta es este documento.

Los números **no se reutilizan ni se renumeran**. `CLAUDE.md`, el changelog y el roadmap los
citan.

La cuarta columna es la que importa. **Una fila con `—` es una decisión que se puede romper en
silencio.** Está permitido, pero tiene que verse.

La prosa y las mediciones viven en el documento que las posee; esto es el índice que las
encuentra.

## Normalización y claves de búsqueda

| # | Decisión | Por qué | Enforced in |
|---|---|---|---|
| D-003 | La clasificación de code points sale de una tabla propia fijada en Unicode 13.0, nunca de `Character.getType`/`unicodedata.category` | Cada plataforma trae su Unicode: Python 3.9 → 13.0, Java 26 → 16, Android una por release. **14.773 code points se clasificaban distinto**; el mismo pack se habría comportado distinto en dos relojes | `ArchitectureTest` (sha256 de ambas copias) + `repertoire.py` |
| D-004 | NFD y `lowercase()` **sí** se delegan en la plataforma | **0 diferencias** medidas sobre los 133.730 code points del repertorio, entre Java 26 y Python 3.9. La política de estabilidad de Unicode garantiza que la descomposición no cambia una vez asignada | — *(medición, no mecanismo)* |
| D-005 | `norm()`/`fuzzy()` se espejan a mano en Kotlin y Python, sin regex, solo reemplazo literal | Dos regex "equivalentes" divergen en un caso borde que nadie nota. El reemplazo literal tiene semántica idéntica en ambos | `normalization-vectors.tsv`, en los tests de ambos lados |
| D-006 | `NORM_VERSION` distinta ⟹ el pack se rechaza | El pack está indexado con otras reglas: no falla, devuelve menos resultados | `audit_dictionary.py` compara las constantes; `PackFile.open()` rechaza el pack |
| D-019 | El reemplazo portable de una API JVM lleva **nombre distinto** (`appendUtf16`, no `appendCodePoint`) | En la JVM el miembro nativo gana sobre la extensión: el código portable nunca se ejecutaría, funcionando hoy y fallando al compilar para otro target | `audit_dictionary.py` → `check_shadowed_extensions` |

## Formato de pack

| # | Decisión | Por qué | Enforced in |
|---|---|---|---|
| D-001 | Un SQLite read-only por pack, inmutable, sin migraciones | Permite optimizar el esquema solo para lectura. Room queda descartado para packs: `createFromFile()` **copia** el archivo, duplicando decenas de MB en el reloj | `verify_pack.py` |
| D-002 | Los packs se abren con `BundledSQLiteDriver`, no con el SQLite del sistema | FTS5 no está garantizado en Android. El build de androidx trae `ENABLE_FTS5`; verificado además `THREADSAFE=2` y `MAX_ATTACHED=10` | `PlatformAssumptionsTest` (instrumentado) |
| D-009 | El payload es texto delimitado, no JSON ni CBOR | Se parsea sin dependencias en los dos lenguajes y se lee con la vista al depurar. Después de comprimir, la diferencia de tamaño con un binario es ruido | `PayloadCodecTest` + `test_payload.py` |
| D-010 | `form` y `trans` son `WITHOUT ROWID` con PK compuesta | La tabla **es** el índice: sin rowid y sin un B-tree secundario que duplique los mismos datos | `verify_pack.py` (plan de consulta) |
| D-011 | `fts_def` es contentless (`content=''`) y su rowid es `entry.id` | No guarda una segunda copia del texto, que ya vive comprimido en `entry.payload`. Consecuencia asumida: sin `snippet()`/`highlight()` | `verify_pack.py` + `PlatformAssumptionsTest` |
| D-012 | El prefijo usa un covering index y un rango explícito, no `LIKE 'x%'` | `LIKE` solo se optimiza a range scan si `case_sensitive_like` está bien, y ante la duda SQLite hace full scan | `verify_pack.py` + `PlatformAssumptionsTest`, los dos con `EXPLAIN QUERY PLAN` |
| D-013 | `idx_entry_fuzzy` es angosto a propósito: no es covering | Incluir `headword`/`pos` duplicaría varios MB por un camino que solo se recorre cuando el prefijo no dio resultados | — |
| D-014 | `trans` indexa la frase completa **y** cada palabra, con tope `TRANS_MAX_PER_KEY = 50` | Sin tokenizar, buscar "run" no encuentra "to run". Sin tope, "to" apuntaría a decenas de miles de entradas. Se topea en vez de descartar: buscar "to" sigue devolviendo algo útil | `test_build.py` |
| D-016 | Los índices se crean al final, sobre las tablas ya pobladas | Mantenerlos durante la ingesta es mucho más lento | `test_build.py` (no queda staging) |
| D-063 | `schema_version = 3`: `idx_entry_norm` pasa de `(norm, rank DESC, …)` a `(norm, rank, …)`, **ascendente** | En `rank` menor es más común (`schema.sql`), pero el prefijo ordenaba `rank DESC`: devolvía la entrada **menos** común primero. Con 22 entradas de juguete es invisible —`rank` solo desempata dentro de un mismo `norm`—; en el pack real son **375 de 7.265 norms** los que colisionan, y el síntoma se lee: `escrit` daba *escrito / Participio de escribir* (rank 994) antes que el sustantivo (988). Corregir solo la consulta no alcanza: el índice y el `ORDER BY` separados hacen que SQLite agregue `USE TEMP B-TREE` y la consulta deje de ser de cobertura (D-012) | `test_build.py` (orden **y** plan sin `TEMP B-TREE`) + `verify_pack.py` + `PlatformAssumptionsTest` |
| D-064 | `norm` vacío hace fallar el pack; `fuzzy` vacío **no** | No son el mismo problema. Sin `norm` la entrada es inalcanzable por todo camino. Sin `fuzzy` solo queda fuera del nivel tolerante, que es exactamente lo que corresponde a un lema de una letra muda: `fuzzy("h")` es vacío en el perfil español, y **"h" y "H" son entradas reales del Wikcionario**. Tratarlos igual hacía fallar el primer pack real por dos filas legítimas | `verify_pack.py` (falla por `norm`, cuenta las de `fuzzy`) + `test_build.py` |
| D-070 | `verify_pack.py` comprueba que `schema_version`, `norm_version` y `data_version` sean **enteros** | La app les hace `.toInt()` al abrir (`PackFile.parseMetadata`). Un valor que no lo sea no falla al construir ni al validar: falla **en el reloj**, con un `NumberFormatException` que ni siquiera nombra la clave. No es hipotético — el primer pack real se construyó con `data_version = "2026-09-15"` y `verify_pack.py` dio verde | `verify_pack.py` + `test_build.py` |
| D-028 | ~~Presupuesto **blando** de 50 MB por pack~~ **RETIRADO el 2026-09-21 (D-207)**: se rompió hace rato, nunca gobernó una decisión y medía lo que no duele | Ya no es una suposición. El Wikcionario español (1.036.458 senses, dump 2026-09-15) daba **146.194 entradas y 72.212.480 bytes**, un 44 % por encima; tras la poda de nombres propios y la limpieza de markup son **114.619 entradas y 68.132.864 bytes**, un 36 %. El 46 % del pack es la tabla `form` (33,4 MB, 1.487.695 filas, **93,5 % de ellas conjugaciones de verbos**): el precio de que "corriendo" encuentre "correr". Sigue siendo blando y no bloquea; lo que decide si se recorta es O-3, con este número | `verify_pack.py` §tamanos *(reporta, no falla)* |
| D-055 | La clave de join entre packs es **`entry.uid`**, una columna aparte: `entry.id` sigue siendo el rowid secuencial | Medido sobre 200.000 entradas sintéticas: hacer que `entry.id` *fuera* el hash cuesta **+35,2 % de tamaño**, casi todo en `fts_def_data` (10,39 → 28,35 MB) porque FTS5 guarda **deltas** de rowid. La columna aparte cuesta **+2,3 %**. `(norm, pos)` costaría 0 pero funde homógrafos del mismo `pos` en silencio, y degenera con las entradas sin `pos` | `verify_pack.py` (unicidad + receta) + `PlatformAssumptionsTest` + `LogicalIdentityTest` |
| D-056 | `uid` **no lleva índice** en el pack base | El join ocurre al **abrir** una entrada, cuando la fila ya se leyó entera, no en la lista de resultados —que la sirve el covering index sin tocar la tabla (D-012). El índice costaría +4,5 puntos porcentuales por un camino que nadie recorre. El índice vive en el pack auxiliar, que sí busca por `uid` | `PlatformAssumptionsTest.elUidEsUnicoYNoLlevaIndice` |
| D-057 | `uid` lo calcula **solo el builder**, en Python; la app lo lee y nunca lo recalcula | A diferencia de `norm()`/`fuzzy()` no es un contrato espejado entre dos lenguajes, así que no puede divergir (D-005). Agregar un `TextNormalizer.uid()` reintroduciría esa clase de bug entera | `audit_dictionary.py` → `check_forbidden_mirror` |
| D-058 | Dos entradas con la misma identidad lógica **hacen fallar el build**; no se funden ni se desempatan | Cualquier desempate que dependa del orden de inserción rompe justo la estabilidad entre rebuilds que `uid` existe para dar. La fuente entrega `sense_key` para separarlas | `LogicalIdentityTest` |

## Compresión del payload

| # | Decisión | Por qué | Enforced in |
|---|---|---|---|
| D-007 | deflate con diccionario precargado compartido, **no zstd** | deflate está en `java.util.zip` y en el `zlib` de la stdlib. zstd obligaría a una librería nativa en el reloj *además* de la de SQLite, y a una dependencia de pip | `payload-fixture.tsv` (round-trip Python→JVM) |
| D-008 | `meta.payload_dict_sha256` se verifica al abrir el pack | **deflate no detecta un diccionario equivocado**: descomprime sin lanzar nada y devuelve texto corrupto — medido: "moverse rapidamente" → " nadrse rapidamente" | `verify_pack.py` + `PackFile.open()` |
| D-119 | `payload_codec` pasa a **`deflate-v2`** (tag `Y` de sinónimo), aunque el formato ya tolere tags desconocidos | Los dos códecs declaran que *"los tags desconocidos se ignoran, así un builder más nuevo puede agregar campos sin romper una app vieja"*, y subir `CODEC_ID` **tira esa propiedad**: `PackFile.open` lo compara con `!=` y **rechaza el pack**. Se sube igual **porque hoy el costo es cero**: no hay instalador ni catálogo, los packs se sideloadean con `devpack.py`, así que el precio es reconstruir y volver a copiar — y no la Wi-Fi de nadie. ⚠️ **El número que esta fila citaba, 3 min 38 s, es el de COPIAR por adb, no el de construir**: son dos costos distintos. **Medidos los dos el 2026-09-19**, reconstruyendo contra los mismos dumps: construir son **63,6 s el español** (114.619 entradas, 68.132.864 B) y **2 min 45 s el inglés** (794.355 entradas, 268.083.200 B); copiar al emulador son 10,7 s y 54 s, contra los 20 s y 3 min 38 s del reloj por cable. O sea que el precio real de subir `CODEC_ID` es **~1,5 min de build y ~4 min de cable**, no los 3 min 38 s que decía esta fila. Y coincide con cómo el repo trata `schema_version` (D-001): rechazar y reconstruir. ⚠️ **La contracara, que es lo que hay que recordar**: una vez que exista el instalador, **un tag aditivo NO sube `CODEC_ID`** — para eso está la tolerancia, y forzar a redescargar 300 MB por un campo que el lector viejo ignora sería tirarla a la basura | `audit_dictionary.py::check_version_constants` (compara los dos lenguajes) · `payload-fixture.tsv` |

## Portabilidad

| # | Decisión | Por qué | Enforced in |
|---|---|---|---|
| D-017 | Toda API de JVM en `PlatformJvm.kt`; el resto de `:dict-core` es Kotlin puro | Deja la conversión a KMP como tres pasos mecánicos en vez de una reescritura | `ArchitectureTest` |
| D-018 | KMP evaluado y **no adoptado** | Wear Compose es solo Android y Compose Multiplatform no apunta a watchOS: la UI, que es la mayor parte del trabajo restante, no se comparte con ningún segundo destino | — *(decisión, no mecanismo)* |
| D-072 | La lógica de `:app` **no importa `android.*`**: lo que necesita Android entra por parámetro. Las pantallas quedan fuera de la regla | Es la misma forma que D-017 usa para `:dict-core`, por una razón distinta: acá no se trata de portar, se trata de **poder testear en el gate**. `:app` estuvo sin un solo test hasta el 2026-09-17 y no fue pereza — `SearchViewModel` extendía `AndroidViewModel` y construía el pack desde un `Context`, así que no había forma de correrlo en la JVM. Retrofitear costó un refactor de dos archivos ya commiteados. La frontera es `PackLoad`, que no conoce Android y por eso deja pasar un fake. Un Composable es Android por definición y se prueba en dispositivo | `audit_dictionary.py` → `check_app_logic_is_jvm_testable` |

## Plataforma Wear OS

| # | Decisión | Por qué | Enforced in |
|---|---|---|---|
| D-023 | `minSdk 33`, `compileSdk`/`targetSdk 37` | Wear OS 7 = API 37. Bajo API 33 solo queda hardware descontinuado. Targetear 37 evita migración forzada en ~12 meses | `app/build.gradle.kts` |
| D-024 | Wear Widgets pospuesto; se usan Tiles + Protolayout | `androidx.glance.wear:*` y `androidx.compose.remote:*` están en alpha con packages moviéndose, y solo existen en Wear OS 7 | `audit_dictionary.py` → `check_forbidden_dependency` |
| D-025 | **Prohibido** `androidx.glance:glance-wear-tiles` | Deprecado y será removido. El naming confunde: no es la librería de Wear Widgets | `audit_dictionary.py` → `check_forbidden_dependency` |
| D-026 | La búsqueda vive dentro de la app | Tiles y widgets no aceptan text input | — |
| D-027 | Input por voz (`RecognizerIntent`) primero, teclado como fallback | Es la razón de existir del nivel tolerante a errores: el dictado no coincide exacto con ningún lema | — |
| D-029 | Descargas diferidas a **cargando + Wi-Fi** | Guía oficial de Wear OS, literal. Con packs de decenas de MB no es opcional | — *(el instalador no existe todavía)* |
| D-030 | `android:dataExtractionRules`, no `fullBackupContent` | Con `minSdk 33`, `fullBackupContent` es el mecanismo de Android 11 e inferiores. Sin esto, Drive sube decenas de MB regenerables | — *(lint ya reporta `DataExtractionRules`)* |
| D-032 | Dependencias en stable; nada alpha ni rc en el camino crítico | `sqlite 2.8.0-alpha01` y `work 2.12.0-rc01` existen y no se usan | `gradle/libs.versions.toml` |
| D-033 | Kotlin queda en 2.2.10, no sube a 2.4.20 | `:dict-core` tiene `allWarningsAsErrors`: un bump de compilador convierte cualquier warning nuevo en build roto, y nada del roadmap necesita 2.4 | `gradle/libs.versions.toml` |
| D-071 | ~~El diccionario real viaja como asset del APK~~ **Revertida el 2026-09-17.** El APK lleva sólo un **pack de demostración** (53 KB); los diccionarios reales viven sólo en `filesDir/packs/` | La desviación decía que se revertía al existir el instalador. Lo que la adelantó fue **medir el inglés: 295,1 MiB en disco, 184,7 MiB comprimido** — con los dos idiomas el APK se iba a ~270 MB, y por Bluetooth a un reloj eso no es un detalle. El mecanismo de extracción sobrevive porque con decenas de KB el costo de duplicar es ruido; con cientos de MB no. Ver D-081 | `PackStoreTest` (qué se extrae y qué no) |
| D-073 | La lista de resultados es de **una línea con elipsis** y filas de 48 dp; el lema largo se trunca | La pantalla son **192×192 dp** (384 px a 320 dpi) y la guía de Wear OS pide 48 dp mínimos de área tocable: el presupuesto entero da **tres filas**, medido en pantalla. Con el diseño anterior, de dos líneas y ~74 dp, entraban dos. Los refranes del Wikcionario son entradas y llegan a **96 caracteres**: una fila que creciera para mostrarlos enteros gastaría media pantalla en un caso raro, y el lema completo está a un toque | `PantallasTest.entranTresResultadosSinScrollear` y `unLemaLargoNoSeComeLaPantalla` |
| D-074 | La pantalla de entrada muestra **3 acepciones** y un `Ver más (N)` con el resto | Medido sobre las 3.000 entradas de mejor rank, que son las que más se abren: la mediana es **3**, el p90 es 7 y el máximo **47**. Cortar en tres deja la mitad de las entradas intactas —el botón ni aparece— y evita que `poner`, con 24, sea un rollo donde la acepción útil queda debajo de veintitrés. El contador dice el costo antes de tocarlo | `PantallasTest.conMasDeTresAcepcionesSoloSeVenTresYUnVerMas` y `conTresAcepcionesOMenosNoHayVerMas` |
| D-075 | La entrada de texto **cambia de tamaño**: botón de voz grande con la búsqueda vacía, fila compacta en cuanto hay algo escrito | Cada dp de chrome es un resultado que no se ve. Con la búsqueda vacía no hay nada que compita y la voz puede ocupar lo que le corresponde como camino principal (`app/CLAUDE.md`); con resultados en pantalla, lo que importa son los resultados. La voz no desaparece: queda como botón chico al lado del campo | — *(decisión de diseño; lo que sí está enforceado es la densidad, D-073)* |
| D-081 | El APK trae **un pack de demostración de decenas de KB**, generado en el build por `:app:buildDemoPack` y nunca commiteado | Sin ningún pack la app arranca y dice *"No hay ningún diccionario instalado."*: honesto, pero no se puede enseñar. La diferencia con D-071 **no es el mecanismo sino el tamaño** — duplicar 53 KB en disco es ruido; duplicar 295 MiB no. **El contenido está sin decidir**: hoy lo genera `build_toy.py`, el mismo del fixture de tests, y su propia atribución dice que no es un diccionario real, así que el placeholder se delata solo. Cambiarlo es apuntar la tarea a otro `.db` | `:app:buildDemoPack` (el APK no se arma sin él)  **Y nunca le gana a un diccionario de verdad**: se marca por su origen (vino del APK) y queda fuera tanto de la elección del pack activo como del selector. Sin eso lo decidía el orden alfabético y `demo-` ganaba a `es-` — con los dos instalados, la app abría las 28 entradas de juguete en vez de las 146.194 reales, y el selector mostraba *ES* y *ES* sin forma de distinguirlos (D-088) |
| D-082 | El sideload de desarrollo entra por `tools/devpack.py`, es **atómico** y vive **fuera de `:app`** | Un `adb push` directo sobre `files/packs/<pack>.db` no es atómico: cortado a la mitad deja un pack truncado, **que se abre sin error y devuelve menos palabras de las que tiene**. Se escribe a `.part` —invisible para `packsInstalados`, que filtra por `.db`— se compara **sha256 de los dos lados** y recién ahí se renombra, que es lo mismo que ya hacía `instalarAtomico` para los packs del APK. Se manda por **stdin directo** y no por `/data/local/tmp` + `cp`, que duplica el pack en el reloj (590,2 MiB transitorios para el inglés): **medido el 2026-09-17**, `adb shell` es binary-clean y las dos rutas tardan lo mismo (0,73–0,88 s contra 0,80–0,92 s sobre 68,9 MiB), así que el tiempo no decide y el pico de disco sí. **Cero código en `:app`**: la app no tiene rescan, así que se hace `force-stop` antes —que además es el orden correcto, nunca se pisa un `.db` abierto— y se relanza después | `test_devpack.py` (28 tests del plan, el orden y la comparación de hashes) |
| D-084 | A la búsqueda por definición se llega por una **escotilla con la lista vacía**, y nunca se dispara escribiendo | Que no se cuelgue del pipeline incremental es un contrato de la interfaz, no una preferencia: recorre un índice mucho mayor que el de lemas y no cumple el presupuesto de latencia. La escotilla cuesta **cero dp** con resultados en pantalla, y con 192 dp una fila de chrome es un tercio de la lista (D-073). Volver es editar la query: un botón de "volver" costaría 48 dp por un gesto que el usuario ya tiene | `SearchViewModelTest` (6 tests, incluido que escribir no toca el índice) + `PantallasTest` (5) |
| D-085 | El historial guarda las **entradas abiertas**, tres, y se filtra al mostrar | Abrir es la señal de que el resultado sirvió; una consulta escrita puede ser un tipeo a medias, y un historial de "per", "perr", "perro" no ahorra un gesto. Guardando `packId` + `entryId`, volver **abre** en el diccionario correcto en vez de re-ejecutar una búsqueda. Tres sale de D-073: la pantalla da tres filas. Las entradas de un pack desinstalado se filtran **al mostrar y no se podan al guardar**, porque quitar y reponer un pack es un flujo real con `devpack.py` y así el historial vuelve solo | `VisitaTest` (6, el codec) + `SearchViewModelTest` (5, la política) + `PantallasTest` (3) |
| D-086 | La firma del release sale de **entorno → `local.properties`**, y sin keystore el release sale **sin firmar en vez de romper** | Un clone limpio tiene que seguir compilando, y el gate corre en máquinas sin keystore. El entorno primero para que CI o un agente firmen sin tocar `local.properties`, que está gitignoreado y en la lista de archivos que no se editan. **La keystore vive fuera del repo**; `local.properties` guarda sólo su ruta. `:app:releasePrecheck` dice qué falta, porque el síntoma natural —`INSTALL_PARSE_FAILED_NO_CERTIFICATES`— no dice nada | `audit_dictionary.py` → `check_release_signing` (keystore trackeada, contraseña literal, y firmar con la clave de **debug**) |
| D-087 | ~~El Tile y la Complication del template visibles en el reloj~~ **La mitad glanceable se cerró el 2026-09-19** (D-106 a D-109). Sigue vigente el otro costo aceptado: **R8 apagado** | R8 reintroduce la clase de bug que sólo aparece en release y va atado a una comprobación en dispositivo que no se hizo (O-2); sin baseline tampoco se sabría cuánto compró. Lo que decía esta fila sobre la superficie glanceable —*"Example tile"* diciendo *"Hello, Tile!"*, una complication con el día de la semana en inglés y 24 despertares diarios— **dejó de ser cierto**: hay dos tiles de diccionario y la complication se apagó | — *(lo que queda es R8; lo lleva el roadmap O-2)* |
| D-088 | El pack de demostración **nunca es el activo ni se ofrece** si hay otro instalado | Encontrado usándolo, no razonándolo: con el demo y el español real en el reloj, la app abría el de juguete. Ni la preferencia ni el idioma desempataban —los dos son `es`— así que caía al último escalón de `elegirActivo`, que seguía siendo **el orden alfabético**. Es la misma clase que mató D-079, sobrevivida en el último recurso; y con un pack de demo dentro del APK ese recurso se dispara siempre, no casi nunca. El origen (vino de `assets/`) es el criterio, no el nombre: un nombre es justo lo que fallaba | `SearchViewModelTest` (3 tests: no gana, no se ofrece, y sí se usa cuando está solo) |
| D-089 | Todo `item`/`items` de una lista lazy lleva **`key` estable** | Encontrado usándolo en el reloj: escribir la primera letra **cerraba el teclado**. Con la búsqueda vacía la lista es `[encabezado, voz, barra, …historial]` y al primer carácter desaparecen tres de esos, así que la barra salta del índice 2 al 0. Sin `key`, el lazy layout identifica por **posición**: daba el `BasicTextField` por otro nodo, lo destruía y lo recomponía, y el foco —y el teclado— se iban con él. D-075 no cambia: el encabezado y el botón de voz siguen colapsando, que es lo que el presupuesto de pantalla pide. Medido: **el `key` solo alcanzó**, el `FocusRequester` propio que el plan preveía resultó innecesario | `PantallasTest.escribirLaPrimeraLetraNoCierraElCampo`, con un harness que **cambia el estado al escribir** — los 27 tests anteriores pasaban `onQueryChange = {}` y por eso ninguno lo vio |
| D-090 | La tecla de acción del teclado **cierra el teclado y suelta el foco** | Había `ImeAction.Search` declarado y **cero** `keyboardActions` en todo el paquete. `KeyboardActions.Default` no define comportamiento para `Search` —a diferencia de `Next`/`Previous`, que mueven foco— así que la tecla no hacía nada y la única salida era el gesto de volver del sistema. No dispara una consulta: la búsqueda ya corrió por el debounce. Soltar el foco es lo que devuelve la corona a la lista de resultados sin un gesto extra, y está medido que **no** se come el texto que venía componiendo el IME | `PantallasTest.aceptarEnElTecladoSueltaElCampo` |
| D-091 | El atajo a la búsqueda es la **primera fila del scroll** de la entrada, y cuesta **una fila del primer pantallazo** | Hace falta porque tocar palabras apila entradas sobre entradas: volver al inicio desde tres de profundidad son tres gestos. Va en el scroll y no fijo arriba porque fijo costaría 48 dp permanentes, que es lo que D-084 rechazó. **Pero el costo no es cero, y la primera versión de esta decisión afirmaba que sí**: medido, con tres acepciones cortas *Ver más* dejó de entrar en pantalla y dos tests que no scrolleaban se pusieron rojos. La lista abre anclada en el ítem 1 para que la pantalla muestre **la palabra** y no el atajo | `PantallasTest.laPrimeraFilaVuelveALaBusqueda`, y los dos de `Ver más` que ahora scrollean y documentan el costo |
| D-092 | La barra de búsqueda se distingue por **borde**, no por relleno | Usaba `surfaceContainer`, **el mismo token** que una fila de resultado y que el botón *Ver más*: el campo era indistinguible de un ítem de lista, sin borde, sin elevación y sin estado de foco. Se eligió contorno y no relleno entero por consumo: en un OLED un relleno enciende toda la banda y el contorno sólo el perímetro, y se nota igual. No se construye una paleta propia: `theme/Theme.kt` sigue siendo el template vacío y elegir colores es una tarea aparte, con el reloj delante | — *(decisión de diseño, como D-075: se puede romper en silencio)* |
| D-093 | `espresso-core` se fija **a mano en 3.7.0** | `ui-test-junit4` la arrastra en **3.5.0**, de 2022, mientras `runner` y `monitor` ya iban en 1.7.0 y 1.8.0. Su reflexión sobre `InputManager.getInstance` **ya no existe en Android 17**, así que todo test que haga `performClick` o `performTextInput` moría con `NoSuchMethodException` — en el emulador de API 37 y en el reloj, que es API 37 (D-023). Es decir: la capa de tests que cubre gestos estaba **rota en el único nivel de API que importa**, y nadie lo sabía porque ningún test de gestos existía todavía. 3.7.0 es estable, que es lo que pide D-032 | El propio hecho de que los tests de input corran; sin esto fallan todos con el mismo `NoSuchMethodException` |
| D-095 | El APK lleva **`versionCode` monótono y `versionName` humano**, en `gradle.properties` | Nacieron como los literales del template --1 y "1.0"-- y **nada los incrementaba**: ni tarea, ni script, ni CI, ni un tag. No es cosmético: el instalador de Android **rechaza** un APK con `versionCode` menor al instalado, que es la misma trampa que `devpack.py` ya evita para los packs comparando `data_version`. Viven en `gradle.properties` y no en el `.kts` para que subirlos sea una línea que no toca lógica de build; el `.kts` los lee con un default, así que un clone sin la property sigue compilando (misma regla que D-086). **No se derivan de git**: `rev-list --count` rompe en un tarball sin `.git`. El esquema de Wear de Google —`36[targetSdk][producto][release][wear]`— es para entrega multi-APK en Play y esta app se sideloadea: queda anotado, no adoptado | `audit_dictionary.py` → `check_app_version` (entero ≥ 2, las dos properties presentes, y que el `.kts` no las pise con un literal) |
| D-096 | **El inicio es el estado vacío de la búsqueda, no una ruta propia** | La guía de Wear OS pide *"shallow and linear: avoid hierarchies deeper than two levels"* y **elevar la acción primaria** para que el usuario actúe sin navegación profunda. Un menú de inicio que enruta a la búsqueda agrega un nivel y hunde un toque justo lo que más se repite. Cuatro de los seis ítems pedidos ya vivían ahí —voz, texto, historial, créditos— así que fue **mover y agregar dos**, no construir una pantalla. Efecto lateral que cierra una decisión abierta: **D-091 deja de tener dos destinos posibles** y el botón de volver de una entrada va a la búsqueda | `PantallasTest` (5: la palabra del día se ve sin scrollear y abre en su pack, con dos diccionarios se ven las dos con su idioma, el hueco no se ve, todo desaparece al escribir, y el inicio lleva a ajustes) |
| D-097 | Hay **una palabra del día por diccionario cargado**; es determinista por fecha y es **la mejor de 32 candidatas de la categoría que toca ese día** | Determinista = la misma todo el día, sin red, sin estado guardado y sin WorkManager: no se paga batería. Sale casi gratis porque los `id` de `entry` son **densos** (`1..entry_count`, verificado en los dos packs reales) y `entry_count` ya está en la metadata: es un módulo y un lookup por clave primaria. Una por pack y no sólo la del activo, porque con dos idiomas las dos interesan y además cambiar de idioma no recalcula nada. **Tres cosas se descubrieron midiendo, y las tres cambiaron el algoritmo.** (1) Elegir una sola al azar es inservible: diez días dieron *Eyaralar, piscigranja, Ynda, Voorschoten, nonparaxiality*, porque `rank` está **aplastado** —mediana 992 sobre un máximo de 997— y sólo discrimina en la cola, donde se cuelan nombres propios con páginas ricas (`Ivanivka`, rank 529), que es lo que `rank` mide en realidad (D-067). (2) Un **umbral** lo arreglaba pero era **por idioma** —912 en español, 978 en inglés—: un tercer idioma habría necesitado otro número a mano, así que se reemplazó por el mejor-de-32, que no necesita constante. (3) Aun así, **28 días seguidos daban 28 verbos en español**, y no por prevalencia —el pack es 29,2 % sustantivos contra 28,5 % verbos— sino porque las páginas de verbos traen las conjugaciones y son las más ricas; rotando la categoría objetivo por día la misma muestra da sustantivo 12, verbo 8, adjetivo 7, adverbio 1. Es **preferencia y no filtro**: un pack sin adverbios no puede quedarse sin palabra el día que toca adverbio. Se excluyen nombres propios **con sus dos nombres** —`name` en los packs de kaikki, `proper noun` en el de juguete; excluir uno solo deja pasar el otro— más afijos y abreviaturas | `PalabraDelDiaTest` (13, en el gate) + `SearchViewModelTest` (4: que lleguen al estado, una por pack, que cada pack elija distinto, y que sin fecha no haya) + `PantallasTest` (4) |
| D-098 | La píldora y la fila tocable viven en **`Componentes.kt`**, no copiadas en cada pantalla | El mismo molde --recortar en píldora, pintar, hacer tocable, no bajar de 48 dp-- estaba en **seis lugares de dos archivos**. Seis copias de una regla son seis lugares donde alguien baja el área tocable en uno solo y nadie se entera. **No se unifican los seis**: tres son píldoras de texto a todo el ancho y salen de `Pildora`; los otros tres --dos chips del tamaño de su contenido y una fila con icono-- pedirían media docena de parámetros opcionales, que es peor que la duplicación. Lo que sí comparten los seis es la **forma** y `TOUCH_TARGET`, que es donde estaba el riesgo | Los 34 tests de pantalla que ya existían, **verdes sin tocarse**: esa es la prueba de que fue refactor y no rediseño |
| D-099 | El ajuste de tamaño de texto **multiplica sobre la escala del sistema**, nunca la reemplaza | **WO-V1** de la lista de calidad de Wear OS exige que la app respete el tamaño de fuente del sistema. Un ajuste propio que lo pise deja a quien ya subió la letra en todo el reloj viéndola igual que antes. Se aplica como un factor sobre `LocalDensity.fontScale`, y `NORMAL` vale exactamente `1.0` para que no tocar nada sea literalmente no tocar nada. **Dos valores y no un slider**: un continuo no se puede testear contra el presupuesto de densidad de D-073, que define cuántas filas entran | `AjustesTest` (5, en el gate, incluido que `NORMAL` sea 1.0). ⚠️ **Falta** el test de densidad parametrizado por escala, que es lo que cerraría WO-V1 de verdad |
| D-100 | Los dos botones de arriba de una entrada van en **una sola fila**, hechos a mano | La razón es aritmética: lado a lado cuestan **48 dp** —el mínimo tocable— y apilados costarían **96**, que en 234 dp es una acepción menos a la vista. Se construyen con `Row` y no con `ButtonGroup` de Wear Material3 por lo mismo que el selector de idioma: con `allWarningsAsErrors`, una API que se deprecie en el próximo bump **rompe el build**, y acá no se gana nada que lo justifique. El icono lleva `contentDescription` y no `null`: acá el icono **es** la etiqueta, así que sin eso el botón no tiene nombre para un lector de pantalla | `PantallasTest.losDosBotonesDeArribaCompartenUnaSolaFila` (compara `top` y alto de los dos) |
| D-101 | El menú de opciones de una palabra es un **`AlertDialog` de Wear**, no un desplegable ni una pantalla | **Wear Material3 no tiene menú desplegable ni overflow**, verificado contra la referencia de API: las dos formas soportadas son este diálogo o empujar una pantalla de lista. Se eligió el diálogo porque conserva el swipe-para-volver del sistema —que la lista de calidad exige en casi toda pantalla (WO-V3)— y **no agrega un nivel de navegación**, que la guía limita a dos. Se cierra **antes** de ejecutar la acción: varias navegan, y un diálogo abierto encima de la pantalla nueva queda huérfano. Un menú vacío no se ofrece: el botón desaparece en vez de abrir nada | `PantallasTest` (3: la fila, que sin acciones no hay botón, y que la acción tocada se ejecuta) |
| D-102 | Las palabras guardadas **reusan `Visita` y su codec**, con tope de 100 | Son la misma forma de dato que el historial —`packId`, `entryId`, lema, `pos`— así que un segundo codec sería un segundo formato que puede divergir. El tope es **100 y no 3** porque el del historial sale de D-073: tres son las filas que caben compitiendo con los resultados, y las guardadas viven en su propia pantalla sin competir con nada. Existe igual porque todo esto termina en un String de SharedPreferences. **Se compara por `packId` además del id**: los `entryId` son rowids y el 1 existe en todos los packs, así que sin eso guardar una palabra marcaría también la entrada 1 del otro diccionario | `SearchViewModelTest` (5, en el gate, incluida la de los dos packs) |
| D-103 | La **gestión de diccionarios es una pantalla propia**, y el selector rápido se queda en el inicio | Son dos preguntas distintas: *en cuál busco* se responde muchas veces por sesión y tiene que estar a un toque —por eso el selector sigue reemplazando al título del inicio (D-078)—; *cuánto ocupa y cuál saco* se responde una vez cada mucho y necesita cifras que en una fila de 48 dp no entran. La pantalla muestra **sólo los diccionarios ofrecidos**, que es lo que el usuario llamaría "los descargados": el de demostración aparece únicamente cuando es el único que hay, porque `ofrecibles` ya lo esconde cuando hay uno de verdad (D-088). Debajo va la sección de descarga como **WIP explícito**, y el texto dice *cómo se instala hoy* en vez de un "próximamente" que dejaría al usuario sin saber qué hacer | `PantallasTest` (5: tamaño y check, el demo sin borrar, la confirmación, cancelar, y que la sección WIP diga cómo se instala hoy) |
| D-104 | Borrar un pack **cierra las conexiones antes de tocar el disco**, y el de demostración no se puede borrar | No es una precaución teórica: en Unix un archivo borrado con un descriptor abierto **sigue ocupando el disco** hasta que se cierre, y la app lo seguiría leyendo como si nada — el usuario vería "borrado" y cero espacio liberado, que es peor que no poder borrar. **Medido en el emulador**: libre 9.802.568 kB → con el pack 9.732.048 kB → tras borrar **9.802.568 kB otra vez**, el valor exacto. Se cierran **todas** las conexiones y no sólo la del pack que se va, porque recargar reabre el set entero y dejar las viejas sería filtrar una conexión por borrado. El pack de demostración no se ofrece para borrar porque viene dentro del APK y `PackStore.open` lo **re-extrae al reabrir**: la acción no haría nada y el pack volvería solo. Y pide confirmación diciendo el tamaño y el costo de reponerlo: es la única acción de la app que no se puede deshacer desde la app | `SearchViewModelTest` (4, en el gate, incluida la que comprueba el **orden**: que la conexión ya esté cerrada cuando se borra el archivo) |
| D-105 | El tamaño se formatea **sin `String.format`**, en MB decimales y redondeando al más cercano | `String.format` no existe fuera de la JVM —este repo ya tuvo que reemplazarlo una vez (D-019)— y además su separador decimal **depende del Locale**, así que el mismo pack se vería distinto en dos relojes. Decimales (10⁶) y no binarios porque es la unidad en la que el reloj informa su espacio libre, y dos cifras distintas para lo mismo en la misma pantalla confunden más de lo que la precisión aporta. El redondeo tiene que ser **el mismo en todas las unidades**: la primera versión redondeaba kB hacia arriba y MB al más cercano, y eso mostraba 53.248 bytes como *54 kB* | `TamanoTest` (5, en el gate, con los tamaños reales de los tres packs del proyecto) |
| D-106 | La superficie glanceable son **dos tiles** —últimas palabras y palabra del día— y **ninguno abre un pack** | Lo que faltaba era la decisión de producto, no el trabajo: la deuda estaba escrita idéntica en cinco entradas del changelog. Que no abran el pack no es una precaución de rendimiento —que sin medir estaría prohibida (D-042)— sino el **contrato de la API**: `onTileRequest` está anotado `@MainThread` y *"must complete after at most 10 seconds"*, y la guía oficial pide *"don't start long-running asynchronous work in your tile service"* y *"cache or store the results in local storage"*. Los dos leen `SharedPreferences` y nada más: el historial ya guarda `headword` y `pos` desnormalizados (D-085), y la palabra del día la adelanta la app porque es **determinista por (fecha, pack)** (D-097). La palabra del tile es la del **pack activo** y se rehace al cambiar de idioma: dejarla en el anterior sería una palabra equivocada que nadie reporta, porque nadie abre un tile a propósito | `TileContenidoTest` (15, en el gate) + `check_tiles_dont_open_packs` |
| D-107 | La palabra del día usa un **`Timeline` de siete ventanas de reloj de pared**, no `setFreshnessIntervalMillis` | Verificado en el sources jar de `tiles 1.6.2`: freshness es *"how many milliseconds of **elapsed time (not wall clock time)**"*, además *"inexact"* y con throttling. Pedirle 24 h no es "a medianoche": la palabra se correría unos minutos cada día y una palabra del **día** que cambia a las 15:47 dejó de serlo. `TimeInterval` sí es *"milliseconds since the Unix epoch"*, así que en **una sola** respuesta se emiten las siete ventanas y el renderer cambia solo al cruzar la medianoche local: **cero despertares**. El tile de historial va con `freshnessIntervalMillis = 0` —que según el mismo javadoc significa que el sistema no lo refresca solo— y se empuja desde la app con `requestUpdate` cuando el usuario abre o guarda una palabra | `PalabraTileService` + `TileContenidoTest` |
| D-108 | La complication del template **se apaga**, y con ella `UPDATE_PERIOD_SECONDS = 3600` | Mostraba el día de la semana **en inglés** y despertaba el proceso **24 veces al día** para recalcularlo; la guía oficial pide *"disable automatic refresh, or increase the refresh rate to 2 hours or longer"* y desactivar es la primera opción que nombra. No es diccionario, así que subir el período habría sido pagar la mitad por lo mismo. Cierra la fila de §Decisiones abiertas y la mitad glanceable de D-087. La única que la tenía configurada era la watch face del template, que nadie configuró | El `<service>` ya no está en el manifest |
| D-109 | Un tile abre una entrada con un **extra de componente explícito**, nunca con un deep link `<data>` | `ActionBuilders.launchAction(ComponentName, Map<String, AndroidExtra>)` existe y hay `AndroidLongExtra`, así que el `entryId` viaja tipado en vez de como texto a re-parsear. Un scheme convertiría la ruta de una entrada en **API pública del reloj** a cambio de nada: los dos extremos viven en este APK. Pero `MainActivity` ya está exportada —tiene LAUNCHER—, así que los extras **son entrada no confiable igual**: se valida que el `entryId` sea positivo y que el `packId` corresponda a un diccionario **abierto**, y si no, se ignora y la app arranca en la búsqueda. Validar contra los abiertos y no caer al activo es exactamente D-080: caer mostraría **otra palabra**, sin error | `MainActivity.entradaPedida` + el `LaunchedEffect` que espera a `Ready` |
| D-110 | Las pantallas se prueban con **Robolectric en la JVM** y **entran al gate**; sólo lo que el runtime simulado no puede honrar queda en dispositivo | La suite de pantalla tardaba **6 a 10 minutos** y estaba **fuera del gate**, así que una pantalla rota se veía después de commitear. **Los minutos no estaban en los tests**: medido por los artefactos del propio repo, los 31 de `:dict-data` ejecutan en **3,3 s** y los 6 de tiles en **0,05 s** — el tiempo se iba en compilar, empaquetar e **instalar dos APK de 50 MB** en el emulador. Robolectric saca el emulador: **46 de los 47 pasan en 21 s** y el gate en frío queda en **1m07s**, contra 1m26s *sin* las pantallas. El que no pasa es tocar una palabra dentro de una glosa: el nodo del enlace se encuentra y el click se despacha, pero el callback no se dispara, porque el hit-testing cae sobre el rectángulo de una palabra dentro de un párrafo y eso depende del layout de texto real. Vive en `EnlacesEnDispositivoTest` con el motivo escrito. ⚠️ **Robolectric 4.16.1 llega hasta SDK 36** y el proyecto targetea 37 (D-023): sin `robolectric.properties` todo falla con *"Package targetSdkVersion=37 > maxSdkVersion=36"*. O sea que **la suite rápida no corre en el nivel del reloj**, y lo que dependa de API 37 —este repo ya tuvo un caso, D-093— sigue necesitando dispositivo. Otra sesión había descartado Robolectric el mismo día, midiendo contra los tiles: con 47 pantallas la cuenta da al revés | El propio gate: `:app` pasa de 108 a **154 tests JVM** |
| D-111 | El inicio es **la barra de búsqueda arriba** y debajo **secciones con título**: *Palabra del día*, *Recientes* y *Opciones*, con el selector de idioma adentro de Opciones | Buscar es la acción primaria y la guía de Wear OS pide elevarla; quedaba **cuarta**, debajo del encabezado, la palabra del día y el botón de voz. Cada lista lleva título porque sin ellos la palabra del día se confundía con una entrada del historial y el selector con un resultado. ⚠️ **Esto cambia lo que decía D-078**: el selector ya **no cuesta cero filas** —no reemplaza al título— sino una fila propia dentro de Opciones. Se acepta a cambio de que deje de competir por el lugar de arriba. Y **la palabra del día ya no se ve sin scrollear**, que es el costo directo de elevar la barra: queda escrito en el nombre del test. El encabezado sólo aparece **si hay algo debajo** — un título sin contenido es peor que no tener título | `PantallasTest` (4: la barra arriba de todo, las tres secciones con su título, el selector dentro de Opciones, y que no haya sección vacía) |
| D-112 | *Ver traducción* se ofrece **sólo si hay un pack bilingüe abierto** | La versión anterior la ofrecía con cualquier otro pack abierto, y lo que hacía era buscar **el mismo lema** en el otro diccionario. Con dos packs monolingües (D-034) eso casi nunca encuentra nada: *"house"* no es una palabra española. Encontraba préstamos y nombres propios —*chocolate*, *Madrid*— y en todo lo demás era un botón que no hacía nada, que enseña a desconfiar de los que sí. Traducir de verdad exige que el pack **traiga** las traducciones: hoy `trans` está vacía y `MatchKind.TRANSLATION` no devuelve una fila. `PackKind.BILINGUAL` es la señal correcta y no el nombre ni el idioma, porque es lo único que obliga a declarar `lang_dst`. **Hoy: nunca aparece** | `AccionesDeLaPalabraTest` (5, en el gate) |
| D-113 | Las seis pantallas comparten un **margen final** de 32 dp bajo el último ítem | Ninguna lo tenía: las seis pasaban el `contentPadding` del `ScreenScaffold` tal cual, y los últimos ítems sólo traían padding horizontal. En una pantalla **redonda** el borde de abajo se curva hacia adentro, así que el último renglón se veía cortado y no había forma de bajar más. Va como padding y no como un ítem espaciador porque varios tests fijan cuántas filas entran, y un ítem extra los habría movido sin que nadie lo pidiera. Una función en `Componentes.kt` y no 32 dp repetidos seis veces: el día que alguien mida cuánto se come la curva, hay un solo lugar donde tocarlo | El propio uso: las seis pantallas llaman `conMargenFinal` |
| D-114 | Las acciones destructivas **livianas** piden un segundo toque **en el mismo control**, no un diálogo | Borrar el historial borraba al primer toque, y en una muñeca los toques sueltos existen. Un diálogo, en cambio, es lo que se reservó para borrar un diccionario (D-104), que cuesta ~90 s de cable reponer: el historial se rehace solo usando la app. El botón cambia a *Confirmar* y se pinta con `error`, así que el segundo toque es deliberado y se ve | `PantallasTest.borrarElHistorialPideConfirmacionEnElMismoBoton` |
| D-115 | El ícono es **un libro abierto en vector**, con `roundIcon` declarado y sin bitmaps | Llevaba el **bugdroid verde del template** en seis densidades, y `android:roundIcon` **no estaba declarado**, así que en un reloj redondo se recortaba el cuadrado. Se borran los 10 `.webp`: un adaptive icon vectorial se rasteriza a cualquier densidad y pesa menos que una sola de ellas. Un libro y no una lupa porque la lupa es *buscar* y esto es un diccionario —la búsqueda es el medio—; y no una letra, porque una letra pertenece a un alfabeto y la app es multiidioma. El splash usa **el mismo** dibujo, que es lo que pide WO-V15 | — *(es diseño; lo que sí se verifica es que el manifest declare `roundIcon`)* |

## Capa de consulta

| # | Decisión | Por qué | Enforced in |
|---|---|---|---|
| D-050 | Una conexión por pack, confinada a un dispatcher de **un solo hilo** | El SQLite empacado reporta `THREADSAFE=2`: multi-thread, **no** serialized. Una conexión no se puede usar desde dos hilos a la vez | `SqlitePackSource` + `PlatformAssumptionsTest` verifica el pragma |
| D-051 | El nivel tolerante solo se intenta si la cascada confiable devolvió menos de `FUZZY_TRIGGER` | Es el camino más caro y el menos confiable. Agregar candidatos por distancia cuando ya hay resultados buenos solo ensucia la lista | `SqlitePackSourceTest`; el fixture lo protege en `ToyPackFixtureTest` |
| D-052 | Los umbrales del nivel tolerante (`FUZZY_TRIGGER`, `FUZZY_PREFIX_LENGTH`, `MAX_EDIT_DISTANCE`) son **provisorios** | Elegidos a priori, sin medición. Con un pack de 22 entradas no significan nada: se ajustan con el pack real (roadmap O-1) | — *(sin medición; es lo que O-1 existe para arreglar)* |
| D-053 | Todo token de una búsqueda full-text se envuelve en comillas antes de llegar a FTS5 | Sin eso, un `"` suelto o un `OR` escrito por el usuario cambian la consulta o la hacen fallar | `SqlitePackSourceTest.elTextoLibreNoSeRompeConSintaxisDeFts` |
| D-068 | El prefijo ordena **`(exacta primero, rank, norm)`** y deduplica por `(headword, pos)`, aceptando que el covering index ya **no** satisfaga el `ORDER BY` | Ordenar por `norm` es ordenar alfabéticamente, y con 146.194 entradas eso entierra la palabra buscada: medido, `perro` salía en la **posición 619 de 782** para el prefijo "per", y la lista muestra 30. Con este orden sale **5ª**. El costo es un `USE TEMP B-TREE` sobre el rango: **1,8 ms p95 en el peor caso** (una letra, 22.358 filas) contra 0,01 ms, con un presupuesto de 20 ms — **en escritorio; el número de reloj falta (O-1)**. La deduplicación es en Kotlin con over-fetch ×3 y no `GROUP BY`: el `GROUP BY` cuesta 7,9 ms p95 y **no saca los duplicados que se ven**, que difieren en `pos` | `SqlitePackSourceTest` (orden, exacta primero y sin repetidos) + `test_build.py` (el fixture tiene las dos trampas) |
| D-069 | La deduplicación va **al final de la cascada**, no dentro del nivel de prefijo | `accumulated` se indexa por `entryId`, así que dos entradas distintas con el mismo lema y pos sobreviven las dos. Lo encontró un test: deduplicando solo en `byPrefix`, el **nivel tolerante volvía a meter** la entrada que el prefijo ya había fundido. Hacerlo después de ordenar además conserva la de mejor nivel y mejor score, en vez de una al azar | `SqlitePackSourceTest.noRepiteElMismoLemaConElMismoPos` |
| D-083 | `searchDefinitions` **reimpone el orden de FTS5** antes de devolver, en memoria y no con un `JOIN` | FTS5 ordena por bm25 y después `WHERE id IN (...)` devuelve en orden de **rowid**: el ranking se calculaba entero y se tiraba, así que la definición que mejor coincide no encabezaba. Es la misma clase de bug que hacía que `per` no devolviera `perro`. Se reordena con un mapa —lo que ya hacía `byFuzzy`— y no con un `JOIN` porque el `JOIN` cambia el plan de consulta, y acá un plan no se cambia sin medirlo (D-012). Son 30 filas como máximo | `SqlitePackSourceTest.elOrdenDeRelevanciaDeFtsNoSePierde` + la trampa del fixture, que **sí** corre en el gate |
| D-094 | Una palabra dentro de una glosa es navegable **sólo si es lema**, y todas se resuelven en **una consulta sin `ORDER BY`** | El color es una promesa: pintar tocable algo que no lleva a ningún lado enseña a no confiar en el color. Una consulta por pantalla y no una por palabra — `norm IN (…)`, servida entera por `idx_entry_norm`. **Sin `ORDER BY rank`** aunque haga falta el mejor rank: ordenar globalmente sobre un `IN` obliga a SQLite a un `TEMP B-TREE` y la consulta deja de ser de cobertura (D-012, mismo efecto que midió D-063). El mejor rank se elige en Kotlin sobre el puñado de filas que devuelve una glosa. Qué es una palabra **no lo decide el tokenizer**: un code point abre palabra si y sólo si `norm()` lo conserva, porque una tabla de puntuación propia sería una segunda fuente de verdad sobre el invariante central (D-003, D-005) | `GlossTokenizerTest` (7, **en el gate**) + `SqlitePackSourceTest` (4, incluida la de mejor rank con las dos entradas que normalizan a `arbol`) + `PlatformAssumptionsTest.resolverPalabrasDeUnaGlosaNoArmaUnaTablaTemporal` |

## Rendimiento y batería

| # | Decisión | Por qué | Enforced in |
|---|---|---|---|
| D-042 | No se optimiza nada sin una medición previa | Hoy los cuatro presupuestos de `docs/formato-pack.md` son objetivos escritos a priori, sin un solo número real detrás | `CLAUDE.md` §Verificación; `benchmark` skill |
| D-043 | La correctitud se verifica en **emulador**; el rendimiento y la batería, en **reloj físico** | La imagen del emulador trae el ICU y el SQLite de su nivel de API, así que sirve para normalización y planes de consulta. Para rendimiento la guía oficial pide *"physical Wear OS devices"* | — *(convención; la lleva el `benchmark` skill)* |
| D-044 | La batería se mide con el power metric de Macrobenchmark, Perfetto o el Power Profiler | **Battery Historian ya no se mantiene**, según su propia documentación, y es lo que recomienda casi toda la guía de terceros | — |

## Contenido y licencias

| # | Decisión | Por qué | Enforced in |
|---|---|---|---|
| D-015 | Los packs se construyen en `tools/packbuilder`; la app nunca parsea fuentes crudas | El JSONL del Wikcionario son 1,1 GB. La poda es trabajo de ingeniería, no configuración | `audit_dictionary.py` (sin parsers en `:app`/`:dict-core`) |
| D-031 | El contenido viene de Wiktionary (CC BY-SA); la pantalla de atribución **no es opcional** | El DLE de la RAE no tiene licencia abierta ni API pública. Wiktionary es efectivamente la única fuente utilizable para definiciones en español | Tres capas, y ninguna sola alcanza: `audit_dictionary.py` → `check_attribution_screen` (en el gate, comprueba que la pantalla exista y lea de `meta`), `PantallasTest.laAtribucionMuestraLaLicenciaYLaFuente` (en dispositivo, comprueba que se **vea**) y `SearchViewModelTest` (que el estado la traiga del pack) |
| D-034 | El primer pack es **monolingüe con definiciones**, español | Decisión de producto. Cambia el caso primario del esquema: `trans` pasa a ser opcional, porque en monolingüe duplica lo que `fts_def` ya indexa mejor | — *(el roadmap lo lleva)* |
| D-065 | Las páginas de forma flexionada (`form-of`) no son entradas, pero **la fuente hace dos pasadas** para recolectarlas como formas de su lema | Son el **82,33 %** de los registros del dump (703.506 de 854.460). Parecía que bastaba una pasada, porque el lema trae su conjugación en `forms` (`amigar` trae 137 formas). **Medido, no basta**: el `forms` de los lemas cubre el **92,31 %** de las palabras-forma y el 7,66 % restante —**53.708 palabras**, entre ellas "palpitaciones", "curvilínea" y "animalito"— se perdería sin error y sin log | `test_source_kaikki.py` |
| D-066 | Las páginas de forma **sin** el tag `form-of` ("Participio de escribir") **se conservan como entradas**, aunque sean el 19,35 % | Son ruido en la lista de resultados y cuestan ~7 % del payload, así que descartarlas es tentador: hay una señal estructural buena, `tags: [form-of]` a nivel de registro, que agarra 28.414 de ellas. **La medición lo prohíbe**: 1.341 de esas palabras (4,72 %) no llegan a ningún lema por ningún otro camino. En un repo cuyo único bug invisible es *falta una palabra*, 4,72 % de pérdida no se cambia por 7 % de tamaño | `test_source_kaikki.py` *(la poda; la conservación es por omisión)* |
| D-067 | `rank` en el pack del Wikcionario es un **proxy de riqueza de la página**, no frecuencia de uso | El Wikcionario no trae frecuencia y cruzar un corpus externo suma una fuente y una licencia. El proxy suma acepciones, ejemplos, formas, traducciones y etimología. **Medido y sirve**: ordenando por él, `perro` sube de la posición **619 a la 5** en el prefijo "per", y "hac"/"com" encabezan con `hacer` y `comer`. Se declara proxy a propósito: es reemplazable por frecuencia real sin tocar el formato | `test_source_kaikki.py` §RankTest *(que ordene bien es medición, no mecanismo)* |
| D-076 | El pack de inglés es **enwiktionary sección English**, monolingüe, y se construye con el mismo pipeline que el español | La poda resultó ser **estructural, no del idioma**: se apoya en los tags `form-of` de wiktextract, iguales en todos los dumps. Lo único que no se hereda es la calibración del `rank`, que vive en un `Perfil` por idioma. Medido: **956.150 entradas, 309.424.128 bytes (295,1 MiB)**, build de 180,6 s y 290 MB de RSS, desde el dump de 3.244.676.342 B del 2026-09-09 | `test_source_kaikki.py` §IdiomaTest + `verify_pack.py` |
| D-077 | **Por qué pesan los packs, medido** — y la respuesta es distinta en cada idioma *(actualizada por D-116: los nombres propios ya no son un recorte hipotético, son el default; ES 72,2 → 68,1 MB, EN 295,1 → 255,7 MiB)* | Se creía que un pack es sobre todo morfología. Es una verdad **del español**: ahí `form` es el 47,4 % (1.487.695 filas, 93,5 % conjugaciones). En inglés `form` es el **7 %** y `entry` el **48 %**: el pack inglés pesa porque tiene **6,5× más entradas** (956.150 contra 146.194), no por flexión. También se midió qué se podría recortar y **el almuerzo gratis no existe**: sólo el 0,7 % de `form` (0,2 MB) es redundante con la búsqueda por prefijo; podar `form` por divergencia ≥4 ahorraría 13,6 MB a cambio de que **602.681 formas dejen de resolver**; los 163.470 nombres propios del inglés cuestan **40,7 MB (13,8 %)**. Todo recorte cuesta una función de búsqueda | — *(medición; vive en `docs/formato-pack.md` §Presupuestos)* |
| D-078 | Con varios packs se **elige uno**, no se fusionan resultados | Fusionar es composición y necesita `SearchRepository`, que no existe. Un selector reusa lo que ya había: `source` ya era un flow dentro del `combine`, así que cambiar de idioma es asignarle otro valor y la query vigente se repite sola. **El selector no cuesta una fila de resultados** —con 192 dp serían un tercio de la lista (D-073)—: reemplaza al título con la búsqueda vacía, y con query sin resultados aparece como *Buscar en \<idioma\>*, que es justo cuando sirve | `SearchViewModelTest` (6 tests) + `PantallasTest` (6 tests) |
| D-079 | Sin preferencia guardada, el pack activo lo decide **el idioma del reloj**, nunca el orden alfabético | No es una preferencia: es un bug que ya existía. `packInstalado` devolvía el primer `.db` alfabético, así que instalar inglés habría escondido el español **en silencio** —`en-` ordena antes que `es-`—. Tres escalones: el pack exacto de la última vez, cualquiera de ese idioma, y por último el que venga | `SearchViewModelTest.conDosPacksArrancaEnElGuardado` y `siNoHayNadaGuardadoManaElIdiomaDelReloj` |
| D-080 | La entrada se abre **en su propio pack**: la navegación lleva `packId` además de `entryId` | La ruta pasaba sólo `entryId` y tiraba el `packId` que `Suggestion` ya traía. Con dos packs abiertos, tocar un resultado de inglés lo resolvía contra el pack activo y mostraba **otra palabra**, sin error. `entry(packId, …)` devuelve `null` si ese pack no está abierto en vez de caer al activo: caer sería el mismo bug, pero mudo | `SearchViewModelTest.abrirUnaEntradaLaBuscaEnSuPropioPack` |
| D-116 | Los packs **no traen nombres propios**, salvo los que tienen vida léxica | `pos = name` es el **22,1 % de las entradas en español** (32.305) y el **17,1 % en inglés** (163.470, **40,7 MB**). No es que pesen: es que **diluyen**. **26.265 entradas españolas tienen como definición completa la palabra "Apellido."** — el 18 % del pack — y en inglés hay **4.267 `norm` donde el topónimo le gana en rank a la palabra común**: buscar *freedom* devolvía primero *Freedom*, *"a census-designated place in Santa Cruz County, California"*. De los 29.599 `norm` que el español pierde, el **90,4 % no definía nada**, y una muestra al azar da *Etchechury, Bazterra, Huentepan, Llacaleo*. ⚠️ **La poda a secas se llevaba puesto "January"**: los meses en inglés son nombres propios y **6 de los 12 desaparecían** (sobreviven *march*, *may*, *august* por ser también verbo, modal y adjetivo). La excepción es **estructural y medida**, no un regex sobre la glosa: se conserva el nombre propio con `translations + descendants + derived >= 5`. January 69, February 50, Paris 172, Moscow 330, España 77, Chile 73 — contra **0** de Hopewell e Ivanivka. Recupera 1.675 entradas en inglés (1,0 % de los nombres propios) y 731 en español (2,3 %). **El costo aceptado**: vuelven ~350 nombres de pila españoles (*Jorge*, *María*), porque un nombre de pila tiene traducción a muchos idiomas. El regex sobre la glosa se descartó: en inglés sólo captura 86.957 de 163.470, y contradice el docstring de la fuente (la poda es estructural, D-076) | `verify_pack.py` (`meta.proper_nouns` en `REQUIRED_META` + que sean minoría) · `test_source_kaikki.py` (4) · `test_build.py::PacksDeclaradosTest` y `PoliticaDeContenidoTest` (5) |
| D-117 | Los sinónimos del Wikcionario entran al pack, **por acepción**, en **los dos idiomas** *(ampliada por D-124: decía «sólo en español» y eso era incorrecto)* | 26.845 entradas españolas (17,8 %) traen `synonyms` y el builder los tiraba. Es la única ganancia de contenido disponible **sin cambiar de fuente**, y ataca justo lo que se siente incompleto: el 28,0 % del pack son entradas cuya definición completa cabe en 25 caracteres. **Por acepción y no por entrada, porque la fuente lo permite**: el 100 % de los `synonyms` traen `sense_index`. *domingo* tiene `mesada · paga` en la acepción de la paga semanal y nada en la del día; juntos no significan nada. ⚠️ **La clave es el `sense_index` que declara la fuente, NUNCA la posición**: `_senses()` poda las acepciones `form-of` antes de emitir, así que los ordinales se corren y un `enumerate()` le cuelga a la acepción que sobrevive los sinónimos de la que se fue — sin lanzar, sin log, y `verify_pack.py` no lo ve. **Sólo español**: en inglés **0 de 43.679** sinónimos traen `sense_index` (traen `_dis1`, un vector de pesos, y `source: "Thesaurus:*"`) y el ruido es estructural — *cat* figura como sinónimo de *cat*, y en español *chulengo* también se lista a sí mismo. Un sinónimo sin `sense_index` se descarta (5 en todo el dump): colgarlo de la primera acepción sería inventar la atribución. Tope de 4 por acepción. **Medido en el pack real: +0,89 MB** — tres veces la estimación, porque `fts_def` los indexa además del payload | `test_source_kaikki.py::SinonimosTest` (6) · `test_payload.py` (3) · `PayloadCodecTest.kt` (3) · el fixture |
| D-118 | Los sinónimos entran a `fts_def`, **no** a `trans` ni a un `MatchKind` propio | Si sólo fueran al payload se verían recién al **abrir** una entrada que ya encontraste, o sea cuando ya no hacen falta. `fts_def` ya existe y no cuesta tabla nueva. `trans` queda descartada por D-034: en un pack monolingüe duplica lo que el índice ya hace mejor. ⚠️ **Lo que esto NO cierra**: el orden de los resultados de texto libre sigue saliendo del proxy de `rank` (D-067), así que buscar un sinónimo **encuentra** la entrada pero puede devolverla lejos — es la misma deuda de ranking ya abierta, no una nueva | `test_build.py::SinonimosEnElIndiceTest` (comprueba además `fts_def.rowid == entry.id`, D-011) |
| D-120 | **Open English WordNet queda medido, no adoptado**: el pack de inglés sigue siendo enwiktionary podado | Spike construido de verdad (`sources/oewn.py`, WN-LMF XML de 11,4 MB, CC BY 4.0, versión 2025). **El número: 135.969 entradas en 38,4 MiB, contra 794.355 en 255,7 MiB — 6,7× más chico**, y con **sinónimos en el 70,8 % de las entradas**, que es justo lo que el dump inglés de kaikki no puede dar (D-117). Las definiciones son limpias y modernas. **Lo que lo frena, medido con una sonda dura de 39 palabras: OEWN cubre 64 % contra 100 % del Wiktionary podado.** No tiene *selfie, blockchain, deepfake, ghosting, woke, burnout, workaround, mitochondria, petrichor*: es vocabulario académico congelado, flojo en todo lo posterior a 2000. En vocabulario común empata (56/57 contra 57/57). **Por eso no reemplaza**: 217 MiB no valen el 36 % del vocabulario moderno cuando los 364 MiB de packs ya entran en el reloj con 40 GB libres. Queda como candidato a **segundo pack liviano**, que es una decisión distinta y bloqueada por "dos packs del mismo idioma se pisan". ⚠️ El spike dejó dos cosas aprendidas a golpes: el guard de colisión de `uid` (D-058) **saltó dos veces** —`pate`, dos `LexicalEntry` sin nada que las separe, y `green`, porque `a` y `s` (adjetivo satélite) mapean los dos a `adj`— y que aborte fue correcto las dos veces | `tests/test_source_oewn.py` (6, **de caracterización**: excepción declarada a "el test va primero", porque es un spike) |
| D-121 | Las **etiquetas de mantenimiento del wiki** se sacan de la glosa | wiktextract las deja incrustadas en `glosses` y **no hay versión limpia**: `raw_glosses` es `None` en todos los casos medidos. Son **774 acepciones** en español — 671 `[cita requerida]` y 103 `[definición imprecisa]` —, y en un reloj *"Pene.^([cita requerida])"* gasta media pantalla en decirle al lector que un editor del Wikcionario quería una fuente. ⚠️ **El patrón exige los CORCHETES y eso no es un detalle**: en inglés `^(...)` sin corchetes es **superíndice matemático** —`10^(100)`, `e^(iπ)`, `2^(2/r)`— así que un filtro más ancho destruiría contenido en vez de limpiarlo. Con corchetes hay un solo caso en inglés, `[sic]`. Se saca también el punto que queda colgando, porque la fuente escribe `"...los labios.^([cita requerida])."` y quedaban dos seguidos. **Efecto medido: 665 → 0 acepciones sucias, y el pack pierde exactamente una entrada** — `arterializar`, cuya definición completa en el Wikcionario *es* `^([cita requerida])`, o sea que no tiene definición | `test_source_kaikki.py::MarkupEditorialTest` (4, incluida la que protege la notación matemática) |
| D-122 | **Los ejemplos de uso de enwiktionary §Spanish quedan medidos y pendientes**, no descartados | ⚠️ **Corrige un error de esta misma sesión.** Se había concluido *"cero ganancia de cobertura"* comparando **totales** (118.458 contra 113.889); medido el cruce real por `norm()`, **el solapamiento es sólo del 44 %** y enwiktionary tiene **56.741 lemas que el Wikcionario no tiene** — unión de 160.919, **+54 %**. Aun así **no se fusiona**, por un motivo distinto del que se creía: sus glosas **no son definiciones sino traducciones al inglés** (*entretecho* → "loft; attic; garret"). Eso es un **pack bilingüe**, que el esquema ya contempla, y no una mejora del monolingüe (D-034). **Lo que sí sirve son los ejemplos**: de los 46.326 lemas compartidos, 38.306 no tienen ejemplo acá y **5.307 sí lo tienen allá, en español**, con la traducción inglesa en un campo aparte — subiría los compartidos de 17,3 % a 28,8 %. **Por qué no se hizo ya**: el riesgo no es la licencia (las dos son CC BY-SA 4.0) sino **a qué acepción se pega el ejemplo**. Cruzar por lema y no por acepción pone *"Lo acordaron por unanimidad"* en la acepción equivocada de *acordar* — contenido incorrecto que parece correcto, que es peor que *falta una palabra* | — *(pendiente; el dataset queda bajado en `wearos-dictionary-data/es-en-wikt.jsonl`)* |
| D-123 | El historial y las guardadas **se validan contra el lema** al abrirse, y se corrigen solos si el pack se reconstruyó | `entry.id` es la identidad **física** —el rowid— y **no sobrevive a un rebuild** (D-055): una palabra nueva en el medio corre todas las siguientes. Las preferencias se respaldan con `entryId`, así que tras reconstruir un pack el historial abre **otra palabra, con el lema correcto escrito en la fila** — sin error y sin log. Estuvo latente desde que existen las preferencias y **D-116 lo dispara de verdad**: el pack español pierde 31.575 entradas, así que prácticamente todos los ids se corren. ⚠️ **No se respalda `entry.uid`, que sí es estable**, por dos razones que ya están decididas: no tiene índice en este pack (D-056), así que resolverlo cuesta un scan de 114.619 filas en español y 794.355 en inglés **por toque**; y la app tiene prohibido calcularlo (D-057), que es lo que evita que `uid` se vuelva un segundo contrato cruzado como `norm()`. El lema, en cambio, entra por `idx_entry_norm`, que ya existe. **Cuesta cero consultas extra en el caso normal**: si el lema que hay en ese id es el guardado se abre directo y `resolveHeadwords` no se llama. Cuando corrige, **reescribe el respaldo**, si no cada apertura volvería a pagarlo. **El costo aceptado**: entre homógrafos del mismo `pos` —3.024 pares en español, los que separa `sense_key` en `entry.uid`— se abre el mejor rankeado. Es estrictamente mejor que abrir una palabra sin relación. El lema viaja además en el intent del tile (`EXTRA_HEADWORD`), porque publica ids de `SharedPreferences` con el mismo problema; si no viene, se cae a confiar en el id, que es lo que se hacía antes | `DestinoDeVisitaTest` (7, en el gate) |
| D-124 | Los sinónimos ingleses entran por la forma **anidada en `senses[]`**, y la lista de idiomas desaparece: **decide la fuente** | ⚠️ **Corrige D-117**, que decía que el inglés no podía dar sinónimos porque *«0 de 43.679 traen `sense_index`»*. Ese número es correcto y la conclusión no: **se midió sólo la forma de arriba**. El dump inglés los sirve en **otra forma**, anidados dentro de cada acepción, donde la atribución es **estructural** —el item ya vive en su acepción— en vez de declarada. Medido sobre 120.000 registros vivos de cada dump: `synonyms` arriba es 16,5 % en español y 5,6 % en inglés; **dentro de `senses[]` es 0,0 % en español y 25,8 % en inglés**, y **ninguna entrada usa las dos** — así que no hay precedencia que decidir, es una unión. `IDIOMAS_CON_SINONIMOS = {"es"}` se borra en vez de ampliarse: `_synonyms_by_index` ya descarta lo que no trae `sense_index`, así que la forma de arriba del inglés **se cae sola**, sin puerta. Un default que se sostiene por lo que la fuente trae es más difícil de dejar desactualizado que uno que se sostiene por una constante. **El resultado medido en el pack real: el inglés pasa de 0 a 122.454 entradas con sinónimos (15,4 %) y 240.195 items, por +2,82 MB sobre 268,1 — un +1,05 %** (se había estimado 0,6 %). Buscar *noggin* en `fts_def` ahora devuelve *head*. ⚠️ **Lo que se midió y NO se hizo**: el 74,5 % de los items ingleses traen `source: "Thesaurus:*"` y aparecen primero, así que el tope de 4 parecía quedarse con lo oscuro; **reordenar para preferir la fuente propia cambia 91 acepciones de 4.872 mezcladas (1,9 %) y en la muestra empeora** — `craft` pasa de `ability, aptitude` a `craftiness, foxiness`. Se respeta el orden del dump | `SinonimosAnidadosTest` (6) · `SinonimosTest` (7, con la de arriba reescrita sin la puerta por idioma) |
| D-125 | El `name` del pack es **corto** y los detalles van a una clave nueva, `description`; el tipo de diccionario sale de `kind` | El nombre era *«Español — definiciones»*, 22 caracteres, y se mostraba **en cuatro lugares que son filas de reloj**: el selector del inicio, la pantalla de diccionarios, el diálogo de borrar y la atribución. Se cortaba en los cuatro — reportado como *«el nombre del pack sigue viéndose cortado como Español - definic…»*. Pasa a ser *«Español»* / *«English»*. **Lo que la otra mitad comunicaba no se pierde: se calcula.** `etiquetaDeTipo(kind)` da *definiciones* o *traducción* desde un **dato del pack**, no desde una cadena que alguien tiene que acordarse de escribir bien en cada pack nuevo; el `when` es exhaustivo, así que un tercer `PackKind` no compila hasta que se le dé etiqueta. `description` es **opcional en el lector** (`meta[...]`, no `getValue`): un pack anterior a esta fila sigue abriendo y simplemente no dibuja esa línea. Va a la pantalla de atribución, que es donde alguien lee, y no a una lista que hay que scrollear | `EtiquetaDeTipoTest` (4, incluida una que falla si dos tipos comparten etiqueta) · `PantallasTest.conDosDiccionariosSeVenLasDosPalabrasYSuIdioma` |
| D-126 | Los **antónimos** entran al pack con el tag `A`, **sin subir `CODEC_ID`**, y **no van a `fts_def`** | Pedidos explícitamente, y la medición decide que van en el mismo pack y no en uno aparte: sobre 120.000 registros vivos de cada dump son el **2,1 % en español** (forma de arriba, con `sense_index`) y el **3,2 % en inglés** (forma anidada) — el mismo espejo que los sinónimos de D-124 — por **0,80 B por entrada viva**. Los dos lectores se generalizaron a tomar la clave en vez de duplicarse: son literalmente la misma forma con otro nombre, y dos copias divergen el día que alguien arregle un borde en una sola. ⚠️ **No sube `CODEC_ID` y es deliberado**: D-119 dejó escrito que un tag aditivo no debe subirlo, porque para eso existe que los tags desconocidos se ignoren; un lector viejo muestra la entrada sin antónimos, que es degradación correcta. ⚠️ **Y NO entran a `fts_def`, al revés que los sinónimos (D-118)**: un sinónimo es otra forma de nombrar lo que buscás, un antónimo es lo que **no** buscás. Indexarlos haría que escribir *frío* devuelva *caliente*, con el orden de resultados —que ya es deuda abierta (D-067)— decidiendo qué tan arriba aparece esa respuesta invertida. Hay un test que lo fija, porque sin él alguien lo agrega a `_fts_body` por simetría y el pack sale más grande y la búsqueda más ruidosa, en silencio. En la pantalla el prefijo `ant.` **no es decorativo**: las dos listas comparten estilo, posición y separador, así que esas cuatro letras son lo único que separa *otra forma de decirlo* de *lo contrario* | `AntonimosTest` (6) · `AntonimosFueraDelIndiceTest` (1) · `test_payload` (4) · `PayloadCodecTest` (4, una sobre el fixture con los dos tags cruzados) · `PantallasTest.laAcepcionMuestraSusAntonimosYNoLosConfundeConSinonimos` |
| D-127 | La app es **bilingüe por recursos**: `values/` en inglés es la base y `values-es/` la copia en español | Pedido explícito. La base es inglés porque es el idioma del repo (`CLAUDE.md`) y el fallback de Android: un reloj en cualquier idioma que no sea español ve inglés en vez de ver español que no entiende. El español es **neutro latinoamericano** (`app/CLAUDE.md`). ⚠️ **Lo que esto obliga en la lógica**: `:app` no puede traducir donde no puede importar `android.*` (D-072), así que las capas puras dejan de inventar texto y pasan a emitir un **dato**: `packTypeLabel` sale de `PackSet.kt` a la pantalla, y `SearchViewModel` deja de fabricar *"No hay ningún diccionario instalado"* para emitir un estado que la pantalla traduce. ⚠️ **Y el riesgo que hay que decidir antes de terminarlo**: 178 tests JVM afirman texto en español literal; con la base en inglés, Robolectric resuelve `values/` y fallan en bloque. Hay que elegir entre fijarles el locale español o pasar sus aserciones a inglés, y no es obvio cuál — el primero conserva los tests tal cual, el segundo ejercita la base. **Resuelto**: los tests corren con `qualifiers=es` en `robolectric.properties`, así que siguen describiendo la pantalla que el usuario ve; la base en inglés la ejercita el audit, que compara las dos tablas. Es una línea y tiene vuelta atrás | `audit_dictionary.py::check_locale_parity` (claves idénticas en los dos locales, y que la etiqueta de tipo siga entrando en una fila **en ambos**) · `PackTypeLabelTest` (2) |
| D-128 | Con el **teclado abierto se escribe sin buscar**: `query` es lo que el campo muestra y `submitted` lo que la lista refleja | Reportado desde el reloj: escribir cerraba el teclado, y borrar una letra también. **La causa no es el campo, es la lista.** Con la query vacía el inicio muestra encabezado, voz, palabra del día e historial; con una sola letra todo eso desaparece y entran los resultados. Esa reestructuración destruye y recompone el campo de texto, y el foco —y el teclado detrás— se van con él; borrar la última letra hace lo mismo al revés. **D-089 no alcanzaba**: puso `key` en cada item, que arregla que un item CAMBIE DE POSICIÓN, no que cambie **qué items existen**. Separar los dos campos deja la lista quieta mientras se escribe. Y buscar ahí era trabajo que nadie ve: **en un reloj el teclado TAPA la lista**. La consulta se dispara al soltar el foco —por Buscar, por el gesto del sistema o por tocar fuera— una sola vez y con el texto final. La voz no abre teclado, así que ese camino sigue disparando solo | `SearchViewModelTest` (5) · `ScreensTest.whileTypingTheHistoryStays` |
| D-129 | **Todos** los caminos que abren una palabra la anotan en el historial, y la etiqueta de guardada sale del estado observable | Reportado como *"el historial a veces no se actualiza"*, y el **"a veces" era la pista**: `recordVisit` se llamaba desde UN camino —tocar un resultado de búsqueda—. Abrir la palabra del día, una entrada del historial, una guardada o una palabra tocada dentro de una glosa navegaba **sin anotar nada**. El de la glosa sólo tiene el `entryId`, así que el ViewModel lee la cabecera —una lectura por rowid, la misma que la pantalla hace un instante después— y **no cae al pack activo** si el pedido no es de un pack abierto: caer sería anotar otra palabra con el lema correcto, que es D-080. Aparte: la etiqueta del menú salía de `viewModel.isFavorite`, que lee un `var` común — **Compose no puede observar eso**, así que no recomponía al guardar. Pasa a salir del state recolectado | `SearchViewModelTest` (3) |
| D-130 | Los sinónimos y antónimos **van dentro del pack**, no en un pack auxiliar, y eso **no compromete la modularidad** | La preocupación es legítima y la respuesta es medible. **No ensucian el formato**: son tags del payload (`Y`, `A`), y *los tags desconocidos se ignoran* por diseño — un pack generado por un usuario **sin** sinónimos es válido, abre igual y la pantalla simplemente no dibuja esa línea. No hay columna nueva, ni tabla nueva, ni `schema_version` distinta; el builder los emite **sólo si la fuente los trae**, así que un pack mínimo sale mínimo sin hacer nada. **No pesan**: +0,89 MB en español y +2,82 en inglés — **1,05 %** — y los antónimos +0,04 y +0,11, un 0,06 %. **Y no cierran la puerta al pack auxiliar**: `entry.uid` ya se escribe en cada pack justamente para eso (D-055), así que mover los sinónimos a un pack aparte el día que haga falta no pide reconstruir nada del formato. ⚠️ **Lo que sí habría que cuidar**, y es la única forma real de ensuciar esto: que la UI algún día **asuma** que existen. Hoy no lo hace. El día que una pantalla se vea rota sin sinónimos, el pack del usuario deja de ser de primera clase. ⚠️ **Lo que bloquea la alternativa**, para no redescubrirlo: `SearchRepository` no existe, y `uid` es **por entrada** mientras que un sinónimo es **por acepción** — esa granularidad está sin decidir | `PayloadCodecTest` (tags desconocidos se ignoran) · `test_payload` |
| D-131 | El presupuesto de filas **se calcula contra la pantalla real**, y sólo manda donde no se puede scrollear | El repo cotizó cinco decisiones contra **192 dp** (D-073, D-075, D-078, D-084, D-085) y el reloj del proyecto entrega **234**. Poner 234 en su lugar sería cambiar un número equivocado por otro. `rowsThatFit(screenWidthDp)` interpola entre **los dos puntos medidos** —192 dp compone 2 filas, 234 compone 3, con `@Config(qualifiers)` en el gate— y lo que adapta es **cuántas filas se muestran, nunca el tamaño de una**: bajar de 48 dp rompe el área tocable que la guía de Wear OS exige y **ningún test lo vería**. ⚠️ **Y lo que se aprendió haciéndolo: casi lo aplico donde no sirve.** El primer intento recortaba el historial del inicio a lo que entra; el test lo mostró —2 y 3 esperados, **0 y 1** obtenidos— porque el historial queda debajo del pliegue y **el inicio scrollea**. Recortar una lista scrolleable esconde sin ganar nada. Quedó donde sí manda: **el tile, que no scrollea**. ⚠️ Ahí se topea en el número medido y **sólo puede bajar**: el chrome de un tile —el título, los márgenes del renderer— no está medido, y `rowsThatFit` está anclado en la pantalla, así que dejarlo crecer sería afirmar un número que nadie comprobó. Bajar en un reloj chico sólo puede evitar que se desborde. Aparte, `MAX_HISTORY` pasó de 3 a 8: **lo que se guarda deja de depender de una pantalla concreta** | `RowsThatFitTest` (5) · `ScreensTest` (2, a 192 y 234 dp) |
| D-132 | Las **palabras relacionadas** entran con el tag `R`, con una regla de atribución distinta según cómo las sirva el dump, y **no van a `fts_def`** | Pedido *«mejorar o completar el pack de español»*. **La medición es la que eligió qué agregar, y descartó casi todo.** De 174.395 registros vivos del dump español, las entradas **flacas** —una acepción, sin ejemplo— son 29.817, y el 25,4 % de ellas traía algo aprovechable; pero **el 20,3 % eran sinónimos, que ya entraban** por D-117/D-124. Lo genuinamente nuevo son `related` (4,8 %), `hypernyms` (1,7 %) y `hyponyms` (0,8 %). En el pack terminado: **5.395 entradas en español (4,7 %) y 90.310 en inglés (11,4 %)**, por **+112 KB y +1,4 MB** — 0,17 % y 0,51 %. Más que los antónimos de D-126, que ya se habían aceptado con 2,9 %. ⚠️ **Las dos formas del dump piden dos reglas, y ahí está todo el riesgo.** Medido sobre ~185.000 registros vivos de cada dump: anidadas en la acepción **es 0,0 % / en 13,8 %**, a nivel de entrada **es 5,0 % / en 9,6 %** — el mismo espejo que los sinónimos. **Anidadas** la atribución es estructural: el item ya vive en su acepción y entran siempre. **A nivel de entrada es inexistente** —no traen `sense_index`, a diferencia de los sinónimos— así que entran **sólo si la entrada tiene una sola acepción**, donde no hay otra acepción a la que pudieran pertenecer. Con varias, colgarlas de la primera sería inventar la atribución: no lanza, no loguea, no lo agarra `verify_pack.py` y **sale del pack como contenido correcto**. ⚠️ **El 0,40 % de los items no son palabras y se vieron a la primera**: estas listas son las únicas del payload que la fuente **no limpia** —vienen como enlaces crudos— y traen namespaces (`Appendix:Months`, `mul:12`), referencias a sección (`abbot § Related terms`) y frases (`more at …`): 1.072 de 267.721 items. Poco en el total y mucho donde se ven, porque se ven en las flacas, donde esa línea es **lo único debajo de la glosa** — salió en una muestra de seis (`abbacy → abbé, more at abbot § Related terms`). Y ninguna se puede abrir: `norm()` no las encuentra, serían un enlace muerto. ⚠️ **No sube `CODEC_ID`** (D-119) y **no entran a `fts_def`**, por el criterio de D-126: nadie escribe *camélido* esperando *guanaco*. En la pantalla el prefijo `rel.` **no es decorativo**: es la tercera lista con la misma forma, y `galo` mostrado como sinónimo de *francés* afirmaría una equivalencia que la fuente no da | `RelacionadasTest` (9) · `AntonimosFueraDelIndiceTest` (la relacionada fuera del índice) · `test_payload` (4) · `PayloadCodecTest` (3, una sobre el fixture) · `ScreensTest.lasRelacionadasSeDistinguenDeLosSinonimosPorElPrefijo` |
| D-133 | El espacio bajo el reloj es una **fracción de la pantalla**, no 20 dp | Segundo eje por el que la app se adapta a la resolución, después de `rowsThatFit` (D-131), y pedido así: *«que el código sea genérico pero se adapte de otras formas a la resolución»*. **No es una constante de diseño elegida a ojo, y eso está verificado**: Wear Compose Material3 declara el padding vertical de contenido como **10 % de la pantalla** —leído del artefacto, `compose-material3-1.6.2.aar` → `PaddingDefaults.verticalContentPaddingPercentage = 10.0f`, junto al horizontal de 5,2 % y `edgePadding = 2.dp`— y **los 20 dp que estaban hardcodeados son exactamente el 10 % de 192 dp**, el ancho que el repo asumía. El número siempre fue una fracción: estaba congelado contra la pantalla equivocada. Por eso el refactor es seguro sin mirar un reloj, y el primer test es justamente el que fija esa afirmación. En el reloj del proyecto (234 dp) pasa a **24 dp**: 4 más, y **en la dirección que se pidió** (*«mover el escribir un poco para abajo»*). Piso de 16 y techo de 32: una pantalla mal reportada no puede devolver el campo debajo del reloj —que es el bug que este espacio existe para tapar— y un 10 % de una pantalla grande no puede comerse el único recurso escaso que tiene un reloj. ⚠️ **El margen lateral NO se tocó y es deliberado**: los 16 dp que se repiten en ~12 llamadas **no son el 5,2 % de nada** (a 192 dp darían 10), así que pasarlos a la fracción de la plataforma los achicaría en los dos relojes que existen — un cambio visible en una dirección que nadie pidió, y sin poder mirarlo. Queda en el roadmap con la medición que necesita | `ClockGapTest` (4, la primera fija que 20 dp **era** el 10 % de 192) |
| D-134 | Los nombres propios pasan de un booleano a **tres políticas**, y el que entra **pierde prioridad en vez de desaparecer** | Pedido así: *«quizás no eliminar palabras de la fuente, sólo bajarles la prioridad si son nombres propios raros, pero quiero ver la comparación de tamaño»*. **La comparación, construida y medida en español:** `lexical-only` (hoy) 114.619 entradas / 68,3 MB · **`definitions-only` 117.648 / 69,1 MB (+1,2 %)** · `included` 146.193 / 73,3 MB (+7,3 %). La intermedia es la barata porque **el problema nunca fue el peso sino la dilución**: de las 31.549 entradas que `lexical-only` descarta, **28.314 sólo dicen su categoría** (*«Apellido.»*, *«Nombre de pila de mujer.»*) y sólo **3.235 traen una definición** — Toledo, La Paz, Medellín, Puruándiro, *Arachnis*, *Méjico*, Babieca. ⚠️ **El marcador es `categories` y NO la glosa, y eso es la decisión de diseño.** Un patrón sobre la prosa (`^Apellido`) sería una heurística en un idioma, justo lo que D-116 evitó al elegir una señal estructural; `categories` lo emite wiktextract desde la categorización del propio wiki — medido: 26.708 acepciones en `ES:Apellidos` y 2.398 en los tres `ES:Antropónimos`. Vive en `Perfil`, que ya es la pieza que se calibra por idioma. ⚠️ **El inglés deliberadamente no declara esa lista**, y es una medición: ahí la señal está sucia — `Places in the United States` aparece en 865 acepciones de registro y en **11.455 que definen**, así que la misma categoría está en los dos lados. En inglés la política útil sigue siendo `lexical-only`. ⚠️ **Y el castigo de rank no es decorativo**: sin él esta política reintroduce por la puerta de atrás el modo de falla que D-116 midió **4.267 veces** en inglés —el topónimo le gana a la palabra común—. El castigo es `RANK_BASE` entero, así que el mejor nombre propio (Guadalupe, 1945) queda debajo de la peor palabra común. Y `verify_pack.py` lo comprueba **en el artefacto**: un castigo mal cableado da un pack que abre sin error y devuelve la ciudad arriba. El default **no cambia** — eso es del usuario, y los dos packs sufijan el `pack_id` para poder instalarse al lado y compararse | `PoliticaDefinitionsOnlyTest` (5) · `PoliticaDeContenidoTest` (2, una que falla si el rank no está castigado) |
| D-135 | Los **ejemplos de una segunda fuente** se construyen, se miden y **quedan detrás de una opción**, no en el pack por defecto | Pedido como *«aplica 1»* del roadmap. **La medición redujo el ítem a una décima parte y esa es la entrada.** El roadmap estimaba **5.307** entradas ganando ejemplo, cruzando por lema. Ese número cuenta casos donde **habría que inventar la atribución**. Aplicando la regla de D-132 —no colgar nada donde no se sabe de qué acepción es— el embudo real es: cruce por lema ~5.300 → nuestra entrada tiene **una** acepción **2.172** → allá también tiene una y el mismo `pos` **510** → trae `english` **326**. Construido de verdad: **307 entradas, +8 KB**. ⚠️ **Los cuatro filtros salen de mirar el dump, y cada uno tiene su caso**: sin *una acepción allá*, «y» (5 acepciones allá, 1 acá) recibe *«jamón y queso»*, que puede ilustrar una acepción que nuestro pack **no tiene**; sin *mismo `pos`* entra «A» con el ejemplo de otra cosa; sin `type` entran los 191 items que son notas (*«Near-synonym: pedazo»*, un enlace a Wikipedia); sin `english` entra **«19. Ac4xd5, Ab7xd5»**, que es notación de ajedrez de una partida citada. ⚠️ **Y por qué NO es el default, que es la decisión de verdad**: el precio no es el código sino la **atribución**. Usar dos fuentes obliga a nombrar a las dos en cada pack, para siempre. Las dos son CC BY-SA 4.0, así que no hay incompatibilidad de licencia — hay una **obligación permanente por un 0,28 %**, y pagarla o no es del usuario. El `pack_id` se sufija (`es-def-wikc-ej`) para que los dos packs convivan instalados y se comparen. El modo de falla que esto protege es silencioso: si el merge corre y el crédito no se mueve, el pack sale entero, abre, funciona **y está mal licenciado** — nada en el contenido lo delata | `test_source_enwikt` (8) · `test_segunda_fuente` (5, una que falla si el crédito no acompaña al contenido) |
| D-136 | `SearchRepository` existe, vive en `:dict-core`, y **no es un `DictionarySource`** | Pedido como *«varios packs para un mismo idioma de distintas fuentes que puedan convivir»*, y es la pieza que el roadmap venía nombrando sin construir. El selector **pasa a elegir un idioma, no un archivo**: todos los packs abiertos cuyo `langSource` coincide con el activo se consultan juntos, y la ganancia es **la unión de sus lemas**. Se queda *dentro* de un idioma a propósito — con español e inglés instalados, escribir *casa* no puede devolver entradas inglesas (D-078 sigue valiendo, sólo que ahora elige idioma). ⚠️ **No implementa `DictionarySource`, y la razón es una clase de bug**: la mitad de esa interfaz se direcciona por `entryId`, que es un **rowid local a un pack**; contestar `entry(7)` sobre un conjunto obliga a elegir uno, y elegirlo en silencio es D-080 — la app muestra **otra palabra**, sin error en ningún lado. La superficie angosta lo vuelve irrepresentable: todo devuelve `Suggestion`, que lleva su `packId`. ⚠️ **El orden cruza los packs en vez de concatenarlos**: `MatchKind` primero (un prefijo del segundo pack le gana a un fuzzy del primero), después `score`, y **después `headword`, `packId` y `entryId`**. Ese desempate no es cosmético: los packs salen de listar un directorio, que **no promete orden**, y si el orden de entrada cambia entre arranques la lista se reordena sola — que es justo lo que D-128 midió que cierra el teclado. ⚠️ **Lo que el orden NO promete**, escrito para que nadie lo asuma: `score` deriva de `rank`, que cada pack calcula contra su propio dump con su propio perfil (D-063); compararlo entre fuentes distintas es una aproximación sin medir. ⚠️ **Y un pack roto no se lleva puesta la búsqueda de los otros** — un archivo truncado abre bien y falla al consultarlo; con dos instalados, que uno se caiga no puede dejar al usuario sin buscador. Fan-out **secuencial**: los packs son uno o dos y `:dict-data` ya serializa cada uno (D-050), así que `:dict-core` sigue sin dependencias en producción. ⚠️ **La composición NO está construida**: esto es la capa que le faltaba, pero unir por `uid` sigue bloqueado por la granularidad (uid es por entrada, un sinónimo es por acepción) | `SearchRepositoryTest` (10) · `SearchViewModelTest` (3) |
| D-137 | Las **frases de corpus (Tatoeba)** son la tercera fuente, y rinden 21 veces más que la segunda | Buscando otra fuente para el pack español. Tatoeba **no es un diccionario**: es un corpus de oraciones sueltas, y lo único que aporta es uso real — que es justo lo que le falta al **70,4 %** del pack. **Rinde 6.499 entradas contra las 307 de D-135, por +368 KB (+0,54 %)**, y la razón del salto es estructural: un ejemplo de corpus **no necesita que dos fuentes coincidan en cómo numeran las acepciones**, sólo necesita contener la palabra. La regla de atribución que mató el 94 % del rendimiento de enwiktionary acá no muerde. ⚠️ **La condición no es «la palabra aparece» sino «la palabra lleva a UNA entrada del pack»**, lema o forma flexionada, y la comprueba el builder contra su propio índice en `finish()` —no en `add()`, porque la ambigüedad es **global**: cuando entra *vino* la entrada de *venir* puede no existir todavía—. Cuesta la mitad del alcance (14.023 → 7.019) y se paga entero: *«Ella vino ayer»* colgada de la bebida no lanza, no loguea, no lo agarra `verify_pack.py` y sale del pack como una definición con un ejemplo que la contradice. ⚠️ **Y hay un segundo filtro que sólo apareció LEYENDO el pack construido**: *nadal* (Navidad, dialectal) recibió *«Alonso, Nadal y Pau Gasol…»*, porque `norm()` baja a minúsculas y **el filtro de ambigüedad no puede verlo** — D-116 podó los nombres propios, así que no queda entrada con la que empatar; la clave parece inequívoca *porque su competidor fue podado*. El primer intento fue una heurística de **posición** (descartar mayúsculas a mitad de frase) y **falló**: *«Nadal, mejor deportista español…»* empieza con el apellido. Lo que separa de verdad es un **hecho del corpus**: una palabra escrita en minúscula alguna vez es común; una escrita siempre en mayúscula, en 442.135 oraciones, es un nombre propio. Cuesta 518 entradas y una segunda pasada sobre 40 MB. ⚠️ **Licencia: CC BY 2.0 FR**, no CC0 — el export `sentences_CC0` fue lo primero que se probó y trae **37 frases en español** de 562.186. Atribución obligatoria, así que también es una opción (`--frases`) y no un default | `test_source_tatoeba` (10) · `test_frases` (7, una con el homógrafo *vino*/*venir*) |
| D-216 | El **ejemplo dice de dónde se citó**, recortado a año y autor, con tag propio y fuera de `fts_def` | Reportado desde el reloj: `Thomas` mostraba *«An infidel or doubter.»* con una frase de una novela del XIX y **nada que dijera de dónde salía** — hubo que ir a Wiktionary a mano para encontrar la referencia, que ahí **sí está**. ⚠️ **El dato estaba en el dump y el builder lo tiraba**: `kaikki._senses` leía `example["text"]` y descartaba `type` y `ref`. Y no es un caso raro: medido sobre el dump inglés entero, **622.680 ejemplos son `type: quotation` contra 97.288 `example`** — el **86,5 %** de lo que el pack muestra como ejemplo es una cita de un texto publicado, así que sin atribución la mayoría de los ejemplos se lee como una definición que no cierra. De lo que el builder guarda (`MAX_EXAMPLES_PER_SENSE = 1`), el **75,5 %** declara `ref`. ⚠️ **Se recorta a los dos primeros campos y eso es la decisión, no un detalle**: el `ref` completo promedia **119 bytes** —editorial, ciudad, →OCLC, página— y recortado a año y autor promedia **31**. Medido construyendo el pack dos veces con el mismo dump y los mismos flags: la cita completa costaba **+6,3 %** estimado y la recortada **+2,52 % medido** (+7,7 MB sobre los 306,8 del pack inglés). ⚠️ **La estimación previa era +1,6 % y la medición la mató por 58 %**: suponía que las citas comprimirían como el resto del payload (1,71×) y comprimen a **1,23×**, porque son nombres propios y dígitos que el diccionario precargado de 32 KB no contiene y que una entrada de ~600 bytes no le da a deflate espacio para aprender. ⚠️ **Tag propio `C` y NO un sufijo `\x1f` sobre `E`**, aunque el mecanismo ya existe y las traducciones lo usan: un sufijo sobre un tag que nació sin él hace que un lector viejo **pinte el byte separador dentro del ejemplo**. Un tag nuevo degrada a nada; un sufijo nuevo degrada a basura. Por eso **no sube `CODEC_ID`** (D-119), igual que `A` y `R`. ⚠️ **La cita nombra a la línea de arriba y a ninguna otra**: una `C` que no venga pegada a su `E` se **descarta** en los tres lados, en vez de colgarse del último ejemplo visto — elegirle un ejemplo es la atribución inventada de D-179. ⚠️ **No entra a `fts_def`**, como el antónimo (D-126) y la relacionada (D-132): buscar *Richard Marsh* no tiene que devolver `Thomas`, y D-117 ya midió que indexar en `fts_def` **triplica** el costo estimado. Verificado: con y sin citas, `fts_def_data` pesa **exactamente lo mismo**. ⚠️ **Dos defectos del recorte los encontró LEER el pack construido, no los tests**: `captive` salía como `1850, [Alfred` —el Wiktionary encierra el nombre editorial del autor entre corchetes y la coma es interna, **369 citas (1,3 %)**, todas desbalanceadas— y otras **184 (0,64 %)** traen el `ref` entero envuelto en corchetes, donde el par no cierra nunca y salía la cita completa. ⚠️ **La versión ingenua del segundo arreglo —sacar el delimitador inicial siempre— rompía dos casos** (`[1877], Anna Sewell` → `1877], Anna Sewell`), así que se desenvuelve sólo si el corte quedó desbalanceado: con la guarda, las citas con un par sin cerrar pasan del 0,64 % a **1 de 14.062** en el pack construido, y esa una es larga, no incorrecta. ⚠️ **El español NO trae cita todavía**, y es alcance, no olvido: su dump no emite `type` (0 de 874 muestreados) y sirve el `ref` con **puntos y el autor primero** (`Miguel Nicolau. Iniciación a la Teología. Página 85. 1984.`), así que el separador inglés produciría basura. El separador se declara por idioma en `Perfil` —donde D-076 ya puso la calibración por idioma— y encenderlo es una línea | `test_source_kaikki.CitaDelEjemploTest` (12, con los `ref` reales de Tennyson, Tyndale y la BBC) · `test_payload.CitationTest` (15, una fija que `CODEC_ID` no sube) · `test_build.CitaHuerfanaTest` (3, sobre los bytes de un pack manipulado) · `test_build.AntonimosFueraDelIndiceTest` (la cita fuera de `fts_def`) · `PayloadCodecTest` (9, con el caso del fixture compartido) · `verify_pack._citas_huerfanas` |
| D-217 | La app verifica **todas** las invariantes baratas al abrir un pack, recuerda tambien los **rechazos**, y un pack rechazado **no se carga de ninguna forma** | Pedido: *«si es que ahora solo verifico una vez cada pack dentro de la app, me interesa que los checkeos sean más completos [...] que si un pack es rechazado este resultado también quede guardado para no reescanear el pack [...] que estos packs no se carguen de ninguna forma, por ejemplo que no se muestren en los créditos»*. ⚠️ **Lo que lo vuelve posible es el memo, no un presupuesto nuevo**: desde que la muestra de claves corre **una vez por archivo** y no por arranque, sumar comprobaciones cuesta una vez y no todos los días. Medido sobre el pack inglés de 306,8 MB: las siete que se suman son **5,7 ms** contra los **14,7 ms** que la muestra ya costaba — `entry_count` contra las filas reales 4,2 ms (agarra un archivo truncado), `fts_def` una fila por entrada 1,4 ms (D-011: desalineadas, buscar por definición devuelve **otras** entradas), y claves de meta, licencia, los dos índices, la tabla de staging y `norm` vacío a 0,0 ms. ⚠️ **Lo caro se queda en `verify_pack.py`**, donde corre al construir y no en el reloj: `uid` único **377 ms** y los huérfanos completos **672 ms**; en la app los huérfanos se miran sobre 64 filas, que acota el daño sin eliminarlo — el mismo trato que D-142 le dio a las claves. ⚠️ **Todas rechazan, también las que sólo degradan, y eso se decidió contra la recomendación.** Se propuso separar las que MIENTEN de las que sólo hacen la búsqueda lenta —falta un índice— y rechazar sólo las primeras; se eligió la regla simple porque *una segunda categoría se llena de excepciones y deja de ser honesta*. **Costo aceptado**: un diccionario legible al que le falta un índice desaparece, y lo único que el usuario lee es una línea. ⚠️ **Y `meta.sources` pasa a ser obligatoria, revirtiendo la tolerancia que D-138 le puso**: un pack que no declara de dónde sale su contenido vuelve imposible de cumplir D-031, que dice que la atribución no es opcional. Eso además **resuelve solo** el pedido de que no aparezca en los créditos: no hay que acordarse de filtrarlo en ninguna pantalla porque `PackHandle.Incompatible` **no tiene `PackMetadata`** — sin nombre, sin licencia y sin fuentes, ninguna pantalla puede mostrarlo aunque quiera, y lo impone el compilador y no la disciplina. ⚠️ **Cachear un NO es más peligroso que cachear un SÍ**, y esa es la parte delicada: un sí de más cuesta que un pack malo se use; un **no** de más cuesta que un pack bueno **desaparezca para siempre**, porque el archivo no cambia y nada lo vuelve a mirar. Lo único que lo impide es que la huella lleve las **reglas enteras** — `NORM_VERSION`, `schema_version`, el códec y un `CHECKS_VERSION` que sube cuando cambia **qué** se comprueba—, así que soportar un esquema nuevo caduca solo todo rechazo por esquema. Un fallo que **no** es del contenido (disco, memoria) no se anota. ⚠️ **El ORDEN de las comprobaciones lo corrigió un pack real**: los siete `.db` de `schema_version` 3 que quedaron en el directorio de datos se rechazaban como *«metadatos incompletos»* —no traen `langs` ni `fuzzy_profiles`, que nacieron con el esquema 4— en vez de *«otra versión del formato»*. Los dos rechazan; el segundo es el que sirve. `schema_version` es la clave que dice **qué otras claves tienen que existir**, así que se mira primero | `PackIntegrityTest` (11, el orden incluido) · `PackVerificationTest` (16, la caducidad de un rechazo) · `ScreensTest` (la fila incompatible, y que NO aparece en los créditos) · `SearchViewModelTest` (los dos canales separados) · **`audit_dictionary.check_rejection_mirror`**, que compara los motivos de `verify_pack.py --como-la-app` contra el enum `PackRejection` **con su orden** · `test_build.ComoLaAppTest` (10, uno por motivo) |
| D-218 | Las listas del payload **no repiten un item**, y se deduplican en `render` | Lo encontró **barrer los packs construidos**, no un test. Medido sobre los reales: el **3,1 %** de las entradas con traducciones de palabra del pack bilingüe repetían un término —`where` traía `donde, donde` y `do, do`; `Brazil` traía `carioca` dos veces— y en el pack español eran **88 de 407** de las que traen traducciones por acepción. En una fila de reloj es la misma palabra dos veces, ocupando un ancho que ya se corta. ⚠️ **En `render` y no en cada fuente**, por el mismo argumento que `merge_duplicate_senses`: es el **único** paso por el que pasan todos los packs, y puesto en `kaikki` habría que repetirlo en `oewn`, `wikidata` y `bilingual` — la propiedad sería cierta sólo en los packs cuyo autor se acordó. ⚠️ **Conserva el orden de la primera aparición y no ordena**: la fuente pone primero lo que más se usa y los topes de 4 y de 8 hacen que el orden decida **qué se ve**. Se compara el item entero y exacto, así que `donde` y `dónde` son dos palabras y las dos se quedan. ⚠️ **El espejo Kotlin NO deduplica, a propósito**: ahí se está **leyendo** un pack que ya existe, y alterar en silencio lo que el archivo trae escondería que un pack ajeno viene con repetidos | `test_payload` (3, una que fija el orden) · verificado por mutación |
| D-219 | El corte de un **nivel** se decide por **frecuencia de uso**, y el nivel **declara la cobertura que logra** | Pedido: *«me interesa que la división de packs por core, main y full se basen en métricas verificables y se documente el motivo de esto; quiero que esta lógica, a pesar de no ser tan compleja, dé el mejor resultado posible»*. ⚠️ **Lo que faltaba no era complejidad: era una métrica.** El corte era *«ordenar por `rank` y llenar hasta el presupuesto estimado»*, y nadie medía qué lograba — un nivel se juzgaba por cuántas palabras traía, que es un número sin unidades. La métrica correcta es **cobertura de tokens del corpus**: qué fracción de lo que alguien va a buscar está adentro, medida sobre **tokens y no sobre tipos** — que falte una palabra que aparece un millón de veces no es lo mismo que falte una que aparece tres. ⚠️ **Y medirla mostró que ordenar por `rank` era peor**, aunque `rank` ya salga de la frecuencia, por dos motivos: está **bucketizado** —`int(round(zipf × 70))`, 500 cubetas, y el desempate es alfabético, así que una palabra gorda con `a` desplaza a una más usada y más flaca— y **pasado el 500 deja de ser frecuencia**: es riqueza de página, que correlaciona **−0,250** con el uso real (D-185). Medido sobre los packs reales: inglés 25 MB **93,46 → 94,43 %**, 40 MB **94,75 → 96,03 %**, 130 MB **95,10 → 96,63 %** *(la del pack completo)*; español 25 MB **77,59 → 78,87 %** *(también la del completo)*. Y entran **más** lemas, no menos. ⚠️ **La lectura que importa: un núcleo español de 17,7 MB cubre lo mismo que el pack completo de 74**, y un `main` inglés de 130 MB alcanza la cobertura del de 307. ⚠️ **La métrica SATURA**, y decirlo evita que alguien la use para justificar un nivel más grande: más allá de las ~50.000 palabras que la lista atestigua, sumar lemas no la mueve. Lo que un nivel mayor compra a partir de ahí es encontrar lo raro, que es otra métrica y no existe todavía. ⚠️ **El número se escribe en el artefacto** (`meta.corpus_coverage`) y no en un changelog: un nivel que no declara su cobertura obliga a recalcularla para saber si el corte fue bueno, y nadie lo hace. `verify_pack.py --frecuencias` la **recalcula y falla si no coincide** — sin la lista dice que no la comprobó, en vez de callarlo. ⚠️ **La lista es opcional y sin ella se corta por `rank`, avisando por stderr**: `build_core` deriva de un pack ya construido y puede correrse sobre uno cualquiera sin tener el corpus a mano; degradar al criterio viejo es mejor que fallar | `test_core` (3, una por empate de `rank`, una sin lista y una con un lema sin señal) · `test_source_frequency.CoberturaTest` y `PorNormTest` (5) · `verify_pack` recalcula lo declarado · `test_build_packs` fija que el pipeline pase la lista |
| D-220 | El presupuesto de un nivel es un **rango que el ARCHIVO cumple**, y sus extremos salen de la curva medida | Pedido: *«da lo mismo que el core se quede corto, lo importante es que la calibración esté dentro del rango del presupuesto y la elección de este presupuesto esté validada»*. Eran dos cosas y ninguna estaba. **(1) El artefacto no cumplía el presupuesto.** `vocabulario_por_presupuesto` estima el tamaño escalando los payloads por la proporción del pack de **origen**, y el derivado tiene otra —se lleva las formas de sus lemas y no las de los demás, y los índices crecen distinto—: **pedir 25 MB dio 17,7**, por debajo del mínimo del nivel, sin error y con el pack pasando todas sus invariantes. ⚠️ **Y ninguna constante lo compensa, porque la razón archivo/presupuesto NO es monótona**: medida sobre el pack español a seis presupuestos da **0,60 · 0,67 · 0,91 · 1,26 · 1,14 · 1,01** —sube hasta un pico y vuelve a bajar, porque `form` crece más rápido que los payloads (12,98 flexiones por lema en español) hasta que el vocabulario se agota—. La única garantía es **derivar, medir el archivo, corregir el factor y volver**, con tope de 4 vueltas y apuntando al **medio** del rango, que es lo que deja margen en las dos direcciones; y **exit 1 si no converge**, porque un exit 0 fuera de rango le dice al pipeline *esto cumple*. **(2) Los extremos no estaban validados.** Criterio escrito **antes** de leer los números: el rango tiene que contener el **codo** de la curva cobertura/tamaño, definido como *+10 MB compran menos de 0,5 puntos*. Medido sobre los dos `full` reales, derivando a 8 y 11 presupuestos: el español llega al codo en **~13 MB** y satura en 17,7 (78,87 %, el valor del pack completo); el **inglés**, que es el exigente, en **~29 MB**, y satura en 45,2 (96,63 %, también el del completo). ⚠️ **Eso corrigió el mínimo: 25 MB estaba 1,1 puntos por debajo del codo inglés, y pasa a 30.** ⚠️ **Y el máximo NO lo valida ninguna métrica de contenido, que es el hallazgo que hay que decir en vez de disimular**: pasado el codo la cobertura no se mueve, y `lemma_coverage` —la segunda métrica, la fracción del diccionario que el nivel se lleva— es **convexa**: acelera, **0,168 → 0,248 → 0,301 → 0,388 puntos por MB**, porque las palabras raras tienen payloads chicos. Por esa vara **siempre** conviene gastar más, así que no hay codo superior y el máximo es una decisión de producto sobre cuánto se le pide al reloj. Lo que sí está medido es el **ancho**: `máximo ≥ mínimo × 1,5`, porque la dispersión de la razón (0,60–1,26) hace que un rango más angosto no converja; y el **salto** `mínimo(main) ≥ máximo(core) × 2`, para que los dos rangos no puedan solaparse. De ahí `core` **[30, 50]** y `main` **[100, 150]**. ⚠️ **Un idioma cuyo `full` cabe entero en el rango de `main` no genera `main`** —el español son 73,6 MB— porque sería una copia del completo | `test_core` (5: dentro del rango, el `full` que no llega al mínimo, las dos métricas declaradas, la CLI que converge y la CLI que sale 1) · `test_build_packs` fija que el pipeline pase `--rango-mb` con los números de `RANGO` y no `--budget-mb` |
| D-138 | Cada pack lleva un **manifiesto**: qué es, de dónde viene y **una licencia por fuente**; y el `pack_id` es un **código**, no un nombre | Pedido: *«que las atribuciones y licencias vengan en los packs, y que todas se carguen desde los packs y se muestren en la app»*. ⚠️ **El disparador es que `meta.license`, en singular, pasó a ser mentira**: el pack español mezcla definiciones CC BY-SA 4.0 con frases de corpus CC BY 2.0 FR (D-137). Un solo nombre para todo el pack **o reclama de más o acredita de menos**, y la atribución es la *condición de uso* del dato (D-031), no una cortesía. `meta.sources` declara una línea por fuente —`rol`, `nombre`, `url`, `licencia`, `url de licencia`— y la pantalla las muestra todas. **Verificado en el emulador**: *definitions · Wikcionario (es.wiktionary.org) · CC BY-SA 4.0* / *sentences · Tatoeba · CC BY 2.0 FR*. **Texto delimitado y no JSON**, la misma elección que el payload y por la misma razón: parsea sin dependencia en los dos lenguajes y `:dict-core` sigue sin una librería de JSON. ⚠️ **El crédito se deriva de datos, no se escribe**: `build_pack.FUENTES` es la tabla de la que salen *las dos* cosas —la prosa de `attribution` y las filas de `sources`—, así que **no existe una forma de sumar contenido sin sumar el crédito**. El modo de falla que cierra es silencioso: el pack sale entero, abre, funciona **y está mal licenciado**. ⚠️ **Y el `pack_id` pasa a ser un código con gramática** `<idioma>-<tipo>-<fuente>[-variante]`, por la colisión que D-136 hizo posible: dos packs del mismo idioma y distinta fuente se instalan y se consultan juntos, comparten idioma y tipo, y **lo único que los separa es el código de fuente**. Con `pack_id = "espanol"` uno pisa al otro al instalarse y el historial del reloj queda apuntando a un pack que ya no está — sin que nada lance. `verify_pack.py` rechaza un id que no cumpla la forma, un pack sin `sources`, y **una fuente declarada sin licencia**, que es peor que no declarar nada porque parece completo | `PackSourcesTest` (6) · `ManifiestoTest` (5) · `test_segunda_fuente` (2) · `ScreensTest` (2) |
| D-139 | **Wikidata Lexemes** es el segundo pack base de español, y sigue la convención de `uid` aunque tenga una identidad mejor | No mejora el pack del Wikcionario: es **otro diccionario**, que se instala al lado y se consulta junto con él (D-136). **15.269 entradas, 4,4 MB**, de las cuales **5.092 (33,3 %) no están en el otro pack** — y lo que aporta es complementario, no redundante: gentilicios regionales (*iquiteño*, *huantino*, *ucayalino*, *abiyanés*) y locuciones (*a su vez*, *entre tanto*, *así como así*), justo lo que un wiki editado mayormente desde España cubre peor. **Es el único CC0 del catálogo**: no suma obligación de atribución a nadie, y se declara igual en el manifiesto porque saber de dónde viene un dato es útil aunque no sea obligatorio decirlo. ⚠️ **Las categorías léxicas se identificaron mirando los lemas de cada Q-id en el dump**, no de memoria: `Q1084` sale con berilio/vino/viernes, `Q147276` con Miguel/Portugal/Turquía. Los `Q147276` se mapean a `name` para que la poda de D-116/D-134 los agarre sin que este módulo sepa de ella. ⚠️ **Y la lección cara: `sense_key` es el id del lexema SÓLO cuando hay homógrafo.** `L12345` es una identidad estable declarada por la fuente —más de lo que kaikki tiene— y la tentación es usarlo siempre. **No se puede**: `uid` existe para unir la misma entrada entre packs (D-055), y si este pack mete `L12345` en el hash y el del Wikcionario no mete nada, la entrada de *vino* de los dos deja de unir. Lo agarró `verify_pack.py`, y el arreglo cuesta una pasada extra sobre el dump porque no viene agrupado por lema. **Medido después del arreglo: 8.595 `uid` coinciden entre los dos packs** — esa es la llave de la composición, funcionando | `test_source_wikidata` (10) · `verify_pack.py` (identidad lógica) |
| D-140 | Un **texto traducido escrito a mano** en el código es un fallo del gate | Encontrado **mirando la pantalla**, no un test: en un emulador en inglés el inicio mostraba *«Guardadas»* y *«Ajustes»* en español, al lado de *About this data* en inglés. ⚠️ **`check_locale_parity` no puede verlo, y por eso hace falta otro chequeo**: las dos tablas estaban perfectamente parejas —`home_saved` y `home_settings` existían en los dos idiomas— y la pantalla dibujaba el literal igual. El recurso existía y nadie lo pedía. `check_no_hardcoded_translations` busca cada valor de `values-es/` entre comillas en los `.kt` de `:app`, y al encenderlo **aparecieron cinco más que nadie sabía**: *Opciones* (×2), *Buscar*, *Gestionar*, *Recientes* — todas con su clave ya escrita. Se compara contra el español y no contra el inglés a propósito: un valor inglés corto (*Search*, *Saved*) aparece legítimamente en un nombre de test. Y **se ignoran los comentarios**, porque un KDoc que cita el texto —*«no puede parecerse a `sin.`»*— está explicando la regla, no rompiéndola; un chequeo con falsos positivos se termina apagando | `audit_dictionary.py::check_no_hardcoded_translations` (probado devolviendo el bug) |
| D-141 | **Ninguna fuente pierde palabras por defecto**: la poda se pide por nombre | Pedido explícito: *«no filtres palabras en ninguna fuente, quiero que vayan completas antes que tener que decidir qué eliminar y qué no y hacerlo erróneamente»*. El argumento es bueno y no es de gusto: **podar es tomar una decisión de contenido sobre datos ajenos, y equivocarse en esa decisión no deja rastro** — la palabra simplemente no está, y nadie puede reportar lo que no ve. El default de `sources/kaikki` y `sources/wikidata` pasa a `included`; las dos políticas podadoras (D-116, D-134) siguen existiendo y se piden por nombre, y son las que produjeron sus números. **Costo medido: 114.619 → 146.193 entradas, 68,3 → 73,5 MB (+7,6 %).** ⚠️ **Lo que vuelve seguro este default no es esperar que no molesten: es `CASTIGO_NOMBRE_PROPIO`.** D-116 midió el problema real —**4.267 casos en inglés donde el topónimo le gana en rank a la palabra común**— y ese problema es de **orden**, no de presencia. Con el castigo entran sin desplazar nada, y `verify_pack.py` lo comprueba en el artefacto. **Y hay un efecto secundario que mejora otra cosa**: con los nombres propios adentro, *Nadal* existe en el pack, así que el filtro de ambigüedad de D-137 lo agarra solo, sin depender de la heurística de mayúsculas. ⚠️ Lo que se sigue descartando **no son palabras**: markup del wiki (`Appendix:Months`, `abbot § Related terms`), acepciones *form-of* —que se emiten como formas, no se pierden— y lexemas de Wikidata **sin ninguna glosa**, que no tienen definición que dar | `test_source_kaikki` (default + política por nombre) · `test_source_wikidata` |
| D-142 | El orden **no confía en la calibración de ningún pack**, y las claves se **recalculan sobre una muestra al abrir** | La preocupación: *«si hay un pack de la comunidad que no hace una buena normalización podría matar la lógica de ordenado»*. Son **dos fallas distintas** y cada una tiene su respuesta. ⚠️ **(1) Normalización mala = palabras que desaparecen.** `norm_version` es un número que el pack **se pone a sí mismo**: puede declararlo bien y tener `entry.norm` construida con otras reglas —otro ICU, minúsculas dependientes del locale, NFC donde va NFD— y entonces falta una palabra, sin excepción y sin log. Hasta ahora eso sólo lo cubría `verify_pack.py`, que corre **al construir**; un pack que no pasó por nuestro builder nunca lo vio. Ahora `PackFile.open` **recalcula `norm()` y `fuzzy()` sobre 64 entradas repartidas** y rechaza el pack si no coinciden: 64 lecturas por rowid, una vez por pack, y convierte una declaración en una prueba. Los rowids van repartidos a propósito —un pack correcto sólo al principio se agarra igual—. ⚠️ **(2) Calibración mala = orden envenenado, y era menos grave de lo que parecía**: `Suggestion.score` **no es el `rank`, es la posición dentro de la lista de su propio pack**, así que la mezcla ya era **ordinal** e inmune a que un pack calibre en otra escala. Lo que no cubría es un pack cuyo **orden interno** sea basura: entrega su basura en la posición 0. **La señal resiliente es la banda de cobertura** — cuánto del lema escribió el usuario, calculado del texto escrito y del lema, **sin mirar un solo dato del pack**. Medido sobre los dos packs reales: escribir *cas* devolvía `castigar, castreño, cascar` y **no devolvía `casa`**; ahora devuelve `casa, casar, cascar`. *per* pasa de `percibir, perder` a `perro, persa`; *arb* de `árbitro` a `árbol`. **Arregla un bug de orden que existía con un solo pack** (D-067). Bandas gruesas y no el ratio crudo, para que dentro de una banda siga mandando el pack, que con uno sano es mejor que cualquier heurística; y **no se aplica en FUZZY**, donde `score` es distancia de edición, que la calculamos nosotros. ⚠️ **El «acuerdo entre packs» se midió al lado y se RECHAZÓ**: mejoraba *cas* y empeoraba *tomat*, y hace que el orden dependa de qué otros diccionarios estén instalados. ⚠️ **(3) Y para saber si dos calibraciones son comparables, se mide**: `compare_calibration.py` calcula **Spearman sobre las entradas que comparten `uid`** — correlación de órdenes, así que no le importa la escala. Entre `es-def-wikc` y `es-def-wd`: **ρ = +0,388, control barajado +0,001, 110 de las 200 más comunes compartidas**. Dos fuentes honestas que comparten señal sin ser intercambiables. Un ρ bajo **no condena al pack**: dice cuánto se está apoyando la mezcla en una calibración ajena — y desde esta fila, poco | `SearchRepositoryTest` (4 de orden resiliente) · `PackKeySampleTest` (3, instrumentados, **corridos en emulador: 0 fallas**) · `test_compare_calibration` (6) |
| D-143 | Volver a buscar **borra la palabra**, atrás con texto vuelve al inicio, y cada resultado dice **de qué idioma viene** | Tres pedidos de interfaz, y los tres cierran huecos que la convivencia de packs (D-136) abrió o agrandó. **(1) La lupa de una entrada vuelve a una barra vacía**: antes volvía con la palabra todavía escrita y había que borrarla a mano para buscar otra cosa. **(2) Atrás con texto vuelve al inicio en vez de SALIR de la app**: el inicio es la primera pantalla, así que el gesto caía en la Activity y la cerraba — en un reloj eso es una salida brusca para lo que el usuario quiso decir con *«deshacé lo que escribí»*. ⚠️ **Sin texto no se habilita, y es deliberado**: atrapar el gesto siempre dejaría la app sin forma de cerrarse con el gesto que todo el sistema usa. Verificado en el emulador: el primer atrás limpia, el segundo sale. **(3) Cada resultado lleva el idioma abreviado** junto al tipo: `perron · noun · EN`. Con varios diccionarios conviviendo, una fila sin origen obliga a abrir la entrada para saber cuál contestó — y se vio de inmediato al probarlo: la etiqueta delató que el pack activo era el inglés, que era justo la ambigüedad a resolver. ⚠️ **Un `packId` que no esté entre los packs abiertos NO recibe etiqueta**, en vez de heredar la del activo: heredarla sería afirmar un idioma que nadie comprobó, la misma familia de falla que D-080. Va en la **misma ranura** que el tipo y no en una tercera columna: en una fila de 234 dp el lema ya compite por el ancho. ⚠️ **Lo que NO está cubierto por el gate**: la condición del `BackHandler`. `clearQuery` sí lo está, pero `createComposeRule()` no trae Activity y sin Activity no hay despachador de atrás; el comportamiento se verificó a mano en el emulador | `SearchViewModelTest` (2) · `ScreensTest` (2, una que falla si se inventa el idioma de un pack desconocido) |
| D-144 | Los sinónimos y antónimos salen también de **WordNet**, que agrupa por significado y no por quién se acordó de escribirlos | Pedido: *«buscá una fuente para sinónimos y antónimos más completa»*. La del wiki es desigual por construcción —se escriben a mano— y daba **18,4 % de las entradas en español y 15,4 % en inglés**. WordNet está hecho al revés: un *synset* **es** un conjunto de sinónimos. **Medido, construido y verificado: español +3.801 entradas con sinónimos; inglés +30.423 con sinónimos y +2.376 con antónimos.** Dos formatos porque cada idioma tiene el suyo: **Open English WordNet 2024** en WN-LMF (**CC BY 4.0**, 120.630 synsets) y el **Multilingual Central Repository** vía OMW en `.tab` (**CC BY 3.0**, declarado en el `index.toml` del repo oficial *y* en la cabecera del propio archivo). ⚠️ **La regla de atribución de D-117 muerde fuerte acá**: un synset es *una* acepción, así que sólo entra cuando hay **una acepción de nuestro lado y un synset del suyo** — `domingo`, que está en dos synsets del MCR, queda excluido y se verificó que lo esté. ⚠️ **Los antónimos sólo vienen del inglés, y es deliberado**: en WordNet la antonimia es una relación **léxica**, entre acepciones y no entre synsets, así que **no se puede transferir** por el synset compartido — el `.tab` sólo da lemas por synset y eso no alcanza para saber qué acepción española es el opuesto de cuál. Inventarlo sería peor que no tenerlo (D-126). Por lo mismo, el antónimo inglés **no** exige synset único: su atribución ya viene dada. ⚠️ **Y el MCR trae ruido que el inglés no**, porque se construyó automáticamente. Dos filtros, los dos encontrados **leyendo el pack construido**: (a) **una flexión del propio lema no es un sinónimo** —`coreano` venía con `coreana, coreanos, coreanas`— y se detecta contra la tabla `form` del propio pack, que es un dato que ya tenemos (por eso el merge corre en `finish()` y no en `add()`); (b) **una variante morfológica tampoco** —`decolorarse → decolorar`, `organismos → organismo`, `básicamente → basicamente`—, detectada con una regla que no sabe español: *uno es prefijo del otro y sólo cambia una terminación corta*, con piso de raíz para no matar abreviaturas (`Oct`/`October`) y techo de diferencia para no matar parientes reales (`ente`/`entidad`). Saca **6.318 de 99.292 candidatos (6,4 %)**. Lo que sigue pasando y no tiene filtro estructural: algún synset mal mapeado del MCR y alguna palabra inglesa colada (`meadero → jakes`) | `test_source_wordnet` (14) · `test_tesauro` (8, una que falla si se acepta una flexión como sinónimo) |
| D-145 | Wikidata **se funde** en el pack español en vez de ser un pack aparte | Reportado desde el reloj: con dos diccionarios de español el inicio mostraba **dos chips «ES» y dos palabras del día**. ⚠️ **La mitad es un bug mío**: D-136 dejó escrito que *el selector pasa a elegir un idioma, no un archivo* y **no lo llevé a la pantalla**, que sigue listando packs. La otra mitad es que esos dos packs no deberían existir. **Wikidata aporta 6.092 lemas que el Wikcionario no tiene** —gentilicios regionales, locuciones— y eso es una **unión de FILAS, no de campos**: se funde en el build sin composición y sin mecanismo nuevo. Los 8.595 lemas que comparten se quedan con la definición de la fuente base, así que **no hay nada que arbitrar** — y eso es lo que la vuelve barata. ⚠️ **La objeción de D-139 sólo aplicaba al solapamiento**, y la de licencias murió cuando `meta.sources` pasó a llevar una por fuente (D-138). El pack quedó en **152.281 entradas y 75,2 MB**, y el inicio volvió a un «ES» y una palabra del día por idioma, verificado en el emulador. ⚠️ **Y apareció un fallo que sólo existe al fusionar, agarrado por `verify_pack.py`**: cada fuente decide si una entrada necesita `sense_key` mirando **sus** homógrafos, así que un lexema que tenía gemelo en Wikidata puede perderlo al descartarse el duplicado y quedarse con una clave que ya no corresponde. Eso rompe `uid`, que es **la llave del join entre packs** (D-055): el mismo lema con clave en un pack y sin clave en otro deja de unir. La regla que vale es la del **pack final**, y por eso los registros sumados se bufferean en vez de emitirse en streaming | `SumarVocabularioTest` (7, una que recalcula el uid de cada entrada sin homógrafo) |
| D-146 | El APK de **release** lleva sólo las ABIs que existen en un reloj | Medido sobre el release real: las cuatro ABIs de SQLite nativo suman 4,9 MB y **`x86` y `x86_64` son sólo de emulador**. Sacarlas baja el APK de **35 a 33 MB**. ⚠️ **El costo hay que saberlo antes de perder una tarde: el APK de release ya no se instala en un emulador x86.** El de debug sigue trayendo las cuatro, que es donde se verifica a diario. ⚠️ **Y el número deja claro dónde está el peso de verdad**: de los 33 MB, **32,9 son dex**, porque R8 sigue apagado (O-2). Esto es una mejora real y chica; la grande sigue pendiente | el propio `assembleRelease`, comprobado listando las ABIs del APK |
| D-147 | El selector muestra **un chip por idioma**, no uno por archivo | Reportado desde el reloj: con dos diccionarios de español había **dos chips «ES»**, los dos activables, y tocarlos no cambiaba en qué se buscaba — desde D-136 se busca en **todos** los packs del idioma activo. ⚠️ **Era una incoherencia que D-136 introdujo y no terminó**: dejó escrito que *el selector pasa a elegir un idioma, no un archivo* y la pantalla siguió listando packs. `languageChips` agrupa por `langSource`. **El representante de un idioma** es el pack activo si ya lo es —tocar otro idioma y volver no puede cambiarte el diccionario elegido por debajo— y si no, **el que más entradas tiene**, que es el que más veces va a tener la palabra. ⚠️ **El orden es por código de idioma y no el de `available`**, que sale de listar un directorio y no promete orden: si los chips lo siguieran cambiarían de lugar entre arranques, y un control que se mueve solo se toca por error — mismo criterio que el desempate de D-136. La lógica vive fuera del composable porque **es una decisión, no un dibujo**, y así la cubre el gate en la JVM en vez de Robolectric | `LanguageChipsTest` (7) |
| D-148 | El inicio muestra **tres recientes**; el resto vive detrás de «ver más» | Pedido explícito. El inicio es la pantalla más disputada del reloj: con los **8** que D-131 permitía, los ajustes y la atribución quedaban a varios scrolls. Tres es lo que entra después del campo y la voz sin empujar nada fuera de alcance. ⚠️ **El botón aparece sólo si hay más**: uno que lleva a la misma lista que ya estás viendo es cromo, y en un reloj el cromo se paga en filas. Y como ahora hay dónde mostrarlo, **`MAX_HISTORY` sube de 8 a 25**: el tope guardado es el techo de lo que cualquier pantalla podría mostrar, no lo que muestra una. ⚠️ **La pantalla es la MISMA que la de guardadas**, parametrizada con título y texto de vacío: `FavoritesScreen` pasó a `WordListScreen`. Dos composables idénticos con distinto nombre divergen en cuanto alguien arregle uno solo. Se renombró el archivo también — eran dos referencias | `ScreensTest` (4) · `SearchViewModelTest` (el tope, ahora calculado de la constante y no escrito a mano: estaba fijado en 12 y se rompió al subir el tope) |
| D-149 | Cada tile trae **su propia preview**, y **sin texto** | Reportado: *«a los tiles les falta la preview de sistema al momento de añadirlos»*. El diagnóstico fue peor que el reporte: la preview **existía y estaba declarada**, pero era **el placeholder del template** —un círculo negro que dice *«Hello, Tile!»*— **y los dos tiles apuntaban al mismo archivo**. En el carrusel de «agregar tile» aparecían dos veces lo mismo y ninguno mostraba qué hacía. Ahora cada uno dibuja su estructura: el de palabra del día una cabecera y una tarjeta grande centrada; el de historial una cabecera y tres filas. ⚠️ **Sin texto, y es deliberado**: una preview con el texto horneado queda en **un** idioma, y la app es bilingüe (D-127) — en un reloj en español se vería la cabecera en inglés. Lo que distingue un tile del otro de un vistazo es la estructura, no las palabras. ⚠️ **Vectorial y no PNG**: escala del reloj de 192 dp al de 234 sin remuestreo y el diff es legible, a diferencia del binario que reemplaza | el propio `assembleDebug`; **la preview dibujándose en el carrusel no se pudo verificar**: agregar un tile es un gesto del usuario y no se puede hacer por `adb` |
| D-150 | Hay un **emulador que reporta lo mismo que el reloj**, y se crea con un script | El AVD que trae Android Studio para Wear es `wearos_small_round`: **384×384 a 320 dpi → `sw192dp`**, y además `hw.lcd.circular=false`. El reloj es **498×498 a 340 dpi → `sw234dp`**, y es redondo. ⚠️ **O sea que el emulador por defecto miente en las dos cosas que este repo más pelea**: el ancho en dp, contra el que se cotizaron cinco decisiones antes de medir el reloj (D-131, D-133), y la forma redonda, de la que dependen el edge transform y el recorte de los bordes. `tools/avd_como_el_reloj.py` crea uno a partir de `wearos_large_round` y parcha `config.ini`. **Verificado comparando `am get-config` contra el reloj**: los dos dicen `sw234dp-w234dp-h234dp ... round ... 340dpi`. Difieren en `highdr`/`lowdr`, que no participa de ninguna medida. La captura del inicio en el emulador nuevo es indistinguible de la del reloj, incluida la curva del borde. ⚠️ **Lo que un emulador sigue sin poder decir** (D-043): rendimiento y batería. Iguala la geometría, no el hardware | `am get-config` en los dos, y las dos capturas del inicio |
| D-151 | **Una palabra del día por idioma**, y la fila dice la **fuente** cuando el idioma no alcanza | Dos de las cinco piezas que el plan de multipacks dejaba pendientes. ⚠️ **La palabra del día se calculaba por PACK**, así que con dos diccionarios de español el inicio mostraba **dos palabras del día del mismo idioma** — el bug que D-145 tapó **fundiendo** los packs en lugar de arreglarlo, y que volvía en cuanto alguien instalara un pack propio. Ahora hay un `representativePacks` que elige **uno por idioma**, con la misma regla que ya usaba el selector: el activo si es de ese idioma, si no el que más entradas tiene. **Tres pantallas necesitaban lo mismo**, así que la regla vive en un solo lugar en vez de repetida. ⚠️ **Y la etiqueta de cada fila pasa a ser la fuente cuando hay dos diccionarios del idioma activo**: `ES · ES` no desambigua nada. La fuente sale del **tercer segmento del `pack_id`**, que la gramática de D-138 garantiza y `verify_pack.py` verifica. Un `pack_id` que no la cumpla —uno anterior a D-138— **cae al idioma** en vez de que se le invente una fuente partiendo su nombre. Sólo cuentan los packs del idioma activo: desde D-136 se busca únicamente ahí, así que un pack inglés no puede hacer que una fila española muestre su fuente | `LanguageChipsTest` (9) · `ScreensTest` (2, las dos comprobadas fallando sin el arreglo) |
| D-152 | **Toda fila de palabra dice lo mismo**: palabra · tipo · idioma | Reportado: *«en búsqueda aparece palabra-tipo-idioma, pero en el historial sólo palabra-tipo»*. Dos filas que representan la misma cosa con distinta información **le enseñan al usuario que la etiqueta significa algo distinto según dónde esté**, que es peor que no mostrarla. Ahora el historial del inicio, la pantalla de historial completo y las guardadas usan el mismo `wordDetail` que los resultados. ⚠️ **Y un `packId` que no esté entre los packs abiertos sigue sin recibir etiqueta**, igual que en los resultados: heredar la del activo afirmaría un idioma que nadie comprobó (D-080). ⚠️ **De paso se unificó el separador**, que estaba escrito a mano `" · "` en las filas y como recurso `entry_list_separator` en la entrada — dos definiciones de la misma cosa esperando divergir, y una de ellas ya había perdido sus espacios una vez | `ScreensTest` (3, una comprobada fallando sin el arreglo) |
| D-153 | La base de recursos **en inglés se ejercita**, con tests que fijan el locale | ⚠️ **`robolectric.properties` afirmaba una cobertura que no existía.** Fija `qualifiers=es` para que los ~40 tests que afirman texto literal sigan describiendo la pantalla que el usuario ve (D-127), y su comentario decía que la base inglesa *«la cubren los pocos tests que fijan `qualifiers = "en"` a mano»* — **no había ninguno**. El riesgo no es teórico: D-140 encontró **seis textos escritos a mano** que en un reloj en inglés salían en español. Ese caso lo agarra `check_no_hardcoded_translations`; **éste agarra el otro**, que es una clave presente en `values-es/` y ausente o vacía en `values/` — la paridad de claves la verifica el audit, pero que el valor inglés sea el que **se dibuja** sólo lo prueba dibujarlo. Son tres tests sobre las pantallas con más texto y no la suite duplicada: lo que puede romperse es el **cableado**, y con una pantalla de cada tipo alcanza. **Comprobados fallando los tres** al cambiar el locale a español | `EnglishLocaleTest` (3) |
| D-154 | Un **nombre propio va debajo** de una palabra común, salvo que sea exacto o casi | Pedido: *«que los nombres propios automáticamente aparezcan más abajo que palabras comunes a menos que hagan match exactos o muy parecidos»*. ⚠️ **El castigo del builder no alcanzaba, y eso se midió**: `CASTIGO_NOMBRE_PROPIO` (D-134) ya los pone al fondo del `rank` —los `name` van de 993 a 1997 y los `noun` de 795 a 997— pero **la banda de cobertura va delante del rank** en la mezcla (D-142), así que un topónimo corto le gana a una palabra común larga. Sobre el pack real: escribir *ital* devolvía **`Italia` primero y `italiano` segundo**; *medel* ponía `Medellín` arriba de `medellinense`. Con el castigo: `italiano` lidera y `Medellín` baja. ⚠️ **La excepción es la mitad que lo vuelve útil**: `Fez`, `Car` y `Perú` normalizan a exactamente lo escrito —o casi—, y quien escribe la palabra entera puede estar buscando la ciudad. Se aplica **sólo** cuando no es exacto y además no está en la banda de cobertura máxima. Se calcula del `pos` y del texto escrito, **sin mirar ningún número del pack**, igual que la banda | `SearchRepositoryTest` (4) · simulado sobre el pack real antes de escribirlo |
| D-155 | Una guardada se quita **manteniéndola apretada** y confirmando | Pedido, y corregido sobre la marcha: la primera versión fue un botón de borrar a la derecha de cada fila y el pedido pasó a *«que se active manteniendo apretada la palabra, ésta se ponga en rojo con un selector de confirmar borrado»*. ⚠️ **El gesto es mejor y en un reloj no es estética**: un botón de 48 dp al lado de cada fila le come el ancho al lema justo donde el lema es lo único que importa, y queda pegado al blanco que abre la palabra, así que un toque impreciso borra lo que se quería leer. Mantener apretado **no tiene forma de dispararse por accidente**. Armada, la fila se pinta con el color de error y dice qué va a pasar; el segundo toque borra. ⚠️ **Con algo armado, un toque en otra fila desarma y NO navega**: si abriera la palabra, te irías de la pantalla con una fila roja esperándote al volver y sin forma evidente de cancelar. ⚠️ **El historial no se puede armar**: se llena solo al abrir palabras y ya tiene tope — para vaciarlo entero está Ajustes. No hizo falta tocar el modelo: `toggleFavorite` sobre una que ya es favorita la saca | `ScreensTest` (5: que un toque normal NO arma, que el largo arma, que el segundo confirma, que otra fila cancela, y que el historial no se arma) |
| D-156 | El selector de idioma va **debajo de la barra y siempre visible** | Pedido: *«que en la pestaña de resultados también se muestre el selector y sea muy visible en qué idioma se está buscando»*. ⚠️ **Vivía dentro del bloque «sin búsqueda»**, al fondo y bajo *Opciones*, donde no competía con la barra por el lugar de arriba (D-078). El costo apareció al usarlo: **desaparecía justo al buscar**, que es cuando hace falta — mirando resultados que no son los esperados porque el idioma activo no era el que uno creía. Ahora va **pegado a la barra**, que es donde se mira y donde queda claro que modifica lo que se está buscando. **Cuesta una fila de las ~3 que entran** y se paga: el caso que evita es escribir una palabra inglesa con español activo y no entender por qué no aparece. ⚠️ El test que fijaba la ubicación anterior **afirmaba lo contrario y se reescribió**, con el motivo del cambio adentro | `ScreensTest` (2 nuevos y 1 reescrito) |
| D-157 | El botón de voz es un **micrófono**, sin la palabra | Pedido: *«que el voz sea un ícono de micrófono mejor, para que sea multiidiomas»*. La etiqueta decía *Voz* / *Say a word* y ocupaba ancho en la fila que comparte con la barra de búsqueda — ancho que en 234 dp le sacaba a lo único que ahí importa, el campo donde se escribe. ⚠️ **El ícono se dibujó a mano y no se agregó una dependencia**: `Icons.Filled.Mic` no está en `material-icons-core`, vive en `material-icons-extended`, que son varios MB de vectores para usar uno. Es una cápsula, un arco y un pie, en `ic_mic.xml`. ⚠️ **La cadena no se borró, se movió a `contentDescription`**: un ícono sin descripción es mudo para un lector de pantalla, y ahí el idioma vuelve a importar. Eso rompió `EnglishLocaleTest`, que afirmaba el texto *Say a word* con `onNodeWithText` — se reescribió contra la descripción, que es donde la traducción de verdad se lee ahora | `EnglishLocaleTest` (3) · `ScreensTest` |
| D-158 | El idioma de la interfaz **lo guarda la plataforma**, y el diagnóstico va al fondo de Ajustes | Pedido: *«completá la traducción de toda la interfaz para que funcione el selector de idiomas, por defecto en automático»*, y luego *«que la información de diagnóstico aparezca al fondo del menú de ajustes»*. ⚠️ **El idioma de la UI no es una preferencia de la app**: desde API 33 `LocaleManager` lo guarda **por aplicación** y lo aplica antes de que corra un solo Composable, así que una copia nuestra en `Settings` daría dos fuentes de verdad y la de la plataforma ganaría en silencio. Con `minSdk 33` no hace falta `appcompat` ni una clave nueva. **Automático es la ausencia de elección** —un `LocaleList` vacío—, no un tercer idioma: por eso el callback reporta `null` y no un tag. ⚠️ **Y el tag que devuelve la plataforma no es el que se escribió**: viene resuelto, con región —`es-CL`, `es-419`, `en-US`—, así que comparar la cadena entera dejaba la lista **sin nada marcado**, que se lee como que la elección se olvidó. `UiLanguage.of` compara sólo el subtag primario. ⚠️ **Los endónimos —`Español`, `English`— son literales a propósito y son la única cadena de la UI que no se traduce**: un selector de idioma escrito en el idioma que no entendés no le sirve a nadie, y justamente a quien le llegó el reloj en un idioma ajeno es a quien el selector existe para rescatar. El audit lo vigila en las dos direcciones — una carpeta `values-xx/` sin fila en el selector es una traducción que **nadie puede elegir**, y una fila sin carpeta deja la app en inglés con el selector marcando otra cosa. ⚠️ **El diagnóstico va último y eso fue pedido explícito**: se mira una vez, cuando algo anda mal, y arriba empujaría fuera de pantalla los ajustes que la gente sí cambia. Muestra `BuildConfig.VERSION_NAME` —el número del APK, no un literal que se queda atrás de la property (D-095)— y una línea por pack instalado con sus entradas. Para eso se encendió `buildFeatures.buildConfig`. ⚠️ **La pantalla sigue sin leer un servicio del sistema**: el idioma entra por parámetro y `MainActivity` hace la llamada, que es lo que deja estos cuatro tests en el gate en vez de en un dispositivo (D-072) | `UiLanguageTest` (4, una comprobada fallando con el tag completo) · `ScreensTest` (4) · `check_ui_language_picker`, comprobado fallando en sus tres mitades |
| D-159 | La ficha **escribe el tipo entero**; la fila lo sigue abreviando | Pedido: *«que sust., adj., noun aparezcan resumidos como ahora donde no hay espacio, pero que dentro de la ficha de una palabra se expanda»*. Son **dos lugares con presupuestos distintos** y por eso son dos funciones y no una: en una fila de 234 dp el lema es lo único que importa y `sustantivo` le come el ancho (es la razón de D-125 y de las abreviaturas); la ficha no compite con nada, y ahí `sust.` es una abreviatura que alguien tiene que descifrar para leer su propio diccionario. ⚠️ **Las 15 claves `pos_full_*` ya estaban escritas en los dos idiomas y no las usaba nadie**: 30 cadenas de texto muerto que el audit no ve —la paridad las acepta porque están en las dos tablas— y que se iban a quedar así porque nada falla. `posLabelFull` **cae a la abreviatura y no al código crudo** si falta la clave larga: un pack puede traer un `pos` que no conocemos, y media etiqueta es mejor que `intj`. ⚠️ **El test que afirmaba `sust.` en la ficha se actualizó, no se duplicó**: el nuevo que escribí al lado decía lo mismo, y dos tests del mismo hecho divergen en cuanto alguien arregle uno | `ScreensTest` (el de la ficha, actualizado, más uno nuevo que falla si la FILA se expande) |
| D-160 | Las herramientas de calidad **son gates o se pudren**, y el linter va separado del formateador | Revisión completa del repo. ⚠️ **Los dos linters existían y ninguno era un gate, y los dos habían derivado**: Android lint acumulaba **17 advertencias** que nadie veía, y `hatch run lint:check` tenía **26 violaciones** de ruff — entre ellas variables asignadas y nunca usadas. Ahora `lint { warningsAsErrors = true }` rompe el build con una advertencia. ⚠️ **Se apagan exactamente tres reglas y por un motivo estructural, no por conveniencia**: `NewerVersionAvailable`, `AndroidGradlePluginVersion` y `GradleDependency` avisan de que hay una versión más nueva de una dependencia, así que **rompen el build con el paso del tiempo sin que nadie toque nada** — y un gate que se rompe solo se termina apagando entero. Actualizar dependencias es una tarea deliberada. ⚠️ **Del lado de Python la causa de la deriva era el empaquetado del comando**: `lint:check` corría linter **y** formateador juntos, y `ruff format --check` reporta **2.359 líneas en 28 de 36 archivos**, porque el Python de este repo se escribió a mano con otro estilo y el formateador nunca corrió. Con ese ruido encima las 26 violaciones reales quedaban enterradas. Ahora `check` busca **defectos** y tiene que estar en cero; adoptar el formateador es un acto aparte con su propio comando, porque un diff de 2.359 líneas no puede mezclarse con nada que alguien tenga que revisar. ⚠️ **Y lint pagó en el acto algo que yo no sabía**: al volverlo fatal reportó que el CLDR moderno le da a español una categoría `many`, que existe por gramática y no por formato — se dice *«un millón DE entradas»*. El pack de inglés tiene 956.150, así que ese caso está a una fuente de distancia. Sumado: los reportes del compilador de Compose quedan detrás de `-Pdictionary.composeReports`, que es la única forma de medir el trabajo de UI | `lint { warningsAsErrors }` en el gate · `hatch run lint:check` en cero · la categoría `many` la encontró el propio chequeo |
| D-161 | Un número que un documento afirma **tiene enforcer** | ⚠️ **Se encontraron cuatro conteos falsos a la vez**: `README.md` decía 560 tests, `dict-data/CLAUDE.md` 31 instrumentados, `tools/CLAUDE.md` 129 de Python y `app/CLAUDE.md` 178 de JVM. Ninguno rompía nada, ninguno era cierto, y cada uno le hace perder el tiempo a quien lo lea — o peor, le hace creer que una suite encogió. Es la misma decadencia que `check_doc_paths` ya atrapa para las rutas, aplicada a los números. **El conteo es estático** —líneas que empiezan con `@Test` o `def test_`— y eso **no es una aproximación**: comparado contra los conteos de runtime de Gradle y unittest da exactamente lo mismo (81, 251, 250), y ser estático deja el chequeo dentro de la auditoría, que es stdlib pura y no compila nada. ⚠️ **Si alguien reescribe la frase y el patrón deja de matchear, el chequeo FALLA en vez de callarse**, que es la misma política de `check_app_logic_is_jvm_testable` con sus archivos vigilados: un chequeo que se apaga solo cuando tocan lo que vigila no vigila nada. Comprobado fallando en los dos caminos — número equivocado y frase reescrita | `check_test_counts` (11 afirmaciones en 5 documentos) |
| D-161b | `check_doc_paths` pregunta por el **repo**, no por la máquina | Encontrado verificando un commit con `git worktree`, que es el único lugar donde se ve. El chequeo usaba `os.path.exists`, así que una ruta **generada y no commiteada** —el pack de juguete, que D-020 decidió no versionar— **existe en la máquina de cualquiera que haya construido una vez y no existe en un clone limpio**. ⚠️ **Venía fallando en todo clone limpio desde hacía sesiones y pasando en el árbol de quien escribía**, que es la peor forma de fallar: el que lo rompe nunca lo ve. La exención sale de `.gitignore` y no de una lista aparte, porque esa es la definición que ya existe: si git lo ignora, no es un archivo del repo. El matcher es deliberadamente parcial —prefijos de directorio y sufijos— y no reimplementa el de git, que no hace falta. Comprobado en las dos direcciones: una ruta inventada sigue fallando, el pack de juguete ya no | `check_doc_paths`, comprobado fallando y no fallando |
| D-162 | Lo que **no es la pantalla** sale de `SearchScreen.kt` | 861 líneas, de las cuales ~215 no dibujaban nada: agrupación de packs por idioma y resolución de etiquetas. ⚠️ **El síntoma que lo delató no es el tamaño, es una dependencia al revés**: `WordOfTheDayTileService` importaba `posLabel` **desde un archivo de pantalla**. Un Tile no tiene composición ni pantalla; que alcanzara dentro de `SearchScreen` para resolver una etiqueta decía que la etiqueta no vivía donde correspondía. Ahora hay `Labels.kt` —cómo se NOMBRA en pantalla lo que el pack guarda como código, que usan dos superficies distintas— y `PackGrouping.kt` —la traducción a pantalla de *«un pack es una fuente de un idioma»* (D-136), que son **decisiones y no dibujo** y por eso el gate las cubre en la JVM con `LanguageChipsTest` en vez de con Robolectric—. `SearchScreen` queda en **686 líneas**. ⚠️ **Movimiento puro, cero cambio de comportamiento**: lo único que cambió de firma es `matchLabel`, que pasó de `private` a `internal` porque cruzó de archivo. Aparecieron de paso **cuatro imports sin uso** —uno de ellos, `CircularProgressIndicator`, sobraba desde antes— y `allWarningsAsErrors` **no los agarra**: Kotlin no advierte por un import sin usar | el gate entero verde sin tocar un solo test |
| D-163 | **R8 encendido**, con las reglas mínimas y un enforcer que las mantiene vivas | Medido antes de encenderlo: el APK pasa de **33,0 a 5,47 MB** y el dex de **29,5 a 2,70 — un 91 % menos**. Importa para batería y no sólo para tamaño: menos dex es menos carga de clases, menos memoria y menos JIT en **cada arranque del proceso**, y en esta app cada arranque es alguien mirando la pantalla (`docs/bateria.md`). Cierra O-2, que llevaba desde D-087 como «planificado» sin número. ⚠️ **Las reglas son tres y ninguna hace falta para que compile**: compila y encoge sin una sola. Están para que **siga** funcionando cuando alguien toque algo. Los dos `TileService` la tienen porque están nombrados en el manifest **y** se pasan como literal de clase a `requestUpdate`, y `app/CLAUDE.md` ya tiene escrito que romperlos **no da error de compilación ni test**; el driver de SQLite la tiene porque cruza JNI y del otro lado el nombre está compilado en el `.so`. ⚠️ **Lo verificado y lo NO verificado, que es la parte importante**: se comprobó que el manifest del release conserva los tres nombres y que el dex encogido los contiene, con `aapt2 dump xmltree`. **Eso es un hecho sobre un archivo, no sobre una app que corre**, y el riesgo que O-2 siempre nombró —código que sólo alcanza la reflexión— sólo se cierra instalando en el reloj. ⚠️ **Y las reglas pueden dejar de servir en silencio de dos formas**: que alguien pierda el `files.add(...)` al reorganizar el build —las reglas siguen en disco y no se aplican, el build sigue verde— o que una regla nombre una clase renombrada. `check_r8_keep_rules` cubre las dos | `check_r8_keep_rules`, comprobado fallando en sus tres mitades — y **el propio chequeo tenía un falso negativo** que apareció al comprobarlo: miraba el build file con comentarios, y el comentario nombra el `.pro` |
| D-164 | Un pack **no se vuelve a probar si es el mismo archivo** | Pedido explícito: *«me interesa quitar validaciones innecesarias a los packs de idiomas desde el reloj cada vez que se inicia»*. ⚠️ **Yo había listado esto como «debilita un guardrail y no es decisión de un agente»**; el usuario lo pidió igual, así que se hizo — pero **no sacando la prueba, sino dejando de re-probar un archivo inmutable**. Medido: la muestra de 64 claves de D-142 son **36 de los 41,33 ms** que costaba cada arranque con los dos packs reales; ahora el primer arranque tras instalar sigue pagando 41,33 y **todos los demás pagan 6,30 — un 85 % menos**. El pack es inmutable por D-001, así que volver a probar byte por byte el mismo archivo no prueba nada que no se supiera. ⚠️ **Lo que hace que esto sea seguro y no un atajo es que la huella lleva `NORM_VERSION`**: si `norm()` o `fuzzy()` cambian, **todo** pack verificado vuelve a probarse, y alcanza con ese número porque D-005 y D-006 obligan a subirlo en el mismo commit que cambia cualquiera de las dos. La huella lleva además tamaño y `mtime`, porque `devpack.py` y el instalador escriben el `.db` **en su lugar** (D-082) y el nombre no alcanza. ⚠️ **No lleva un hash del archivo a propósito**: hashear 301 MB en cada arranque cuesta más que releer 64 filas. ⚠️ **Y el interruptor apaga SÓLO la muestra**: `schema_version`, `norm_version`, `payload_codec` y el sha256 del diccionario se comprueban siempre — son 6,30 ms y cubren fallas distintas. El default de `verifyKeys` es `true`, así que quien no sepa de esto obtiene la validación entera. ⚠️ **Lo que NO está cubierto por el gate**: el cableado en `PackStore.openFile`. La decisión pura está en `PackVerificationTest` y el interruptor en dos tests instrumentados, pero que `PackStore` consulte el memo necesitaría un `Context` **y** un pack real, o sea un test en dispositivo | `PackVerificationTest` (8, una que falla si un bump de `NORM_VERSION` no invalida) · `PackKeySampleTest` (+2, instrumentados, **sin correr**: no hay reloj) |
| D-165 | El hash va **al instalar**, no en cada arranque | Pregunta directa: *«que la validación se realice una vez y luego se guarde que ese pack ya fue validado por algún método barato, considerando quizás hash»*. ⚠️ **Un hash es la herramienta correcta para una de las tres preguntas y la equivocada para otra**, y separarlas es toda la decisión. **(1) Al instalar o descargar** la pregunta es *«¿llegaron los bytes que se publicaron?»*, y ahí el sha256 sale **casi gratis**: los bytes ya están pasando para copiarse, así que digerirlos no agrega una lectura. **(2) En cada arranque** la pregunta es *«¿es el mismo archivo que ya probé?»*, y ahí un hash **obligaría a releer 301 MB** — mucho peor que las 64 filas que se querían ahorrar; por eso ese chequeo es la huella de D-164. **(3) Descubrir un pack que la app no instaló no necesita mecanismo**: sin anotación en el memo se valida entero la primera vez, o sea que **la ausencia de la anotación *es* el descubrimiento**. ⚠️ **El orden dentro de la instalación es todo**: se compara **antes de renombrar**. Renombrar primero dejaría un diccionario corrupto llamándose como el bueno, y un `.db` truncado **es SQLite válido** — el siguiente arranque lo abre sin quejarse. ⚠️ **Sin `sha256` esperado no se digiere nada**, porque el pack de demostración sale de los assets y no tiene contra qué compararse: calcular un hash que nadie mira, sobre cientos de MB y en un reloj, no es gratis. Esto cierra la **mitad de mecanismo** de lo que el roadmap llamaba *«el requisito número uno»* del instalador; la otra mitad es el catálogo, que es decisión de producto | `PackStoreTest` (4, una que falla si se renombra antes de comparar) |
| D-166 | Hay un build type **instalable por adb que lleva R8** | Pregunta: *«¿se puede activar R8 en modo debug? Sigo sin release productivo para poder instalarlo por adb»*. Se puede, y es la herramienta equivocada: **R8 tarda ~3 minutos** y `debug` es la build que se compila veinte veces por día, y dejaría de ser la build donde se verifica a diario — sería ya otra cosa. En vez de eso hay un build type `benchmark`: `initWith(release)`, firmado con la **clave de debug**, `isDebuggable = false`. **5,48 MB, firma V2 de Android Debug**, o sea que `adb install` funciona hoy sin keystore de release. ⚠️ **NO lleva `applicationIdSuffix`, y es deliberado**: con sufijo se instalaría al lado del debug, con otro `filesDir`, y habría que volver a copiar **300 MB de packs** para probar. Sin sufijo y con la misma clave, reemplaza al debug en el lugar y **los packs se quedan donde están**. ⚠️ **`isDebuggable = false` no es cosmético**: un build debuggable desactiva optimizaciones del runtime, así que medir arranque sobre él mediría otra cosa. Y no es un invento para esta sesión: **Macrobenchmark exige exactamente esta forma** para generar los baseline profiles, que es el paso siguiente. ⚠️ **`check_release_signing` tenía un falso positivo latente**: buscaba `getByName("debug")` en todo el archivo, así que habría prohibido este build type por accidente. Ahora mira sólo dentro del bloque `release`, que es de lo que la regla habla | `apksigner verify` sobre el APK · `check_release_signing`, acotado y comprobado fallando en el caso real |
| D-167 | El manifest declara su **lista de idiomas** y no pide permisos que no usa | Revisión pedida de buenas prácticas. **Tres hallazgos, uno de ellos un hueco que yo mismo abrí.** ⚠️ **(1) Faltaba `localeConfig`, así que la app NO aparecía en Ajustes → Idiomas del sistema.** El selector propio de D-158 **funciona igual** —`setApplicationLocales` no lo necesita— y por eso el hueco pasó dos sesiones sin que nadie lo notara: mirando la app no se ve. Se resuelve con `generateLocaleConfig = true`, **generado y no escrito a mano a propósito**: AGP lo arma desde las carpetas `values-*` reales, así que no puede quedarse atrás de una traducción nueva — una lista a mano sería una segunda fuente de verdad del mismo dato. Verificado en el manifest fusionado: declara `en` y `es`. ⚠️ **(2) `WAKE_LOCK` estaba declarado desde el template con cero usos en el código.** No rompe nada y se le muestra al usuario al instalar. Fuera. **(3) `appCategory="productivity"`**, que es como el sistema agrupa el uso de batería de la app. **Lo que se revisó y está bien, y queda escrito para no re-derivarlo**: `uses-feature hardware.type.watch`, `standalone=true`, `uses-library com.google.android.wearable` requerida, `roundIcon`, `taskAffinity=""`, los dos tiles exportados con permiso y sin `GROUP`, `dataExtractionRules` excluyendo `packs/`, y **ningún permiso de red** — que es la primera línea del README y ahora tiene enforcer | `check_manifest_hygiene`, comprobado fallando en sus tres mitades |
| D-168 | Si el idioma activo no tuvo **nada parecido**, contestan los otros | Pedido: *«que evite generar conflictos cuando la palabra que busco está en inglés pero por error seleccioné español»*, con la condición de *«que no se sobrecargue la búsqueda en varios packs innecesariamente»*. ⚠️ **El umbral se midió antes de elegirlo, sobre los dos packs reales**: se dispara cuando no hay ninguna coincidencia exacta **y** ninguna fila en la banda de cobertura máxima. De **400 lemas españoles comunes, 0** lo disparan — el caso normal no paga absolutamente nada. De **400 lemas ingleses comunes, 321 (80 %)** sí. Los 79 que no lo disparan —`break`, `man`, `go`, `line`, `bear`— **están de verdad en el pack español**, así que no llegar al respaldo es la respuesta correcta y no un falso negativo. ⚠️ **El respaldo NO va simplemente «después», y ésa fue la corrección**: si el idioma activo devolvió diez resultados por parecido fonético, poner la respuesta correcta debajo de todos la deja fuera de pantalla, que es lo mismo que no haberla buscado. Se mezcla con el mismo comparador, y el idioma activo desempata **después** de la calidad — a igualdad de todo lo demás manda el que elegiste, porque lo elegiste. ⚠️ **La condición se calcula del texto escrito y del lema, sin mirar un número de ningún pack**, igual que `coverageBand`: un pack mal calibrado no puede ni disparar el respaldo ni taparlo. ⚠️ **No aplica a la búsqueda por definición**: ahí lo escrito es una palabra de la glosa y no un prefijo del lema, así que la cobertura no significa nada y el umbral no se puede calcular sin inventar un criterio | `SearchRepositoryTest` (5, una que mide lo que NO se consulta) |
| D-169 | Sinónimos y antónimos: **categoría arriba, palabras tocables abajo** | Pedido: *«primero mostrando la categoría y abajo las palabras, pudiendo hacerles click para ir a ellas»*. La categoría pasa de prefijo de cuatro letras —`sin.`, `ant.`, `rel.`— a título escrito entero, que es la misma regla de D-159: en una fila se abrevia porque el lema necesita el ancho, en la ficha no compite con nada. ⚠️ **Cuesta una línea más por lista y en 234 dp eso se paga**; se acepta porque la línea que agrega es justo la que dice de qué lista estás leyendo, que es lo que D-126 y D-132 dicen que no puede faltar, y se abarata con `labelSmall` y sin padding vertical entre el título y sus palabras. ⚠️ **Sólo se pinta como enlace lo que existe en el pack**, igual que en la glosa: el color es la promesa de que lleva a algún lado. Un término que el pack no tiene **se sigue mostrando** —la fuente lo dice y esconderlo sería perder información— pero sin pintar. ⚠️ **Dos consultas y no una, y el motivo es un tope duro**: `MAX_PALABRAS_POR_CONSULTA` es 64 porque cada clave es un parámetro enlazado de SQLite, y las glosas ya promedian 39,9 claves en español y 58,8 en inglés. Meter además los términos desbordaría el tope y **recortaría en silencio justo lo que más vale como enlace**. Separadas, cada una tiene su propio tope: 0,18 ms cada una | `ScreensTest` (3 nuevos, 3 actualizados) · `EnglishLocaleTest` (las tres cadenas nuevas) · el clic sigue siendo `GlossLinksOnDeviceTest`, que necesita dispositivo |
| D-170 | `data_version` lo **deriva el builder** del reloj del build; la fecha del dump es `source_date` | Pregunta: *«ayúdame a generar un data_version basado en algo reconocible, ¿podría ser timestamp?»*. ⚠️ **El campo mezclaba dos cosas y por eso ninguna elección servía**: *de qué volcado sale el contenido* (informativo, del humano) y *qué build del pack es esto* (ordena, y tiene que distinguir dos builds del mismo dump). Separadas, cada una tiene la forma que le corresponde. `data_version` pasa a ser **`AAAAMMDDHHMM`**: ordena como números, **se lee sin convertidor** —`202609211432` es 21/09/2026 14:32, que era la mitad del pedido— y distingue al minuto, de sobra cuando un build tarda minutos. Un epoch también ordenaría y nadie podría leerlo de un vistazo. ⚠️ **Y es reservado**: declararlo a mano es un error del builder, porque si se puede escribir a mano alguien se olvida de subirlo — que es exactamente lo que pasó durante meses y el motivo del ítem del roadmap. ⚠️ **No entra en un `Int` de 32 bits** (202609211432 > 2.147.483.647), así que `PackMetadata.dataVersion` pasó de `Int` a `Long`. Si alguien lo revierte, el pack revienta **al abrir, en el reloj**, con un NumberFormatException que no nombra la clave (D-070) — hay un test del builder que lo fija desde el otro lado | `DataVersionTest` (5) · `verify_pack.py` ya comprobaba que sea entero, y el test que lo cubría se reescribió porque su causa original ya no puede ocurrir |
| D-171 | **Instalado y consultado dejan de ser la misma lista**, con dos reglas en una función | Pedido: *«ayúdame a definir estas reglas»*, después de cinco preguntas de diseño sobre núcleo y completo. ⚠️ **La distinción es la mitad de la decisión**: Ajustes tiene que seguir mostrando **todo** lo que ocupa disco —si no, un pack que no se consulta se vuelve invisible y no hay forma de borrarlo— y la búsqueda tiene que preguntarle sólo a lo que puede aportar algo. Hasta acá eran la misma lista. **Regla 1, con usuario hoy: de cada `pack_id`, el `data_version` mayor.** Dos archivos con el mismo `pack_id` son el mismo diccionario (D-138) y el build viejo es estrictamente peor (D-170). Cierra un defecto real: `PackStore` abre **todos** los `.db` del directorio, así que reinstalar sin borrar el anterior dejaba **dos builds contestando** — no salía mal, porque la deduplicación por `(lema, tipo)` lo tapa, pero se pagaba el doble de consultas y de disco **en silencio**. **Regla 2, sin usuario todavía: un pack no se consulta si el que lo contiene también está.** Es la que el núcleo va a necesitar; se escribe ahora porque es la misma forma y separarlas serían dos recorridos con el mismo bug. ⚠️ **`subset_of` y no `entry_count`**: el conteo dice cuál es más **grande**, y lo que hace falta saber es **contención** —dos packs de fuentes distintas pueden ser los dos grandes sin que ninguno contenga al otro, y ahí consultarlos a los dos es lo que se quiere—. Y la contención no se puede deducir en el reloj: comparar 150.000 lemas costaría más que la búsqueda. ⚠️ **El ciclo casi deja la búsqueda sin diccionario, y lo encontró el test escrito para eso.** La versión obvia —*«sacá a todo el que nombre a alguien presente»*— con dos packs que se declaran subconjunto mutuamente **los saca a los dos**. La regla correcta es que **sólo absorbe el que no fue absorbido**; el costo es que la contención no encadena, y se acepta: el peor caso de no encadenar es trabajo de más, el de encadenar es quedarse sin nada | `PackSelectionTest` (7, una que falla con la versión obvia de la regla) |
| D-172 | El pack **activo** sale de las mismas reglas que deciden a quién se consulta | ⚠️ **D-171 cerró la regla y dejó abierto el único camino que la anulaba.** La selección sacaba el build viejo de la lista a consultar, pero el activo se elegía **aparte**, con un `firstOrNull` sobre el listado del directorio —que no promete orden— y después se agregaba a la consulta **siempre**. Si caía el viejo, se consultaba el viejo por ser activo **y** el nuevo por estar en la lista: los dos builds contestando, que es exactamente el defecto que D-171 venía a cerrar. Tres lugares elegían por su cuenta —`PackStore.open`, `SearchViewModel.onPackChange` y `repositoryFor`— y ahora los tres pasan por `activePack`. El orden de preferencias quedó explícito y cada escalón tiene motivo: **sólo entre los que se consultan** (elegir a mano un pack que otro contiene no puede devolverlo: no se le va a preguntar nada), **un diccionario real le gana a uno de demostración** (D-081, y era otro `firstOrNull` sobre una lista sin orden), y recién ahí **lo que el usuario eligió**. ⚠️ **La lección de las dos decisiones juntas**: una regla que filtra una lista no sirve si otro camino construye esa lista de nuevo. Lo que hay que buscar al escribir una regla así no es dónde aplicarla, es **quién más decide lo mismo** | `PackSelectionTest` (13, seis del activo) |
| D-173 | Un pack del APK **dice que lo es**, y el flag se llama como lo que decide | Pedido: *«que se pueda mostrar en la lista de diccionarios indicando que es un pack incluido y sin dar la opción de borrar»*. ⚠️ **La mitad de borrar ya estaba y la mitad de explicarla no**: el pack del APK nunca ofreció borrar —volvería sola al reiniciar, porque `PackStore` lo re-extrae— pero la fila **no decía por qué**, y un botón que falta sin explicación se lee como un bug y no como una decisión. Ahora la fila dice *«Incluido en la app»* **en lugar del tamaño**, que es justo el dato que ahí sobra: el tamaño sólo sirve para decidir si conviene borrar algo. ⚠️ **Y `isDemo` pasó a `isBundled`, porque el nombre mentía sobre lo que decide.** Lo que preguntan sus cinco usos es *«¿este archivo lo trajo la app o lo puso el usuario?»* — de ahí sale que no se pueda borrar y que no le gane a un diccionario real (D-081). Que hoy el pack incluido sea uno de juguete es una propiedad de **este** build, no de la regla: cuando el núcleo ocupe ese lugar, las reglas son las mismas y el nombre habría quedado falso. ⚠️ **Y lo que NO se puede hacer, verificado con `javap` sobre `sqlite-bundled 2.7.1`: abrir el `.db` dentro del APK sin extraerlo.** La superficie entera del driver es `open(String)` y `open(String, Int)` — sin VFS propio, sin `sqlite3_deserialize`, sin descriptor. Un asset vive en un offset dentro del ZIP y SQLite abre por ruta asumiendo el byte 0. **La extracción se queda**, y su objeción original (D-071) no aplica a esta escala: duplicar 7,5 MB sobre un reloj con 40 GB libres es el 0,02 % | `ScreensTest` (3: que lo diga, que no lo diga de más, y que sólo el instalado ofrezca borrar) |
| D-174 | Extender el formato es **aditivo o cuesta 372,6 MB**, y cada superficie tiene su regla | Pedido: *«¿tenemos espacio para desarrollo incremental o migración de esquemas sin romper muchas cosas? Me interesa empezar a tener consideraciones de extensibilidad»*. **No hay migraciones** (D-001): un pack incompatible se rechaza al abrir y hay que reconstruirlo, o sea que lo único barato es lo que un lector viejo puede **ignorar**. ⚠️ **Y el número que hace que la regla importe: equivocarse cuesta 372,6 MB** de packs a reconstruir y volver a subir. **Inventariado, no supuesto**: la app lee 4 tablas, 13 consultas, **11 claves de meta con `getValue` y 6 con `meta[...]`**, y **no hay un solo `SELECT *`**. De ahí salen las cuatro reglas: un **tag de payload** se agrega en las dos direcciones —el parser ignora los desconocidos— siempre que **no se suba `payload_codec`**, que se compara con `!=` y tiraría esa propiedad (D-119 ya lo documentó); una **clave de meta** se agrega en las dos direcciones si se lee con `meta[...]`, hecho tres veces sin romper un pack; una **columna o tabla** es segura hacia atrás —ninguna consulta usa `SELECT *`— pero **no hacia adelante**: una app nueva consultando una columna que un pack viejo no tiene falla **al consultar**, que es el peor lugar, porque D-001 existe justamente para fallar fuerte **al abrir**; y cambiar el significado de algo que ya existe sube `schema_version` y rechaza todo lo instalado. ⚠️ **La asimetría que vale la pena saber: el código es más tolerante que el gate, en exactamente una superficie.** Agregar una columna no rompería una sola consulta y `schema_version` rechaza el pack igual. No se arregla hoy —sería partir la versión en dos números, y eso se paga cuando haya un cambio que lo pida, no antes— pero queda escrito que la palanca existe | `check_required_meta_keys`, comprobado fallando en las dos direcciones · `docs/formato-pack.md` §Extending the format |
| D-175 | El APK lleva los **núcleos de ES y EN**, derivados del pack completo | Pedido: *«reemplazar el pack demo por los packs core de español e inglés»*. El juguete de 28 entradas sale; entran **7.349 entradas en 4,76 MB** (español, de 71,68) y **16.652 en 11,92 MB** (inglés, de 300,93). El APK pasa de **5,48 a 17,11 MB** y la app deja de decir *«instalá un diccionario»*. ⚠️ **Se DERIVAN del pack completo, no se construyen en paralelo, y eso es correctitud y no comodidad**: el núcleo declara `subset_of`, y derivándolo esa afirmación es cierta **por construcción** — construido aparte sería cierta sólo mientras las dos corridas usaran las mismas fuentes y los mismos filtros, una promesa que nada comprueba y que se rompe en silencio la primera vez que alguien agrega una opción a una sola. Además tarda **7 segundos** en vez de una hora y no necesita los 4,5 GB de dumps. ⚠️ **El vocabulario se elige por FRECUENCIA DE USO (Tatoeba), nunca por `rank`, y la diferencia está medida**: por `rank` un núcleo se lleva el **91 %** de la tabla de flexiones del pack español —las páginas más ricas son los verbos, y un verbo tiene 33 formas— y por frecuencia, el **5,5 %**. Dieciséis veces. Quien lo implemente con `rank` obtiene decenas de MB y concluye que la idea no servía. ⚠️ **El `uid` se COPIA, no se recalcula**: `sense_key` se decide contando los homógrafos del pack final (D-145), y un subconjunto tiene menos, así que recalcularlo le daría otra identidad lógica a la misma palabra y rompería el join entre packs (D-055). ⚠️ **Y degrada**: los packs completos viven fuera del repo, así que un clone limpio no los tiene y empaqueta el juguete — misma regla que la keystore (D-086), **un clone limpio tiene que seguir compilando**. Verificado construyendo con `-Pdictionary.packsDir=../no-existe` | `test_core` (9, una que falla si el uid se recalcula) · `verify_pack.py` sobre los dos núcleos reales · el clone limpio construido |
| D-176 | Una versión nueva de la app **re-extrae sus packs incluidos** | ⚠️ **Bug visto en el reloj**: la app avisaba que `demo-es-en.db` no era compatible —se extrajo con `deflate-v1` y la app pasó a `deflate-v2` (D-119)— mientras el APK traía uno bueno que **nunca se copiaba**. La causa: la extracción miraba **sólo si el nombre faltaba en disco**, así que un pack incluido se extraía una vez y no se actualizaba jamás. Es exactamente el costo que D-119 aceptó *«porque hoy el costo es cero»*; con D-175 el pack incluido pasa a ser el diccionario de verdad y dejar de actualizarlo no sería una molestia. Ahora se guarda el `versionCode` que extrajo por última vez y una versión nueva re-extrae. ⚠️ **Lo que el usuario instaló NO se toca nunca**: son 372 MB que nadie quiere volver a copiar por adb, y hay un test que falla si se tocaran. ⚠️ **Y se anota DESPUÉS de copiar**: si la extracción falla a medias, el próximo arranque la reintenta en vez de darla por hecha | `PackStoreTest` (6, dos nuevos) |
| D-177 | Hay un **tercer pack, bilingüe ES→EN**, y sirve en las dos direcciones ⚠️ **superada por D-195/D-196: ya no es ES→EN sino ES↔EN, con entradas de los dos idiomas** | Pedido: *«comenzá a construir el pack de traducciones como un tercer pack para instalar»*. **123.979 entradas en 50,2 MB**, `kind = bilingual`, `lang_dst = en`, y es el primero que llena `trans` — **206.727 filas**. ⚠️ **Un solo pack sirve para las dos direcciones y eso hay que verlo para creerlo**: las entradas son palabras españolas con glosas en inglés, así que `perro` se encuentra por prefijo y `dog` por el índice inverso, que es el peldaño 3 de la cascada y ya existía sin usarse desde D-034. Comprobado sobre el pack real: `hammer → martillo`, `pepper → ají, pimentón, pimiento`, `scaffold → andamio, cadalso`. ⚠️ **La primera medición dijo que NO se podía, y estaba mal planteada.** Medí *glosas enteras limpias*: **11,7 %**, y concluí que la dirección inversa sería una lotería. Lo que el índice necesita es mucho más débil —**un término usable de cualquier acepción**— y eso da **87,6 % de las entradas**, con 2,3 claves cada una. La conclusión se dio vuelta al construirlo. ⚠️ **El lado de las entradas NO se reimplementó**: `kaikki.records` ya poda las páginas de flexión al `form`, agrupa homógrafos y aplica la política de nombres propios, todas decisiones con su propia medición; una segunda copia sería un segundo lugar donde arreglar cada bug. Lo único nuevo es qué glosa **es** una traducción, y la regla es conservadora a propósito: `MatchKind.TRANSLATION` le promete al lector que la fila contesta lo que escribió, y una clave sacada de una descripción lo convertiría en «alguna definición menciona esto», que es lo que ya hace FTS. ⚠️ **Dos fugas se encontraron LEYENDO la salida, no razonando**: `CAT scan")` y `or cultures)`, y las agarran **reglas distintas** — la primera es un término bueno con basura pegada, la segunda empieza con una conjunción porque el separador la cortó del medio de una frase. Por eso `or` y `and` están en la lista de descriptores y `of` **no**: *of course* es una traducción | `test_source_bilingual` (11) · `verify_pack.py` sobre el pack real |
| D-178 | Las traducciones entran al payload **por acepción** (tag `T`), leyendo el campo `translations` que el dump del Wikcionario siempre trajo | El pipeline nunca lo leyó. Medido: **34.710 pares ES→EN** sobre 22.520 lemas, y el **99,9 %** de las acepciones declara `sense_index`, así que el join es un **match de claves** y no aritmética — el mismo `_by_sense_index` que D-117 usa para los sinónimos. ⚠️ **El índice numera la PÁGINA del wiki, no el registro**: `alemán` tiene 2 acepciones y traducciones en `[1] [2] [4]`, porque kaikki parte la página en un registro por categoría gramatical. Cada acepción declara su etiqueta, así que los índices ajenos **no unen, y eso es correcto**. ⚠️ **Se filtra por idioma**: `en` son 34.710 de 281.022 items, sin filtro una entrada española mostraría su traducción al polaco. ⚠️ **Se expanden rangos** (`1-2`), +13,2 puntos, y tocar el lector compartido es inocuo: sinónimos y antónimos son **100 % índices simples**. Sin cambio de esquema: el tag ya existía en los tres extremos y nadie lo dibujaba. **Costo medido contra un build gemelo: +36 KB sobre 6,16 MB, +0,60 %** | `TraduccionesTest` (9) · `ScreensTest` (2) |
| D-179 | **Segundo canal**: el tag `W` lleva las traducciones **de la palabra**, que la fuente no pudo atribuir a una acepción | ⚠️ **Existe para que la opción deshonesta deje de ser la barata.** Con un solo canal, un builder con dato no atribuible sólo podía tirarlo o **embadurnarlo por todas las acepciones** — gratis, invisible, y pasa `verify_pack`: el error de D-117. Medido: el **37,7 %** de las traducciones no trae índice, y en el pack de muestra eran el **34,8 % del dato tirado** (`construir` tenía 16 y mostraba 0). Aditivo, **no sube `CODEC_ID`** (D-119): un lector viejo lo descarta por su guarda `if senses:`. ⚠️ **Se dibuja FUERA de `SenseBlock`**: una lista bajo una acepción **afirma** que le pertenece, y juntarlas desharía en la pantalla lo que el formato separó. Reparto medido: **48,4 % sólo por acepción, 48,6 % sólo lista, 3,0 % las dos** | `TraduccionesDeNivelEntradaTest` (4) · `ScreensTest` (1) |
| D-180 | Una acepción se nombra con `sense_code = sha256(uid ␟ fold_gloss(glosa))[:12]`, que **no nombra ningún pack** | Pedido: *«que el link a la acepción sea inequívoco y único para esa palabra, idioma y pack; core y completo pueden repetir este código»*. ⚠️ **No hizo falta inventar nada**: `entry.uid` ya lleva idioma y palabra (`stable_uid(lang, headword, pos, sense_key)`). Las tres propiedades, verificadas: no nombra pack (por construcción); **núcleo y completo lo comparten — 21.534 de 21.534, 100,0 %**, porque `build_core.py` **copia** el uid (D-175); y **degrada a la palabra**, porque el código es un *sufijo* del término. Colisiones: **22 de 210.249 (0,0105 %)**, glosas que el wiki define dos veces. ⚠️ **Es un SEGUNDO contrato entre los dos lenguajes** y lleva vector compartido: `sense_code(1, "casa")` = `8ec316909e48`. El destino se declara por **idioma** (`translations_to`) y no por pack: nombrar el pack mataba el enlace del usuario que tiene el núcleo y no el completo | `CodigoDeAcepcionTest` (5) · `PayloadCodecTest` (5) |
| D-181 | El código pasa por **`fold_gloss`** —minúsculas, espacios, puntuación final— y **no** por `norm()` | Decidido por el usuario con el número sobre la mesa: entre Wikcionario y Wikidata sube **34,40 % → 42,21 % (+1.531 acepciones)**, con el 100 % del derivado intacto. ⚠️ **Es una regla NUESTRA y versionada**, a diferencia de NFC que es un estándar: cambiarla invalida todos los enlaces escritos. **No** saca acentos —`publico` y `público` son palabras distintas—. ⚠️ **NO pasa por `norm()`**, que es el precedente de D-055: atarlo a `NORM_VERSION`, que D-005 permite subir en cualquier momento, rompería cada enlace de cada pack ya construido. ⚠️ **Se pliega también la clave de fusión**, o dos acepciones que difieren en un punto compartirían código sin fusionarse. ⚠️ **El espacio se enumera a mano y NO se usa `\s`**: en Python es Unicode y en Java ASCII, así que un espacio duro divergiría **sin error y sin log** | `PlegadoDeGlosaTest` (8) · `PayloadCodecTest` (2) |
| D-182 | **Toda acepción es direccionable por `(idioma, palabra, acepción)`**, y `verify_pack.py` lo exige | Pedido: *«sin excepciones»*, así que se trató como invariante. Medido antes de cerrarlo: **350 acepciones de 1,7 millones** no lo eran — 211 en el bilingüe, **0 en el núcleo español**. ⚠️ **Ninguna es culpa del hash: los choques entre entradas distintas son 0.** Todas son dos acepciones de la misma entrada con glosa idéntica (`y` → *and*, cinco veces). ⚠️ **Se FUSIONAN y no se descartan, y lo decidió una medición**: de 12 grupos, **5 traían adjuntos distintos** — `them` repite la glosa con **ejemplos diferentes**. `merge_duplicate_senses` vive en `render` porque es el **único** paso por el que pasan todos los packs. El check se probó **mutando un pack a propósito** | `AcepcionDireccionableTest` (5) · `verify_pack` |
| D-183 | `kind` describe **las definiciones**; las capacidades se leen de `translations_to` | El pack español es `MONOLINGUAL` —define en español— y **traduce al inglés**. Mientras `wordActions` filtraba por `kind == BILINGUAL`, la acción *«ver traducción»* **no aparecía nunca** sobre un pack que sí traduce. Son dos preguntas distintas: `kind` contesta *en qué idioma están las definiciones*, `translations_to` contesta *si traduce*. ⚠️ **Y la acción además desaparece cuando la ficha ya muestra las suyas**: existía para ir a buscar la palabra a otro diccionario, y ahora se dibujan inline. Corolario: `trans` deja de ser espacio gastado en un pack monolingüe — pasa a ser el **canal de búsqueda entre idiomas** (cierra la decisión abierta de §Reorientar el esquema) | `WordActionsTest` (2) |
| D-184 | Un enlace es `WordLink(packId, entryId)`, y las flexiones del idioma destino se leen de un **pack ya construido** | Las traducciones forzaron lo primero: un `entryId` suelto sólo era seguro porque quien navegaba usaba el pack de la pantalla, y una traducción va necesariamente a otro. **Refuerza D-080** en vez de relajarlo: el caso de siempre pasa a ser el mismo tipo con el pack propio. La resolución busca **por idioma y no por `pack_id`**, así que el núcleo sirve igual que el completo. Para las flexiones, la fuente es `en-def-wikt.db` y no el dump de 3,2 GB, por el razonamiento de D-175. ⚠️ **Se eligió expandir `trans` (A) sobre la tabla de indirección (B)**, cambiando el precio cotizado: A no toca esquema ni cascada. Medido sobre gemelos: `trans` **5.688 → 12.792**, **1,38 → 1,48 MB (+7,2 %)**, ~+3,6 MB extrapolado. **B queda anotada con su número.** ⚠️ **El filtro no es opcional**: el 38,7 % de `form` del pack inglés son frases y `no table tags`/`glossary` son artefactos; filtrar descarta el 35 % **sin mover la cobertura** | `FlexionesDelDestinoTest` (5) · `ScreensTest` (1) |
| D-185 | El **prior de orden** sale de la frecuencia de uso real y no de la riqueza de página, en **dos bandas disjuntas** | `_rank` contaba acepciones, ejemplos y sobre todo **formas flexionadas** —tope 80 en el perfil `es`, y un verbo español trae hasta 222—, así que premiaba verbos. Medido: Spearman contra la frecuencia real = **−0,250**, donde se esperaría −1. ⚠️ **El daño no estaba donde parecía**: `coverageBand` (D-142) ya defiende `PREFIX` sin confiar en el pack, pero `orderFor` la aplica **sólo a ese peldaño** — `INFLECTED_FORM` y `TRANSLATION` ordenan por `rank` puro y no tienen defensa: `house → solar, alojar, albergar` y nunca `casa`; `water → gastar, regar`; `book → reservar, fichar`. Arreglar el prior es lo único que alcanza a los tres. **No se tocó `orderFor` ni la cascada**: la investigación de autocompletado converge en dos fases —prior de popularidad, después calidad del match— y el repo ya tenía las dos. ⚠️ **Dos bandas y no una escala mezclada** porque sólo el **17,4 %** de los lemas tiene señal: mezclarlas exigiría calibrar cuánta riqueza vale un punto de Zipf, sin medición que lo respalde. Quien no tiene señal cae en bloque debajo y conserva entre pares el orden de riqueza — no aparecer en 50.000 palabras de subtítulos ya es evidencia de rareza. **Medido sobre gemelos** (misma muestra, sólo cambia el flag): rho **−0,169 → −0,678**, **0 bytes**, porque `rank` es una columna que ya existía | `RankPorFrecuenciaTest` (6) · `CanalDeLecturaTest` (1) |
| D-186 | Las frecuencias vienen de **OpenSubtitles + Tatoeba**, combinadas en **escala Zipf**, y la clave de búsqueda **conserva el acento** | Subtítulos porque la literatura de SUBTLEX es consistente en 6+ idiomas: predicen el reconocimiento de palabras mejor que los corpus de libros, porque se parecen al habla — el registro de alguien buscando en un reloj. **CC BY-SA 4.0**, la misma del pack. Zipf porque la distribución es de ley de potencias (`de` 14.459.520 contra 185 de la palabra 50.000): sin logaritmo la primera aplasta todo. Cada lema toma su valor de **una** fuente; promediar ocurrencias contra frases-que-la-contienen sería calibrar dos escalas sin medirlo. ⚠️ **`wordfreq` descartado como librería**: es dependencia pip y `tools/CLAUDE.md` fija *stdlib only* como propiedad deliberada — se usó la idea, no el paquete. ⚠️ **Y el acento NO se pliega, lo cual costó un pack construido descubrir**: con clave `norm()`, `háber` (unidad oscura) heredaba la frecuencia de `haber` (puesto 210) y salía **rank 97** contra 237 de `hábil`; `líbero` sumaba `libero` + `liberó`. Con la clave acentuada caen a 995 y 994 — la banda sin señal, que es la respuesta correcta. ⚠️ **La métrica mintió a favor del bug**: `rho` se mide contra `tatoeba.frequencies`, que también usa claves `norm()`, así que la versión plegada puntúa mejor (−0,735 vs −0,678) por acertar contra una verdad igualmente plegada. **Se eligió el número peor por ser el correcto** | `ListaDeFrecuenciasTest` (7) |
| D-187 | El pack **declara** cómo calculó su `rank` (`meta.rank_basis`), y a igual posición manda el mejor calibrado | Pedido: incluir el orden multiidioma en la optimización. ⚠️ **Medirlo mostró que la fusión ya era ordinal y que el defecto era otro**: `Suggestion.score` es la **posición dentro del propio pack**, no el `rank`, así que la escala se cancela sola y comparar entre packs es round-robin, no raw-score. Lo que eso **no** arregla: un pack mal calibrado pone la palabra equivocada en la **posición 0** y al interlevar pesa igual que uno bien calibrado. Medido: `es-def-wikc` tiene rank **668–1997** y `es-def-wd` **911–997** —mismo idioma, `wikidata.py` tiene su propia fórmula— y nada se lo decía a la app. ⚠️ **Es la pieza que no se puede agregar después sin reconstruir**, por eso entra con este build. La regla es la más débil que sirve —desempata **después** de `score`, no reordena— porque hundir al otro pack perdería sus lemas exclusivos, que son la ganancia de D-136. `RankBasis.fromId` no lanza ante un id desconocido: degrada a `page-richness`, que es lo que todos eran. **Y Zipf resultó comparable entre idiomas**, medido con 20 pares tipo Swadesh: diferencia media **−0,06** puntos y desviación **0,20** (`agua` 5,45 / `water` 5,43) — 14 puntos de rank sobre una banda de 500, o sea ruido. Eso convierte el *raw-score merging* sin medir en *normalized-score merging* justificado | `SearchRepositoryTest` (2, **verificados por mutación**) |
| D-188 | El plegado de caja sigue el estándar `toCaseFold()`, con una **tabla Unicode fijada** que leen los dos lenguajes | ⚠️ **Corrige D-181, que usaba la operación equivocada.** El estándar separa las dos explícitamente: `toLowerCase()` es *case mapping*, para MOSTRAR texto, y `toCaseFold()` —regla R4, §3.13, referenciada por UAX #31— es *case folding*, para COMPARARLO. El código de acepción compara. Medido: **242 de 133.730** code points del repertorio difieren (`ß`→`ss`, `ſ`→`s`, `ς`→`σ`), y sobre las glosas reales 8 de 97.337 en español y 19 de 162.820 en inglés. ⚠️ **Es una TABLA y no una llamada porque Java no tiene `toCaseFold()`**: lo único que lo ofrece es ICU, y **D-003 lo prohíbe** —cada Android trae su versión de Unicode y eso clasificaba 14.773 code points distinto entre relojes. Se fija igual que `UnicodeRepertoire` y por el mismo motivo, con el sha256 atando las dos copias: **297 pares, Unicode 13.0.0**. ⚠️ **El generador verifica su propio supuesto**: el lado Kotlin indexa por `Char` para no usar APIs de la JVM que D-017 prohíbe en `:dict-core`, y aborta si algún día algo pliega fuera del BMP. **Se hizo antes del build a propósito**: no hay enlaces escritos ni packs construidos con el plegado anterior, así que ahora es gratis y después costaría reconstruir todo. Lo que **sigue** siendo regla nuestra es quitar la puntuación final, que ningún estándar hace | `CaseFoldingTest` (7) · `ArchitectureTest` (2) · `PayloadCodecTest` (1, caso por caso contra Python) |
| D-189 | Una búsqueda contesta **sólo con el idioma activo**; el respaldo entre idiomas queda como el modo `FALLBACK`, apagado | ⚠️ **Revierte el defecto que D-172 midió**, a pedido y con el precio dicho: *«quiero que en los resultados filtrados por idioma solo aparezcan resultados de ese idioma, esto va en contra de lo que había decidido antes pero creo que es mejor»*. **El costo es el mismo número que justificaba el respaldo**: de 400 lemas ingleses comunes, **321 (80 %)** lo disparaban con español activo, y esos 321 ahora no aparecen hasta cambiar de idioma. Se acepta porque una lista que mezcla idiomas sin pedirlo es más cara de leer en 234 dp que una lista corta. ⚠️ **Es un `enum` y no un borrado**, y esa es la mitad de la decisión: el umbral de `needsFallback` se midió sobre los packs reales y esa medición no se reconstruye leyendo el código. Como valor, el **modo auto** que el pedido nombra para más adelante —*«pensemos un modo auto para el futuro donde podamos mezclar idiomas»*— es cambiar `STRICT` por `FALLBACK` en un sitio; borrado, habría que volver a derivar el umbral. Los tres tests que documentaban el respaldo siguen vivos declarando el modo | `SearchRepositoryTest` (4) |
| D-190 | La etiqueta de una fila y de una ficha es **el idioma y nunca la fuente**: `ES`, `EN` | ⚠️ **Revierte D-151.** Esa regla mostraba la sigla del `pack_id` —`WIKC`, `ENWIKT`— cuando dos diccionarios compartían idioma, razonando que `ES · ES` no desambigua. El pedido la revierte: *«solo debe ser EN, ES. No me gusta que haya un ENWIK... porque solo me interesa conocer el idioma de proveniencia»*. Desambiguar **dos packs** es una pregunta de catálogo y tiene su pantalla; la fila contesta otra cosa —en qué idioma está la palabra que voy a abrir— y la sigla no se entiende sin conocer el `pack_id`, así que ocupaba el mismo ancho para decir menos. **Lo que se pierde, dicho para que nadie lo redescubra**: con `es-def-wikc` y `es-tr-enwikt` instalados, la fila no dice de cuál salió. ⚠️ **La ficha ahora la muestra también**, que es donde más falta hacía: saltando por una traducción se llega a una entrada **de otro idioma**, y hasta hoy nada lo decía. Sale del mismo mapa que etiqueta las filas para que las dos pantallas no puedan discrepar | `LanguageChipsTest` (3) · `ScreensTest` (2) |
| D-191 | La gestión de diccionarios **no elige** el diccionario en uso: sólo informa y borra | Pedido: *«quitando completamente el ticket de idioma seleccionado y dejando que esto se haga solo desde la pantalla de inicio»*. Elegir idioma vive en el selector del inicio (D-111), así que tenerlo también acá era una segunda puerta a lo mismo **dos niveles más adentro**, y la guía de Wear OS pide jerarquías de a lo sumo dos. ⚠️ **No es sólo simplificar: el hueco del tick era el ancho que faltaba.** La fila reservaba 20 dp para el check —dibujado o no, para que el nombre no se corriera al cambiar— y el detalle salía cortado: se leía `definiciones · 315,9` sin el `MB`, y el tamaño es justo el dato por el que alguien entra a esa pantalla. Quitarlo devuelve **26 dp**; el detalle además pasa a dos líneas, porque con una sola el corte vuelve en cuanto el texto crece —la escala es ajustable hasta 1,15 y la traducción más larga de `kind` no es la de hoy | `ScreensTest` (1, invertido) |
| D-192 | Las traducciones **de la palabra** van **antes** de las acepciones, y los títulos de sección no llevan el color de los enlaces | ⚠️ **Invierte la colocación que este repo había razonado**, y lo desmiente una medición que ya estaba escrita al lado: iban al final porque *«las definiciones son a lo que el lector entró»*, pero el **48,6 %** de las entradas con traducción tienen **sólo** ésas — para la mitad de los casos la sección del final era la respuesta entera, y quedaba debajo de un `Ver más (12)` que hay que tocar. Sólo el **3,0 %** muestra las dos secciones a la vez, así que empujar las acepciones lo paga una entrada de cada treinta. Pedido: *«que la traducción por palabra en caso de estar disponible sin acepciones aparezca al inicio»* y *«que estas secciones individuales tengan la misma relevancia que una acepción»*. ⚠️ **Y el título iba en `primary`, que es exactamente el color con que se pinta una palabra que navega**: el encabezado prometía un toque que nunca existió — *«deben estar más destacados y visibles y no ser clickeables como hipervínculos»*. Ahora se destaca por **peso**, que distingue un encabezado de un enlace sin competir con él. **Lo que no cambia**: una lista dibujada bajo una acepción sigue afirmando que le pertenece, así que el canal de palabra nunca se funde con el de acepción (D-117) | `ScreensTest` (3) |
| D-193 | La palabra del día **no rota categorías** cuando el pack ordena por frecuencia, y prueba **96** candidatos en vez de 32 | ⚠️ **D-185 invalidó las tres mediciones con que `WordOfTheDay.kt` se justificaba**: que `rank` está aplanado (mediana 992 de 997), que mide riqueza de página (D-067) y que por eso ganan los verbos españoles. El síntoma se vio en el reloj: la palabra del día en inglés era **`straitly`**. **Medido, 112 días sobre los packs reales**, días que caen en una palabra de frecuencia conocida: con rotación **79 % (es) y 49 % (en)**; sin ella **99 % y 82 %**. La rotación existía para combatir un sesgo que ya no existe, y pasó a forzar *la mejor palabra de la categoría del día* por encima de una mucho más común de otra — el adverbio es la categoría más escasa, y de ahí `straitly`. ⚠️ **Y el sesgo no vuelve**: sin rotación, español da 58 sustantivos / 20 adjetivos / 18 verbos / 7 adverbios. ⚠️ **No se borra la rotación, se condiciona**: un pack no se actualiza cuando la app se actualiza, y en uno anterior a D-185 —o de `sources/wikidata.py`, que tiene su propia fórmula— el sesgo de verbos es real y es la única defensa. Lo distingue `RankBasis`, la misma clave que ya decide el desempate de `orderFor`. **96 candidatos y no 32 porque el pack inglés lo exige**: sólo el **5,8 %** de sus 956.150 entradas tiene señal, así que con 32 tiradas la probabilidad de que **todas** fallen es 0,942³² = **15 %** — una semana de cada siete. Medido: 32 → 99 %/85 %, 64 → 100 %/98 %, **96 → 100 %/100 %**, y la elección entera del día cuesta **menos de 6 ms**. El defecto del parámetro es `PAGE_RICHNESS`, que era lo que eran **todos** los packs: un sitio de llamada que lo olvide degrada a lo de siempre, no a algo nuevo | `WordOfTheDayTest` (2, **verificado por mutación**) |
| D-194 | Un pack **bilingüe se consulta por sus DOS idiomas**, no sólo por `lang_src` | ⚠️ **Cierra una regresión que D-189 introdujo el mismo día.** Los packs se elegían con `langSource == idiomaActivo`, y `es-tr-enwikt` declara `es`: con **inglés activo** caía en «otros idiomas», que hasta D-189 contestaban como respaldo y desde D-189 no contestan nunca. Resultado: **el único pack con traducciones quedaba invisible justo en la dirección `en → es`**, que es la mitad de su razón de ser — `dog` dejaba de devolver `perro`. ⚠️ **Y los datos siempre estuvieron ahí, en un solo archivo**: 123.979 entradas españolas con glosa inglesa (`es → en`) más **474.849 filas en `trans`** que mapean términos ingleses a esas entradas (`en → es`). Medido: la dirección inversa cubre el **98,4 % de las 1.000 palabras inglesas más frecuentes**, 95,0 % del top 4.000 y 91,0 % del top 8.000 crudo — y de los 721 que faltan en el top 8.000, casi todos son ruido de subtítulos (`didn`, `gonna`, `ooh`, `mm-hmm`) y nombres de pila (`sam`, `danny`); reales sólo `any` y `cannot`. **El pack ya era bidireccional; lo que no lo era era a quién se le preguntaba.** ⚠️ **La regla mira `kind` y no `translationsTo`**, y la diferencia no es cosmética: el pack español monolingüe también declara `translations_to = en` —es una **capacidad**, trae traducciones por acepción— pero sus lemas son españoles, así que incluirlo con inglés activo llenaría de palabras españolas una lista que el usuario acaba de filtrar a inglés, que es exactamente lo que D-189 eliminó. **Lo que sigue sin existir** es una ficha inglesa dentro del bilingüe: `dog` y `book` no son lemas ahí, así que se llega a `perro` y `libro` pero no se lee `dog` — para eso está `en-def-wikt` | `PackSelectionTest` (2, **verificado por mutación**) |
| D-195 | Un pack declara **`meta.langs` como pares** y cada entrada lleva **`entry.lang`**; `schema_version` sube a **4** | Pedido: *«redefinir las traducciones como bidireccionales por construcción, que declares a la par ambos idiomas y no uno como principal, y que todas las tareas y consultas se puedan hacer usando solo ese pack»*. ⚠️ **`lang_src`/`lang_dst` desaparecen**, y no es cosmético: ese par afirmaba que un idioma era el origen y otro el destino, cierto de un pack que traduce en UNA dirección. El bidireccional tiene entradas de los dos —`casa` y `house` en el mismo archivo— y ninguno manda. ⚠️ **`entry.lang` es la ÚNICA columna que obligó a subir `schema_version`, y por eso está sola**: una columna que la app ignora se agrega sin romper nada, una que la app exige no — y sin ésta no hay forma de saber en qué idioma está un lema sin descomprimir su payload. El resto del formato ya era extensible: las etiquetas de payload son aditivas (D-119), las claves de meta se leen con tolerancia, y una columna ignorada es gratis. **Lo escaso no es la extensibilidad: son las oportunidades de reconstruir**, y por eso este bump arrastra todo lo que ya estaba decidido. ⚠️ **`idx_entry_norm` gana `lang` AL FINAL**: adelante daría mejor selectividad pero partiría el rango de `norm` —el 95 % del uso— y dejaría el `ORDER BY rank` sin satisfacer; al final el índice sigue siendo de cobertura y el filtro por idioma no toca la tabla | `verify_pack.py` (4 invariantes nuevas) · `PackSelectionTest` · `test_build.py` |
| D-196 | El lado inverso de un pack bilingüe son **entradas derivadas**, y `trans` se vacía | ⚠️ **Hasta acá el pack era bidireccional para BUSCAR y no para LEER.** Las palabras inglesas vivían sólo en `trans` —un índice de `norm` a entrada española—, así que `dog` encontraba `perro` pero `dog` **no era un lema**: no había ficha que abrir, ni etiqueta de idioma, ni forma de que la app supiera que el pack lo conoce. ⚠️ **Se derivan del mismo volcado ya leído, no de una fuente nueva**, que es el argumento de D-175 para el núcleo: derivar hace la afirmación cierta **por construcción** —si `dog` lleva a `perro` es porque la glosa de `perro` decía `dog`— y cuesta **cero dumps y cero horas** de build. Medido sobre el pack real: **164.249 términos ingleses**, +20,4 MiB de entradas contra **−13,3 MiB** de `trans`, o sea **+7 MiB netos**. El `pos` se hereda del equivalente más común (100 % de cobertura) y el `rank` del mejor, porque `dog` es tan común como `perro`. ⚠️ **`trans` se vacía y no se borra**: sigue existiendo para los packs monolingües, donde la palabra del otro idioma no es un lema —es como `casa` se encuentra escribiendo `house` en un pack que sólo define en español—. ⚠️ **Una entrada inversa NO lleva acepciones y esa ausencia es honesta**: un bilingüe contesta *«cómo se dice»*, no *«qué significa»*; las definiciones inglesas son de `en-def-wikt` y meterlas acá sería fundir dos packs. Va en el canal `W` porque no se puede atribuir a una acepción que no existe (D-117) | `test_source_bilingual.py` (3) · `verify_pack.py` |
| D-197 | El selector del inicio elige un **idioma**, no un pack, y la búsqueda filtra por `entry.lang` | ⚠️ **Es lo que D-147 ya empujaba —*«un chip por idioma, no por archivo»*— llevado hasta el final, y el pack bidireccional lo volvió obligatorio**: elegir `es-tr-enwikt` no dice en cuál de sus dos idiomas buscar. `SearchState` gana `activeLang`; el pack se deriva de él con las mismas reglas de representante. Un pack aporta **un chip por cada idioma que declara**, así que con sólo el bilingüe instalado ya hay `ES` y `EN`. ⚠️ **El filtro va en SQL y no en Kotlin**: `lang` está al final de `idx_entry_norm`, así que se resuelve dentro del índice de cobertura; filtrando después habría que traer el doble de filas para llenar el mismo límite. ⚠️ **Y el plegado tolerante usa el perfil DEL IDIOMA buscado**, no el del pack: plegar una consulta inglesa con reglas españolas —`ce`→`se`, `v`→`b`— daría una clave que no existe en la mitad inglesa. ⚠️ **La etiqueta de fila pasa a ser una sola** porque la lista está filtrada a un idioma, **pero el historial conserva el mapa por pack**: ahí una fila puede venir de un pack desinstalado, y heredarle el idioma activo afirmaría una procedencia que nadie comprobó (familia D-080). Un pack bidireccional no recibe etiqueta en esa lista, porque `Visit` no guarda el idioma y es mejor sin etiqueta que con la equivocada | `LanguageChipsTest` (2) · `ScreensTest` (3) · `SearchViewModelTest` |
| D-198 | El pack declara **`meta.tier`** y **`meta.rank_signal_boundary`**, que sólo esperaban un rebuild | Entran con D-195 porque **el rebuild es el coste, no el cambio**: agregarlas después costaría otra reconstrucción de 372 MB. `tier` = `core`\|`full` lo recomendaba el roadmap sobre inferir del nombre que termine en `-core`, que es adivinar del nombre lo que D-138 decidió que se declara; `subset_of` contesta *«soy parte de ESE»* y `tier` *«soy un núcleo»*, que es lo que hace falta sin conocer al otro. `rank_signal_boundary` publica el `500` que hasta hoy sólo vivía en `kaikki.py`: `rank` son dos bandas disjuntas (D-185) y la app necesita el corte —la palabra del día lo usa (D-193)—, así que la alternativa era copiarlo en Kotlin y **crear un tercer contrato cruzado** que rompe en silencio, como `norm()` y `sense_code`. ⚠️ **Lo que este bump NO salda**: `detail=none` y `columnsize=0` en `fts_def` siguen sin medir, y cambiarlos exigirá **otra** reconstrucción. No entran porque matan la búsqueda de frases y medirlo pide packs gemelos — cambiarlos sin medir es justo lo que `CLAUDE.md` prohíbe | `verify_pack.py` (2) |
| D-199 | Resolver un término mira **el idioma**, no sólo el `rank` | ⚠️ **Bug reportado por el usuario, y sistémico.** *«Cuando presiono adiós en traducciones de bye me lleva a otra palabra en inglés»*. `resolveHeadwords` elegía por mejor `rank` sin mirar el idioma, lo que era **correcto mientras un pack tuviera uno solo**: al volverlo bidireccional, «resolver en el mismo pack» dejó de implicar «en el mismo idioma» — y se rompió en silencio. `pie` es español (parte del cuerpo) **e** inglés (pastel), así que tocar la traducción `pie` de `foot` abría el `pie` inglés: una traducción que devuelve al idioma del que uno venía. **Medido sobre el pack real: 7.757 de 93.473 traducciones de entradas inglesas (8,30 %) resolvían al idioma equivocado.** ⚠️ **La regla no es «el otro idioma» sino de dónde salió el término** ([TermSource]): las palabras de la **glosa** están en el idioma de la entrada, y también los **sinónimos, antónimos y relacionadas** —un sinónimo de `casa` es español—; sólo las **traducciones** están en el otro. Meterlas todas en la misma bolsa habría roto los sinónimos, que es el mismo bug al revés. Son **tres** consultas y no dos porque `MAX_PALABRAS_POR_CONSULTA` es 64 y las glosas ya promedian 39,9 claves en español y 58,8 en inglés: juntarlas desbordaría y **recortaría en silencio** | `SqlitePackSourceTest` (1, **verificado por mutación**, con un homógrafo cruzado nuevo en el toy) |
| D-200 | Un pack de **traducción no genera palabra del día**, y el representante se elige **entre los de definiciones** | Pedido: *«los packs de traducción no generan palabras del día, esto queda solo para los diccionarios de definiciones»*. La razón se ve al abrirla: una entrada inversa **no tiene acepciones** (D-196), así que la palabra del día abriría una ficha que dice *«se dice `perro`»* y nada más — y una palabra del día existe para **aprender algo**. ⚠️ **Dos reglas correctas por separado borraron la sección entera**, y sólo se vio en el emulador: el representante de cada idioma es el pack **más grande**, y el bilingüe pasó a serlo de los dos (209.484 contra 152.281 del español y 16.652 del núcleo inglés), así que la pantalla pedía la palabra de un pack que —correctamente— no genera ninguna. Se filtra **antes** de elegir representante, no después. ⚠️ Y el primer test que escribí para esto era **vacuo**: con el pack de definiciones activo el fallo no aparece, porque `representativePacks` respeta al activo. Lo agarró la mutación | `ScreensTest` (1, **verificado por mutación**) |
| D-201 | Al **salir de la app** se vuelve al inicio limpio, sin matar el proceso | Pedido: *«que no quede en segundo plano sino que se cierre y guarde todo para que la siguiente vez te lleve a la pantalla de inicio limpia»*. Un reloj no se cierra, se baja la muñeca: volver tres horas después a la ficha de `esdrújula` con `esdrú` escrito no es retomar nada, es encontrarse con la pantalla de otro momento. ⚠️ **No se termina la Activity, y eso es deliberado**: reabrir los packs cuesta **500 ms en frío contra 278 tibio** con 372,6 MB abiertos (⚠️ build de release con R8; el de debug, sin AOT, da 1401–2203 ms — ver `docs/bateria.md`), para ahorrar una memoria que el sistema ya sabe reclamar solo. Se tira el **estado de pantalla**, que es lo que molesta; se conservan los descriptores, que es lo que cuesta. ⚠️ **Se engancha a `ON_STOP` y no a `ON_PAUSE`**, y además se marca cuándo lo causó el **input del sistema**: `ACTION_REMOTE_INPUT` es una Activity de SysUI a pantalla completa, así que dictar una palabra produce el mismo evento que irse — sin la marca, se borraría la pantalla a la que se vuelve con el resultado | `SearchViewModelTest` |
| D-202 | El menú de la ficha pierde *Ver traducción* y gana un **selector de tamaño de 3 botones** | *«El botón view translation no hace nada y es redundante»*, y las dos mitades eran ciertas: resolvía el lema en el OTRO pack y navegaba **sólo si existía ahí** —`casa` no es lema del diccionario inglés— así que no hacía nada, sin error ni mensaje; y desde D-179 las traducciones se dibujan **dentro** de la ficha, y desde D-196 la palabra del otro idioma es una entrada del mismo pack. Se borra en vez de arreglarse porque el caso que quedaba —el mismo lema en otro diccionario— ya lo cubre un toque en el chip de idioma. ⚠️ **En su lugar va el tamaño de texto, y el sitio importa**: que la letra sea chica se descubre **leyendo una definición**, no navegando ajustes, y pedir que el lector salga de la palabra y cruce dos pantallas es el viaje que la guía de Wear OS pide evitar. **Los tres botones muestran el tamaño que aplican** en vez de nombrarlo: se entienden sin leer, que en un reloj vale más que una etiqueta, y además no hay que traducirlos | `WordActionsTest` · `ScreensTest` |
| D-203 | Lo que un **tile** muestra se lo deja escrito la app, y las etiquetas salen de las **mismas funciones** | ⚠️ **Dos instancias más de la clase que D-200 abrió**, encontradas barriendo a propósito: *«una regla que vale en una superficie y no en su paralela»*. **(a)** La app filtra el historial a packs instalados (`visibleOnes`) y el tile leía `PackStore.history()` **en crudo**: mostraba una palabra de un diccionario borrado que al tocarla no abre nada. **(b)** La fila del tile decía sólo `perro` donde el inicio dice `perro · sust.`, y no por densidad sino porque no compartía la función — la guía de tiles pide **no mostrar menos información de la que cabe**. ⚠️ **El historial del tile va por una clave aparte y no reemplaza al completo**: la app *esconde* las visitas de un pack desinstalado pero las **conserva**, y reinstalarlo las devuelve. El tile no puede filtrar solo —no sabe qué packs hay sin abrirlos, y abrir un pack en un tile está prohibido (D-106)— así que lee una lista ya resuelta, el mismo patrón que la semana de palabras del día. ⚠️ **Lo que sigue sin compartirse, y es correcto**: la etiqueta de idioma. Sale del idioma activo, que un tile no conoce, y `Visit` no lo guarda — inventarlo sería afirmar una procedencia que nadie comprobó (familia D-080) | `SearchViewModelTest` (2) |
| D-204 | A igual peldaño, un lema **con frecuencia conocida** va antes que un fragmento corto | ⚠️ **Corrige un defecto medido en inglés que llevaba sin evaluar desde el 2026-09-17.** La posición media de la palabra obvia era **5,1** sobre una primera pantalla de **tres filas**: `hous` devolvía `Hous.` —una abreviatura— primero, `tim` ponía `TIM, Tim, TIM, Tim` antes de `time`, `beaut` dejaba `beautiful` en la 12. ⚠️ **La causa no es `rank` sino `coverageBand` (D-142), que premia los lemas CORTOS**: teclear `wat` cubre `wat` al 100 % y `water` al 60 %, así que el fragmento gana la banda sin importar la frecuencia. En español apenas muerde —pocos fragmentos de tres letras son lema— y el Wiktionary inglés está lleno: interjecciones, siglas, formas ligadas (`-boo`). Y `demoteProperNoun` está desactivado justo en la banda 0, que es donde viven. ⚠️ **Es un BOOLEANO en `Suggestion` y no el `rank`, y esa forma es la decisión**: el rank crudo no es comparable entre packs (D-187), así que exponerlo invitaría a la comparación que no vale. Cada pack resuelve la señal contra **su** `meta.rank_signal_boundary` (D-198) y lo que cruza la frontera es la respuesta. ⚠️ **Va antes de la banda y sólo en `PREFIX`**: después no serviría, la banda ya habría puesto el fragmento arriba. **Medido: 5,1 → 3,6.** ⚠️ **Y no resuelve el fondo**, que conviene saberlo: `wat`, `boo` y `beaut` **también** tienen señal —son tokens reales de subtítulos— así que siguen delante. Es un desempate, no una cura. **Coste cero en consultas**: el peldaño de prefijo ya sale del covering index, que incluye `rank` | `SearchRepositoryTest` (2, **verificado por mutación**) |
| D-205 | La palabra del día del tile muestra **su primera acepción**, y `Visit` gana un campo opcional | `futuro · sust.` no enseña nada; la glosa es lo que vuelve útil una palabra del día de un vistazo. ⚠️ **La glosa la lee la APP, no el tile**: abrir una entrada descomprime su payload y `onTileRequest` es `@MainThread` con diez segundos (D-106). Son **siete lecturas una vez al día**, en el mismo sitio donde ya se calcula la semana. ⚠️ **El parser exigía EXACTAMENTE cuatro campos**, así que agregar uno habría hecho que la app **rechazara sus propios registros nuevos**; pasó a `< 4` y una preferencia vieja se sigue leyendo. Es un contrato con el disco: lo guardado en un reloj no se migra, se tolera. ⚠️ Y la glosa se limpia de `\n` al serializar, porque ese carácter separa **registros**: una glosa con salto partiría la lista y la segunda mitad se descartaría en silencio — el mismo cuidado que `payload.sanitize()` tiene con el tab | `VisitTest` (3) |
| D-206 | El `bottomSlot` de los tiles lleva **buscar**, y `--fix` corrige los conteos de los documentos | **(a)** Un tile no acepta texto (D-026) **pero sí lanza un intent**, y el `bottomSlot` estaba vacío: es exactamente donde la guía de Wear OS pone la acción. Buscar desde el carrusel pasa de **tres toques a uno**. El extra lo lee `MainActivity` una vez, del intent de arranque, y `SearchScreen` abre el input con un `LaunchedEffect(Unit)` — con `Unit` a propósito: sin eso, volver de una ficha reabriría el dictado sin salida. **(b)** `audit_dictionary.py --fix` reescribe los conteos que encuentra mal. ⚠️ **Sólo el NÚMERO**: que un documento haya *dejado de afirmar* un conteo sigue siendo falla humana, porque arreglarlo solo inventaría una frase o **borraría una vigilancia**. La fricción estaba medida —cinco fallas en una sesión, cinco más en otra— y en el primer uso corrigió **cuatro archivos en un comando** | verificado rompiendo un conteo a propósito |
| D-207 | **Se retira el presupuesto de 50 MB por pack** y lo reemplazan dos límites que sí duelen | Pedido: *«elimina la restricción porque hace rato ya la rompimos, reemplacémosla por una que haga más sentido»*. Y tenía razón: el pack español la pasó **desde el primer build** (72,2 MB contra 50) y **nunca hizo cambiar una decisión** — se citó nueve veces para anotar que se incumplía. Un presupuesto que sólo se usa para declararse roto no es un presupuesto. ⚠️ **Además medía lo que no duele.** El reloj del proyecto reporta **9,0 GiB libres en `/data`**; los cinco packs juntos son 478 MB, el **5 %**. El disco no es el recurso escaso. **Lo que lo reemplaza, atado a mediciones que este repo ya tiene:** **(1) el arranque en frío** — 500 ms con 372,6 MB abiertos (O-1) — ⚠️ **cifra de un build de release con R8 y dos packs**; re-medirla sobre release sigue abierto; un pack nuevo no puede empeorarlo sin que se mida, porque eso sí lo siente quien levanta la muñeca. **(2) el núcleo dentro del APK** — los dos suman **17,1 MiB** de los 66 del APK, y ése sí es un límite duro: lo que viaja en el APK se descarga aunque no se use. ⚠️ **Lo que NO se limita**: los bytes de un pack completo, porque llegan por descarga diferida a carga y Wi-Fi (D-029) y el usuario elige instalarlos. La tensión *«que vayan completas»* contra *«50 MB»* se resuelve así: las completas van completas, y lo que se vigila es lo que se paga sin pedirlo | `verify_pack.py` mide y reporta; el arranque, O-1 |
| D-208 | El rediseño a **234 dp se descarta**: se diseña contra la resolución estándar | Pedido: *«ya se ve bien en el tamaño del reloj y me interesa pensar en la resolución estándar»*. ⚠️ **Esto cierra un ítem que llevaba abierto desde el 2026-09-19** —cinco decisiones cotizadas contra 192 dp con la medición ya hecha y el rediseño sin empezar— y lo cierra **sin trabajo**, que es el mejor resultado posible. **Lo que se conserva y por qué**: `rowsThatFit(screenWidthDp)` sigue siendo genérico, así que la app se adapta sola; lo que se descarta es **rediseñar para aprovechar los 42 dp extra** del reloj del proyecto. Diseñar contra el estándar y adaptarse hacia arriba es más barato que al revés, y los dos tests de densidad —192 dp compone 2 filas, 234 compone 3— siguen protegiendo **la relación**, que es lo que importa | `RowsThatFitTest` · `ScreensTest` (2) |
| D-209 | Lo que se guarda de una visita es el **`uid`**, no sólo el `entryId`, y los ajustes se respaldan como cualquier ajuste | Pedido: *«que los ajustes de la app se guarden donde se suelen guardar los ajustes. La idea es que los packs sean inmutables idealmente»*. ⚠️ **El defecto no era dónde se guardan sino QUÉ**: `Visit` guarda `entry_id`, que `schema.sql` declara **identidad física** y que **no sobrevive a reconstruir el pack** — una palabra nueva en el medio corre todos los ids siguientes. Restaurar un backup, o reconstruir un pack (rutina con `devpack.py`), deja el historial y las hasta 100 guardadas apuntando a **otras palabras, en silencio**. ⚠️ **`entry.uid` es exactamente la pieza que faltaba**: identidad **lógica**, estable entre reconstrucciones, ya en el formato desde D-055 y ya expuesta en `Entry`. Guardarlo al lado del `entryId` hace que un backup restaurado resuelva por `uid` y caiga al `entryId` sólo si el pack es anterior. ⚠️ **Y así los packs quedan inmutables de verdad**: hoy `fixEntryId` **reescribe las preferencias** cuando detecta que una visita se movió — un parche que sólo funciona si el lema sigue existiendo con el mismo texto. Con `uid` no hay nada que parchear. **No implementado**: toca el formato en disco de `Visit` por segunda vez en el día y merece su propio corte | pendiente |
| D-210 | El idioma del dictado **se deja como está**, y las opciones quedan escritas | Pedido: *«averigua opciones de cómo resolver esto pero tampoco es tanto problema»*. **Verificado en la documentación oficial**: `ACTION_REMOTE_INPUT` —el selector del sistema, que es lo que la app usa desde D-127— **no admite ningún parámetro de idioma**; dicta en el del reloj y no hay extra documentado. `RecognizerIntent.ACTION_RECOGNIZE_SPEECH` **sí** acepta `EXTRA_LANGUAGE`, y es lo que la app usaba antes: se abandonó porque abre **sólo el reconocedor de Google**, que puede no estar instalado. **Las tres opciones, con su precio**: **(a)** dejarlo — la etiqueta ya nombra el diccionario, que es lo único que le dice al usuario en qué busca; **(b)** ofrecer `RecognizerIntent` con `EXTRA_LANGUAGE` **además**, comprobando con `queryIntentActivities` que exista, y caer al selector del sistema si no — cuesta una rama y un botón más en una pantalla que pelea por filas; **(c)** no hacer nada y apoyarse en el peldaño tolerante, que **existe por esto**: el dictado produce entrada que no coincide con ningún lema exacto. ⚠️ **Se elige (a)+(c)**, que es lo que ya hay: el costo de (b) es una fila permanente para un caso que el peldaño difuso ya absorbe | — |
| D-211 | Un pack **núcleo tampoco genera palabra del día**, y la regla vive en **un solo predicado** | ⚠️ **Visto en el emulador con sólo los núcleos del APK instalados: `my` y `un`.** Un núcleo son las **8.000 palabras más frecuentes** y D-193 elige la de mejor rank — o sea *la más común de las más comunes*, que es siempre una palabra funcional. Una palabra del día existe para **enseñar algo**, y `un` no enseña nada. ⚠️ **Se pregunta por `meta.tier`, que el pack DECLARA (D-198)**, y nunca por el nombre ni por el conteo de entradas: es exactamente para lo que `tier` se agregó —un pack ajeno puede llamarse como quiera, y lo único que la app puede creer es lo que el artefacto declara y `verify_pack.py` comprueba—. Inferirlo de que el `pack_id` termine en `-core` sería adivinar del nombre lo que D-138 decidió que se declara. ⚠️ **Y la regla pasó a vivir en `givesWordOfTheDay`, una sola vez.** Estaba escrita en tres sitios —la pantalla, la caché del tile y el cálculo— y ya había divergido una vez: valía en la pantalla y no en el tile (D-203). Tenerla duplicada es exactamente cómo vuelve a pasar. ⚠️ **La consecuencia, escrita para que nadie la redescubra**: una instalación recién hecha lleva sólo los núcleos, así que **no muestra palabra del día hasta instalar un diccionario completo**. Es la degradación correcta. **Verificado en el emulador con los cinco packs**: `skipper` y `multa`, de los completos | `SearchViewModelTest` (1, **verificado por mutación**) |

| D-212 | La app **se observa por un solo tag**, `Dict`, y el detalle se enciende con `setprop` | Pedido: *«ayúdame a mejorar los logs por toda la app para facilitar el debug por adb»*. ⚠️ **El punto de partida era cero: ni un `Log` en toda la app**, y por eso la sesión de reloj del 2026-09-21 sólo pudo mirar `dumpsys` e **inferir** «ningún pack fue rechazado» de que no hubo crash. ⚠️ **Es `Log.isLoggable` y NO `BuildConfig.DEBUG`, y la diferencia es el motivo de la decisión**: R8 borra las llamadas guardadas por `BuildConfig` en `release`, y `benchmark` hereda de `release` — o sea que **la única build con la que se puede medir arranque y batería sería la única sin logs**. Con `setprop log.tag.Dict DEBUG` se enciende igual en las tres, sin recompilar ni reinstalar (y sin re-empujar 444 MB de packs). ⚠️ **Los mensajes son lambdas y eso es contrato, no estilo**: `d("x=" + x)` construye el String en cada pulsación aunque nadie lea. ⚠️ **Y `:dict-core` no puede loguear** — `ArchitectureTest` le prohíbe `import java.*` y `System.` (D-017) — así que la cascada **reporta** por `SearchTrace`, en Kotlin puro, sin medir tiempo, y `:app` lo traduce: **el tiempo lo mide siempre quien llama**. Lo primero que esto hizo visible fue un defecto de observabilidad y no de comportamiento: `recolectar` se traga un pack que revienta en cada consulta, y su comentario afirmaba que tal pack *«es visible»* — lo es en la pantalla; en `logcat` era idéntico a uno vacío | `DictLogTest` (8, **verificado por mutación** en los dos sentidos) y `SearchRepositoryTest` (3, ídem) |

| D-213 | La promesa pasa de **«100 % offline»** a **«buscar no usa red, nunca»**, y `INTERNET` se declara | Pedido: un servidor de desarrollo para probar la descarga de packs. ⚠️ **Lo atrapó el propio audit**, que tenía una regla prohibiendo los permisos de red *«esa frase es la primera línea del README y de CLAUDE.md»* — y exigía actualizar los dos documentos **y la regla** en el mismo commit. Tenía razón: declarar `INTERNET` contradice la frase tal como estaba escrita. ⚠️ **Pero la propiedad que este proyecto vende nunca fue «no hay red»: es «buscar no usa red»**, y esa distinción es la que se pierde en un commit si nadie la comprueba. Un diccionario cuyo pack ya está en disco responde sin tocar la red jamás; lo que la usa es **instalar un diccionario, y nada más**. ⚠️ **La regla no se borró, se rediseñó**: `ACCESS_NETWORK_STATE` y `ACCESS_WIFI_STATE` siguen siendo una falla —las restricciones de D-029 las impone WorkManager, y un permiso para mirar la red es una invitación a decidir cuándo descargar desde la app, que es justo lo que D-029 sacó de ahí— y `INTERNET` se permite **sólo mientras los dos documentos sigan diciendo qué es lo único que lo usa**. Borrar la frase falla el gate. **Verificado con dos sondas**: quitar la frase del README falla, y declarar `ACCESS_NETWORK_STATE` falla. ⚠️ **Y el HTTP en claro vive en el source set `debug` y no en `main`**: en `main` viajaría al APK de release y degradaría la app publicada para siempre por una comodidad de desarrollo. Consecuencia medida y escrita: `benchmark` hereda de `release`, así que **no puede hablar con un servidor `http://`** | `audit_dictionary.py` (dos reglas, **verificadas por sonda**) |

| D-214 | **Descargar un pack funciona**: dos etapas, dos hashes, reanudación, y WorkManager con las restricciones de D-029 | Pedido: *«sigue probando hasta que logres descargar un pack de idioma desde el servidor»*. ⚠️ **Los dos hashes contestan preguntas distintas y hacen falta los dos.** El del `.gz` dice *«llegaron los bytes publicados»*, y es el único que puede decirlo: **una reanudación mal calculada produce un archivo de la longitud correcta con un agujero dentro**. El del `.db` dice *«inflar produjo el pack publicado»*, y es el que `installAtomically` compara **antes de renombrar** (D-165) — un `.db` truncado sigue siendo SQLite válido y abre sin una queja. ⚠️ **La reanudación existe porque el inglés son 192 MB comprimidos**: una caída de Wi-Fi al 90 % sin `Range` es empezar de cero, que es justo por lo que D-040 eligió `HttpURLConnection`. El `.gz.part` se conserva entre intentos a propósito; un servidor que contesta 200 a un `Range` manda el archivo entero, así que añadirlo encima lo duplicaría — ese caso tiene su test. Un `.gz` que no cuadra **borra el parcial**, porque conservarlo hace que cada reintento reanude sobre bytes malos y falle para siempre. ⚠️ **Las restricciones de D-029 están fijadas por un test**, porque relajarlas es exactamente el cambio que se hace *«para probar más rápido»* y no se revierte. ⚠️ **Y la descarga creó un downgrade silencioso que hubo que cerrar**: `es-core.db` viene en el APK, actualizarlo desde el catálogo reescribe ese archivo, y la regla era que una versión nueva de la app re-extrae sus packs — lo que pisaría el nuevo con el viejo **sin un error**, porque el viejo abre igual de bien. La regla ahora tiene un tercer caso: **el catálogo gana sobre el APK**; lo incluido existe para arrancar de cero. **Verificado en el emulador**, con el `sha256` del archivo instalado idéntico al publicado | `PackDownloaderTest` (8), `DownloadPackWorkerTest` (6), `PackStoreTest` (2), `SearchViewModelTest` (4) — **todos verificados por mutación** |

| D-215 | **Tres tamaños por idioma —`core`, `main`, `full`— marcados en el nombre, y la identidad pasa a ser idioma + nivel** | Pedido: *«los packs son por idioma y en versiones… 3 tamaños, core, main y full, y que esto sí vaya marcado en el nombre»*, con presupuestos de **25–50 MB** para `core` y **100–150 MB** para `main`. ⚠️ **El criterio es `rank`, y eso está medido, no elegido**: desde que `rank` es frecuencia de uso en escala Zipf, ordenar por él es ordenar por importancia. Sobre el pack inglés real, un presupuesto de 50 MB toma **59.503 entradas** y las que tienen señal de frecuencia (`rank < 500`) son **55.903** — *«las palabras importantes y de uso general»* y *«las que algún corpus atestigua»* resultan el mismo conjunto. ⚠️ **Y la medición descartó el corte alternativo**: los nombres propios son el **21,2 %** de las entradas del pack español y sólo el **6,1 % de los bytes** (20 B de media contra 84 B). Quitar clases de palabras no hace un tamaño. ⚠️ **El presupuesto se estima escalando los payloads** por la proporción del pack de origen —el español pesa 7,2× sus payloads porque lo domina `form`, el inglés 3,8× porque lo domina `entry`— así que el archivo real puede pasarse y **la herramienta avisa** en vez de callarlo. Producidos y verificados con `verify_pack.py`, cero fallas: `es-core` 49.298 entradas / **49,0 MB**, `en-core` 59.875 / **33,1 MB**, `en-main` 277.912 / **120,1 MB**. ⚠️ **El español no tiene `main`**: su completo son 73,6 MB, ya dentro de ese presupuesto, así que un `main` español sería un segundo pack con el mismo contenido. ⚠️ **La identidad pasa de las fuentes al idioma+nivel** (`es-def-wikc-tat-freq-wn-wd` → `es-full`), porque atarla a las fuentes hace que **añadir una fuente cambie el `pack_id`** y el pack parezca otro que la app nunca vio; las fuentes siguen enteras en `meta.sources` (D-138). **El costo, nombrado**: dos variantes del mismo idioma ya no pueden convivir instaladas — era una función deliberada y se retira. ⚠️ **El bilingüe queda fuera de los niveles**, por `kind` y no por una lista: *«su propósito es distinto»*, no es un tamaño del mismo diccionario | `test_core.py` (4 nuevos, **verificados por mutación** — uno era vacuo y lo delató la mutación), `test_segunda_fuente.py` (2 reescritos), `verify_pack.py` |

## Higiene del repositorio

| # | Decisión | Por qué | Enforced in |
|---|---|---|---|
| D-020 | El pack de juguete **no** se commitea | Es determinista y se regenera; commitearlo metería un binario que cambia en cada build por `meta.built_at` | `.gitignore` |
| D-021 | `gradle-wrapper.jar` **sí** se commitea | La regla `*.jar` lo excluía y un clone no podía correr `./gradlew`. Es el punto del wrapper | `.gitignore` (excepción explícita) |
| D-022 | `local.properties` no se trackea | Contiene la ruta absoluta al SDK de una máquina concreta | `.gitignore` |
| D-035 | Cada commit queda verde por sí solo; se parten por dependencia, no por tamaño | Un historial no bisecable no sirve para encontrar cuándo se rompió algo | `commit` skill (verificación por worktree) |
| D-054 | `.idea/` no se trackea | Android Studio lo reescribe en cada sync y `workspace.xml` guarda estado de máquina. El estilo de código no se pierde: vive en `.editorconfig`, que es portable | `.gitignore` |

## El método de trabajo con agentes

| # | Decisión | Por qué | Enforced in |
|---|---|---|---|
| D-059 | El método vive en `.agents/` (D-221), versionado por **tres** headers —método, conocimiento y bundle, cada uno con su digest sobre su propio contenido—, y **una lineage tiene un solo writer**: editar el bundle acá **es forkear**, no bumpear la versión | Dos repos en la misma lineage que mejoran el método y suben ambos a la misma versión producen dos versiones distintas con el mismo número, y la comparación ingenua concluye "idénticas" y **descarta un lado en silencio**. Forkear cuesta seis caracteres hex | `audit_dictionary.py` → `check_bundle_digests` |
| D-060 | Este repo es **single-agent a propósito**: no hay `.cursor/`, ni `.github/copilot-instructions.md`, ni `AGENTS.md` | Dos archivos de reglas que *pueden* discrepar *van* a discrepar, y nadie lo nota porque nadie lee más de uno. La asimetría que el método señala ya es la política de acá: el invariante central lo sostienen el hook, los vectores compartidos y el gate — rungs 3 y 4 — no la prosa de ningún agente. **Se reabre** el día que aparezca el archivo de reglas de un segundo agente | — *(política; se reabre por evento)* |
| D-061 | Este repo reconoce **cuatro capas de evaluación** y nombra cuál falta: gate (`./gradlew check`), invariant test (los vectores compartidos), **pre-ship check (la comprobación en reloj físico — NO EXISTE)** y mirar el output | Tener una capa y creer que se tienen cuatro es la forma normal de esta falla. Nombrar la que falta es lo que impide que un gate verde se lea como "funciona" | `docs/roadmap.md` §Comprobación que falta; `:dict-data:devicePrecheck` |
| D-062 | La fricción se registra en el changelog al primer golpe y **sube a `docs/roadmap.md` §Proceso y herramientas al segundo**, con la aritmética; las mejoras de proceso se **proponen, no se ejecutan** | Cada instancia de fricción es individualmente trivial —nadie nota los treinta segundos— y la aritmética es lo que arma el caso. Reescribir el workflow a mitad de tarea le saca al humano el modelo mental justo mientras lo está usando | `CLAUDE.md` §Cómo corre una sesión; `state-review` skill |
| D-221 | El método deja de ser cuatro archivos sueltos en *docs/agents/* y pasa a ser un **bundle completo en `.agents/`**: los 7 prompts, la base de conocimiento de 45 notas, `tracking/`, `bundle.py` y el `incoming/` por donde llega el próximo. *docs/agents/* se retira y sus referencias se re-apuntan en el mismo cambio | La carpeta vieja era v7 de la lineage `m-7c41a9`; la nueva es v21 de `m-351cc8`, cuya ancestry **contiene a la nuestra como prefijo** —un update, no un merge— y los digests de las dos copias verifican contra su propio contenido. La pérdida se midió antes de borrar, sección por sección: **174 secciones en v7 contra 207 en v21, ninguna sin contraparte**, y los 19 principios y 10 artefactos siguen numerados igual. ⚠️ **Lo que obligó a tocar el enforcer es el modo de falla, no la mudanza**: `check_method_digest` hacía `return` si la carpeta no estaba, así que borrar esa carpeta lo habría dejado verde sin comprobar nada — el gate sigue en 0 fallas y la cobertura cae a cero. Ahora la ausencia **falla**, y los tres digests se comprueban en vez de uno: editar cualquiera de las 45 notas era invisible para la versión vieja | `audit_dictionary.py` → `check_bundle_digests` (3 digests + ausencia) |
| D-222 | Una regla que **sale** de `CLAUDE.md` por presupuesto tiene que quedar **alcanzable**: `CLAUDE.md` conserva el invariante en una línea y el puntero, la skill que la recibe amplía su `description` a las situaciones que la regla gobierna, y **ninguna skill queda sin que alguien la nombre** | `CLAUDE.md` se paga en cada request y vive bajo 200 líneas, así que mudar reglas es inevitable. Pero una skill **no se carga sola**: se carga cuando su `description` matchea lo que el usuario dijo. Mudar *«el diff sin commitear es el trabajo»* a la skill `verify` con su `description` vieja —*«corré el gate»*— la hubiera dejado sin leerse justo antes de un `restore`, que es la única situación donde importa. ⚠️ **El chequeo encontró dos huérfanas en su primera corrida**: `commit` y `troubleshoot-diccionario` no estaban nombradas en ningún lado. No comprueba que la `description` dispare —eso no es decidible— y por eso lo que queda es que el mapa las nombre | `audit_dictionary.py` → `check_skills_reachable` (puntero roto + skill huérfana) |
| D-223 | En `.claude/settings.json`, las rutas se deniegan con **`Edit(...)` y `Read(...)` únicamente**. Una regla `Write(ruta)` se saca, no se agrega | Se acepta, **nunca se consulta** y se avisa al arrancar: protege nada mientras parece que protege, que es la forma exacta de *«se lee como rung 3 y se comporta como rung 0»*. Las 4 reglas `Write(...)` de este repo duplicaban 4 `Edit(...)` que sí corren, así que los cuatro archivos generados siguen protegidos y lo que se fue es el decorado. **ASSUNCIÓN**: sale del método (v8, medido corriéndolo sobre otros repos), no de una fuente primaria verificada acá. ⚠️ **Queda un hueco nombrado**: un `Write` fresco sobre esas rutas no lo bloquea nada; cerrarlo pide un hook `PreToolUse`, propuesto en el roadmap y no construido | `.claude/settings.json` → `permissions.deny`, 4 reglas `Edit()` |
| D-224 | El aviso de *decisiones sin enforcer* **excluye la sección de descartadas** | Esa tabla tiene tres columnas —número, qué se descartó, por qué—, así que la «última columna» que ve el regex es el **por qué**, que nunca empieza con raya: las 7 filas se contaban como reglas **con** enforcer y engordaban el denominador. Medido: decía **19 de 221** y son **19 de 214**. Una alternativa descartada no es una regla y no tiene nada que hacer cumplir | `audit_dictionary.py` → `check_rules_without_enforcer` |

| D-225 | El **`versionCode` entra en la huella** del memo de verificación: instalar una app nueva lo caduca entero | Pedido: *«que instalar una nueva versión de la app invalide los resultados guardados de un pack verificado»*. ⚠️ **Cierra la vía que dependía de que alguien se acordara.** De las cuatro partes que tenía la huella (D-164, D-217), tres las obligan a subir D-005 y D-006; la cuarta, `CHECKS_VERSION`, no la obliga nadie — agregar una invariante a `PackFile.open` no toca ninguna de las tres, y olvidarse deja a **todo pack ya anotado saltándose la comprobación nueva para siempre**, que es exactamente el agujero que la comprobación venía a tapar. El `versionCode` no se puede olvidar: el instalador de Android rechaza un downgrade (D-095), así que una app que llega al reloj trae un número mayor. ⚠️ **Y lo que más importa es que caduca los RECHAZOS**: un *sí* cacheado de más cuesta que se use un pack malo; un **no** cacheado de más cuesta que un pack bueno desaparezca sin log, porque el archivo no cambia y nada lo vuelve a mirar. Una app nueva es justo el momento en que un rechazo pudo dejar de ser cierto. **Lo que cuesta**: el primer arranque tras cada actualización paga la muestra de 64 claves de D-142 — medido en D-164, **41,33 ms contra 6,30** con los dos packs reales, una vez por actualización y no una vez por arranque | `PackVerificationTest` (2 nuevos, **verificados por mutación**: sacar `.a$appVersion` tira los dos y sólo esos) |
| D-226 | Entre el núcleo del APK y el que el usuario bajó del catálogo, **gana el `data_version` mayor** — no gana siempre el catálogo | Pedido: *«verificar que los packs core se autoactualicen al instalar una nueva versión de la app si es que tienen una actualización disponible»*. ⚠️ **D-214 había cerrado un downgrade y abierto el simétrico.** Su regla era *«el catálogo gana sobre el APK»*, escrita para que una app nueva no pisara con su núcleo viejo el que el usuario acababa de bajar. El otro lado nunca se miró: un núcleo bajado hace meses le gana **para siempre** al que trae el APK de hoy, aunque el de hoy sea más nuevo. Los dos packs abren bien, así que **ninguno de los dos casos falla con un error** — lo único observable es un diccionario que no se actualiza nunca. La regla nueva cierra los dos con una sola comparación, y usa el mismo entero monótono con el que `Catalog.classify` decide si hay novedad (D-170), así que la app tiene **una** noción de *más nuevo* y no dos. ⚠️ **Lo que cuesta, dicho entero: hay que EXTRAER el asset para poder preguntarle.** SQLite no abre un `.db` dentro del APK (D-173, verificado con `javap`), así que leer un `data_version` obliga a copiar los 50,8 MB del núcleo español a un `.candidate`, compararlo y renombrar o borrar. Se paga **una vez por actualización de la app y sólo por el pack que el usuario reemplazó**; en una instalación normal esa lista viene vacía. ⚠️ **La alternativa se evaluó y se descartó**: que el build escriba las versiones en un archivo al lado de los assets sería gratis en runtime, pero leer `meta` de un `.db` desde Gradle necesita un driver de SQLite que este build no tiene, y la única vía sin driver es adivinar bytes — D-175 había ganado que **el build de Android no dependa de Python** y esto lo devolvería. ⚠️ **Si no se puede leer la versión de alguno de los dos, no se toca nada**: pisar el pack del usuario por no poder leer un número es el downgrade que esto viene a evitar | `PackStoreTest` (3 nuevos sobre `ExtractionPlan`, **verificado por mutación**: vaciar `compare` tira el caso del pack actualizado). ⚠️ **`extractIfNewer` en sí NO lo cubre el gate**: necesita un `Context` y un pack real, la misma limitación que D-164 ya nombró para el cableado de `openFile` |
| D-227 | Un pack del APK **se hace a un lado sólo si otro pack habla todos sus idiomas**, no por el mero hecho de que exista algún pack instalado | ⚠️ **Lo encontró un test escrito para otra cosa, y es la forma exacta del bug que este repo no puede ver.** La regla de D-088 era *«si hay algún pack instalado, esconder todos los del APK»*, escrita cuando lo incluido era un juguete de 28 entradas cuya etiqueta chocaba con la del pack real —el selector leía *ES* y *ES*—. **D-175 puso ahí los núcleos de verdad y nadie volvió a mirar la regla.** El resultado medido: con el español completo descargado, el filtro se llevaba **los dos** núcleos, y `en-core.db` quedaba instalado, abierto y consultable **sin chip de idioma** — el inglés desaparecía entero de la interfaz sin un error, sin un log y sin que nada fallara. La regla nueva es la misma forma que la contención de `packsToQuery`: hacerse a un lado por quien te contiene. Conserva lo que D-088 quería —con el español completo, `es-core` se esconde— y no toca `en-core`, porque nadie más habla inglés | `SearchViewModelTest` (2 nuevos, **verificados por mutación**: volver a la regla de D-088 tira el del núcleo de otro idioma, y una guarda de *«no borres el último pack propio»* tira el de borrar todos los descargados) |
| D-228 | ⚠️ **Desviación declarada de D-207**: los dos núcleos reales viajan en el APK y son **57,11 MB comprimidos**, no los 17,1 MiB que D-207 fijó como límite duro | Decidido contra el precio que puse. ⚠️ **Y antes que nada había un defecto que anulaba la premisa entera**: `bundlePacks` buscaba los núcleos en `../wearos-dictionary-data/` y el rebuild del 2026-09-22 los movió a `dist/`, así que **el APK viajaba sin ningún diccionario**. El síntoma es el que este repo no puede ver: `filter { it.isFile }` deja la lista vacía, que es un caso soportado a propósito (un clone limpio no tiene los packs, D-175), así que **el build seguía verde** y lo único observable era la app diciendo *«No hay ningún diccionario instalado»* en el reloj. Comprobado el 2026-09-23: `src/main/assets/` tenía sólo el `.gitkeep`, y la ruta vieja sigue dando `BUILD SUCCESSFUL`. Ahora avisa fuerte, sin fallar — D-086 exige que un clone limpio compile. **Medido el 2026-09-23 sobre el APK armado**: `es-core.db` 50,84 MB → **24,88 MB** en el APK (48,9 %) y `en-core.db` 42,86 MB → **32,24 MB** (75,2 %), los dos deflateados por AAPT sin ningún `noCompress`. El español comprime la mitad y el inglés tres cuartos porque el payload ya viaja deflateado (D-119) y lo que queda por comprimir es la tabla de flexiones, que en español es enorme y en inglés casi no existe. **APK debug medido: 111,0 MB.** ⚠️ **Lo que la compresión NO quita es la copia doble**: el pack se extrae igual porque SQLite no abre dentro del ZIP (D-173), así que en el reloj quedan **57,11 MB comprimidos + 93,70 MB extraídos = 150,8 MB**. Es la objeción de D-071 y vuelve a morder con estos tamaños; se acepta porque el disco no es el recurso escaso (D-207: 9,0 GiB libres, el 1,6 %) y lo que compra es que **los dos idiomas estén siempre, sin red** | `bundlePacks` avisa cuando no hay núcleos; el número, medido sobre el APK |

| D-229 | El pack incluido **declara su `data_version` en un indice dentro del APK**, y no se lo extrae para preguntarselo | ⚠️ **Corrige el mecanismo de D-226 el mismo dia que se escribio.** Ahi la comparacion obligaba a copiar el asset a un `.candidate` para poder abrirlo —SQLite no abre un `.db` dentro del ZIP (D-173)— o sea **50,8 MB movidos para leer un entero de 12 digitos**. La observacion que lo corrige: *«puedo tener guardada la version del pack incluido en la app, es un caso excepcional y hace la funcion de indice que si tiene el server web»*. Y es literalmente eso: `bundlePacks` lee el **mismo `index.json` que sirve `packserver.py --index-only`** y deja `assets/core-index.tsv` con una linea por pack. Coste en el reloj: dos lineas de texto contra 50,8 MB de copia. ⚠️ **Un indice trae su propio modo de falla y se cierra en el build**: uno viejo al lado de un `.db` nuevo declararia una version que no es, y la app decidiria un reemplazo con el numero equivocado — el mismo downgrade silencioso, por otra puerta. `bundlePacks` compara el `db_bytes` declarado contra el archivo real y, si no coinciden, **no declara nada**; la app entonces deja el pack del usuario como esta. ⚠️ **Y el indice falla devolviendo un mapa vacio, no lanzando**, que es la forma exacta que este repo no ve: sin versiones declaradas los nucleos no se actualizan nunca, sin excepcion y sin log. Por eso el parser esta separado de la lectura del asset, es puro, y entra al gate. ⚠️ **Los dos literales del nombre viven en lados que no pueden compartir una constante** —un script de Gradle y codigo Kotlin— asi que los vigila el audit | `PackStoreTest` (3 del parser, **verificado por mutacion**) · `audit_dictionary.check_core_index_name` (**verificado por sonda**) |
| D-230 | El nivel se estampa en el nombre del pack por **`build.name_with_tier`**, que reemplaza el que ya estuviera, en vez de concatenar | Reporte directo: *«no quiero ni entiendo por que los packs estan como (full) (core) a la vez»*. ⚠️ **Y las dos mitades eran ciertas, que es lo que lo hacia invisible para quien escribio el codigo**: `build_pack` cierra el pack completo como `Español (full)` (D-215), `build_core` **deriva de ese pack** (D-175) y le pegaba `(core)` encima. El nucleo terminaba llamandose `Español (full) (core)` — derivado de uno `full`, y es un `core`. Para quien lo lee en un reloj, no dice nada. La regla nueva: se saca el nivel que ya traiga el final del nombre y se pone el que toca, asi da igual si el nombre viene limpio o heredado. ⚠️ **Solo el ULTIMO token y solo si es un nivel conocido**: un pack ajeno que se llame `Griego (koiné)` no puede perder su parentesis por parecerse a esto. Los dos nucleos se regeneraron —15 s cada uno, derivan del completo y no necesitan los dumps— y salieron **byte a byte del mismo tamano**: cambio el nombre y nada mas. Verificado en la pantalla del emulador: `English (core)` | `test_core.NombreConNivelTest` (5, **verificado por mutacion**) |
| D-231 | Cancelar una descarga **para el trabajo y borra su parcial**, y `CANCELLED` deja de ser `FAILED` | Deuda de UI que el roadmap tenia nombrada desde que el instalador funciono: no habia forma de parar una descarga ni de sacarla de la cola, y con el ingles en 192 MB eso se nota. ⚠️ **Borrar el `.gz.part` es una decision, no una limpieza**: ese archivo es lo unico que hace real la reanudacion (D-214) y conservarlo hace que volver a pedir el pack siga desde donde iba. Pero *cancelar* significa, para quien lo aprieta, **recuperar el espacio y parar**; dejar 150 MB invisibles en `filesDir` de algo que se cancelo es lo contrario de lo pedido. **Costo nombrado**: pedirlo de nuevo baja desde cero. ⚠️ **Se cancela primero y se borra despues** — al reves el worker seguiria escribiendo sobre el archivo recien borrado y lo volveria a crear, la misma forma del error que D-104 cerro para el borrado de un pack. ⚠️ **`CANCELLED` es una fase propia y no `FAILED`**, porque `FAILED` promete un reintento: decirselo a alguien sobre algo que el mismo paro es falso y ademas deja una fila muerta que no se puede quitar. La pantalla trata `CANCELLED` como *«no hay descarga»* y la fila vuelve a ser una oferta. ⚠️ **Y usa el MISMO boton que borra**, porque para el usuario son la misma accion —*«sacame esto»*— y en 234 dp no hay lugar para un tercer control; lo que cambia es que cancelar no pide dialogo: no destruye nada que el reloj no pueda volver a pedir | `DownloadPackWorkerTest` (3 nuevos, **verificado por mutacion**: derivar mal el nombre del parcial tira el que compara las dos derivaciones) |
| D-232 | La app **acepta intents de depuracion** —sembrar una consulta, vaciarla, volcar el estado— y la puerta es un `buildConfigField`, apagado en `release` | ⚠️ **Lo que esto desbloquea llevaba dos sesiones costando.** El campo de busqueda **no toma foco con un tap sintetico** —misma forma del problema que fijo espresso 3.7.0 (D-093)— y eso dejo D-168 y D-169 sin verificar *con el reloj en la mano*, y volvio a morder en el emulador cuando hubo que comprobar que un nucleo devuelve resultados: se contesto leyendo el `.db` con `sqlite3`, que comprueba el dato y no la app. Con esto se verifico de punta a punta el mismo dia: `hous` → **`house` primero**, por la via del usuario. ⚠️ **La garantia de que no viaja a produccion no es que nadie lo registre**: `BuildConfig.DEBUG_INTENTS` es una **constante** `false` en release, asi que R8 pliega el `if`, la clase queda sin referencias y **desaparece del dex**. No es que no se use — es que no existe. Mismo trato que D-213 le dio al HTTP en claro. ⚠️ **Encendido en `benchmark` aunque herede de release**, porque es la build con la que se mide (D-166) y la unica parecida a release que se instala: si fuera la unica que no puede sembrar una consulta, medir una busqueda volveria a necesitar un dedo. Es el razonamiento de D-212 al elegir `Log.isLoggable` sobre `BuildConfig.DEBUG`. ⚠️ **Vaciar el campo tiene accion propia y no `-e q ""`**, y eso salio de usarlo: un extra vacio **no sobrevive a `adb shell`** — medido, `am broadcast -a ... -e q "" -p cl.fadiaz.dictionary` dejo la app buscando literalmente **`-p`** y perdio el filtro de paquete. ⚠️ **El volcado imprime la huella ENTERA de cada anotacion del memo**, una por linea, porque `logcat` corta los mensajes largos y porque es el dato con el que D-225 se comprueba en un dispositivo — abreviarlo lo volveria incomprobable | `DebugIntentsTest` (6 sobre el volcado, puro y en el gate, **verificado por mutacion**) · el receiver necesita dispositivo y no lo cubre el gate |

| D-233 | **§Verification y §Logging obligation salen de `CLAUDE.md`** a la skill `verify` y al propio changelog, dejando invariante + puntero | El triage v8→v16 traía seis bloques y el archivo estaba en **198 de 200 líneas**, así que cada regla que entra desaloja otra (D-222). ⚠️ **Lo que hizo barata la mudanza es que las dos secciones ya estaban duplicadas, palabra por palabra.** Los tres párrafos de reglas de §Verification —medición con fecha y entorno, retractación en todos lados, el diff sin commitear— existían idénticos en la skill `verify`; el *por qué* de §Logging obligation —dos sesiones en paralelo no se ven— existía idéntico en la cabecera del propio changelog. Sacarlas **no retiró ninguna regla de circulación: borró una segunda fuente de verdad**, que es justo lo que el estándar de D-239 prohíbe. Lo único que vivía sólo en `CLAUDE.md` era la distinción emulador/reloj (D-043), y ésa se movió, no se borró. **Medido: 9 de las 10 líneas que hicieron falta salieron de ahí.** ⚠️ **Y las dos `description` se ampliaron**, sin lo cual el puntero sería una regla que dejó de leerse: `verify` ahora dispara con *«un refactor que no debería cambiar nada»* y *«después de una sonda»*; `commit`, con *«una pieza ya pasa y falta el resto»* | `check_skills_reachable` · `check_root_budget` (200 exactas) |
| D-234 | **Una regla que el agente tenía cargada y rompió igual es la señal para subirle el rung**, no para repetirla más fuerte | La escalera, del método v13. Es la regla que decide cuándo las demás se promueven, y sin ella un repo responde a cada incumplimiento agregando prosa — que es exactamente el mecanismo que acaba de fallar. ⚠️ **Prosa que estaba en contexto y no se sostuvo es evidencia medida de que la prosa es el mecanismo equivocado para eso.** Esta misma sesión la aplicó dos veces: el paso 3 del session loop pasó de *«acordate de limpiar la sonda»* a un chequeo del audit (D-236), y el paso 7 pasó de una frase a un procedimiento con comandos en la skill `commit` (D-235) | — *(es la regla que gobierna a las otras; su enforcer es que cada promoción tenga la suya)* |
| D-235 | **Cada commit se ofrece cuando SU pieza pasa**, no cuando termina la tarea; y partir una serie a posteriori tiene procedimiento | Paso 7 del session loop. ⚠️ **El costo de no hacerlo se midió en esta sesión**: ocho cambios retenidos hasta el final necesitaron **cinco commits**, y tres de ellos compartían `app/build.gradle.kts`, `SearchViewModel.kt` o `MainActivity.kt` con un commit posterior — hubo que **construir tres versiones intermedias a mano** para que cada commit fuera coherente. Partir es estrictamente más caro que no apilar. ⚠️ **No autoriza commitear por iniciativa propia a mitad de tarea**: ofrecer no es hacer, y lo que cambia es *cuándo se ofrece*, no quién decide. El procedimiento —construir cada árbol desde el commit inicial, probar los dos extremos primero, respaldar sin `checkout`/`restore`/`stash`/`reset`/`clean`, versiones intermedias por `git --work-tree=<dir> add`, y gatear **cada** árbol intermedio— vive en la skill `commit`. ⚠️ **Y resuelve la fricción de los conteos**, que era la objeción real: `--fix` corre **dentro del worktree**, donde sólo existe el código de ese commit, y su resultado se stagea sin tocar el árbol principal | La skill `commit` · los cinco commits del 2026-09-23, cada uno verde en worktree |
| D-236 | **Las sondas de mutación las encuentra el audit**, no la disciplina de quien las corrió | Paso 3 del session loop. ⚠️ **El modo de falla es el peor que tiene este repo: una mutación olvidada no rompe NADA.** Se escribe para que un test falle, se comprueba que falla, y si el restore no vuelve —o vuelve a medias— lo que queda es código deliberadamente equivocado **con el gate en verde**, porque el test que lo detectaría es justo el que se estaba probando. Y ya mordió de otra forma: macOS cachea bytecode fuera del repo y una mutación del mismo ancho sobrevivió a un restore. `check_no_probes_left_behind` falla sobre `MUTACION`, `MUTATION PROBE` y `SONDA:` en todo `.kt`/`.kts`/`.py`/`.java`. ⚠️ **No cubre una mutación sin marcar —nada puede—** y por eso el marcador pasa a ser la convención: una sonda se escribe con su marca, y esta regla convierte olvidarla en la única forma de que pase desapercibida. La única exención es el propio `audit_dictionary.py`, que las nombra | `audit_dictionary.check_no_probes_left_behind`, **verificado por sonda** |
| D-237 | **Un cambio que no debe cambiar nada se prueba por su invariante, comparado mecánicamente** — nunca leyendo el diff | Paso 4 del session loop. Un refactor, una regeneración, un renombrado: *«leí el diff y parece equivalente»* no es una prueba, y **es donde un agente es más débil, porque el diff es exactamente lo que acaba de escribir**. ⚠️ **Medido el 2026-09-23**: los dos packs núcleo se regeneraron para arreglar su nombre, y el invariante que tenía que sostenerse era *el contenido no se mueve*. El chequeo fue el tamaño en bytes: **50.843.648 y 42.856.448, idénticos antes y después** — eso es lo que permitió llamarlo un renombrado. Leer el diff de un `.db` no prueba absolutamente nada. Las formas que este repo usa: tamaño o `sha256` para un artefacto, `verify_pack.py --como-la-app` para un pack, los vectores compartidos para `norm()`, el plan de consulta para un índice. **Si no se puede nombrar un invariante, el cambio no es el que se está afirmando** | La skill `verify` |
| D-238 | **Observar después de shippear**: cada build dice de cuál se trata, y las preguntas que sólo el reloj contesta viven en un brief permanente | Segunda mitad del principio 7, y la mitad que faltaba: la primera —nombrar la clase de bug que este repo no puede ver y darle un chequeo pre-ship— ya estaba. ⚠️ **`versionName` es el mismo en diez builds del mismo día**, así que sin identidad de build *«el arreglo no funcionó»* y *«el reloj corre el APK de ayer»* son **el mismo reporte** — y cada caché entre esta máquina y la muñeca vuelve probable el segundo. `BuildConfig.BUILD_COMMIT` y `BUILD_TIME` los **genera el build** (`providers.exec`, entrada declarada del configuration cache) y degradan a `"unknown"` sin romper, porque un tarball sin `.git` tiene que compilar (D-086). ⚠️ **El `+dirty` es la mitad honesta**: casi todo lo que se instala en un reloj sale de un árbol sin commitear, y ahí el hash **no identifica** lo que corre; decirlo es la única forma de que el número no mienta. Se lee en Ajustes y en `DEBUG_DUMP`, **primero y no al final**, porque decide si el resto del volcado sirve. ⚠️ **Se calculan fuera de `android { }` y con `import` explícito**: a nivel de script y dentro de `defaultConfig`, `java` resuelve al accessor de Gradle y no al paquete — el error dice `Unresolved reference 'text'`, que no lo insinúa. Y `docs/preguntas-del-reloj.md` es el brief: nueve preguntas abiertas, cada una con el readout que la contesta y qué desbloquea. **Una afirmación que una sesión no pudo verificar entra ahí en el mismo cambio que su entrada de changelog** | `ScreensTest` (la fila del diagnóstico, **verificada por mutación**) · `DebugIntentsTest` (2) · `check_doc_paths` sobre el documento nuevo |
| D-239 | **Un comentario describe el código, nunca el cambio**; **un documento durable no cita un artefacto de sesión**; y **el contenido que escribió el dueño no es del agente para completar** | Los estándares de ingeniería de v12 y el principio 15 de v13. ⚠️ **El primero se cobró un ejemplo en el acto**: el test `elDIAGNOSTICO_es_SOLO_LA_VERSION_y_va_al_fondo` dejó de ser cierto al sumarle la identidad de build, y un nombre que narra el estado de ayer es indistinguible de uno que describe un bug — se renombró a `elDIAGNOSTICO_va_al_FONDO_y_no_nombra_diccionarios`. *«Ahora usa X»* muere en el commit siguiente y el diff ya lo decía. El segundo: una rama, una ruta de scratch o un id de tarea sobreviven al documento que los cita y lo vuelven mentiroso. ⚠️ **El tercero invierte un reflejo**: un slot vacío en prosa del dueño **es una elección** hasta que diga lo contrario — se completan los que uno creó, no los ajenos | — *(rung 1; `check_no_probes_left_behind` cubre un caso vecino y el resto es revisión)* |

| D-240 | La palabra del día se elige **por encima de un piso de `rank`**, y con eso los packs **núcleo vuelven a darla** | Reporte: *«los packs core no generan palabras del día»*. ⚠️ **El diagnóstico anterior era correcto y estaba incompleto.** D-193 elige la de mejor rank entre 96 candidatas, y un núcleo **es** las 8.000 más frecuentes, así que eso da siempre una palabra funcional —`my`, `un`, `a`, `de`— y por eso se los excluyó. Pero **el mismo sesgo está en los packs completos, sólo diluido**: medido simulando el selector real sobre 112 días, los días que caían en la zona funcional eran **84 % (es-core), 94 % (en-core), 41 % (es-full) y 16 % (en-full)**. `en-core` daba `'m`, `TOLD`, `a`, `ah`. Excluir los núcleos trataba el síntoma en una mitad y dejaba la otra rota para todos los usuarios con diccionarios instalados. **Con un piso de `rank` 150 los cuatro caen a 0 %**, y sale `acción, anillo, abierto` / `Christmas, accept, afternoon, arrest, beauty`. ⚠️ **La variedad no baja**: 101 → 100 palabras distintas en `es-core`, 109 → **111** en `es-full`. ⚠️ **Un solo número sirve para todos los idiomas, y ésa es la diferencia con los umbrales que este archivo rechazó** (912 para español, 978 para inglés): aquéllos medían riqueza de página, que no tiene escala común. Desde D-185 `rank` es **frecuencia Zipf**, y los packs lo declaran — los dos dicen `rank_signal_boundary = 500`. Medido al mismo rank en los dos idiomas: 0 da `a, la, no, y` y `a, and, i, it`; 100 da `ahora, muy` y `hear, listen, remember`; **150 da `amable, ataque, avión, cámara` y `attack, bag, clothes, expect`**. 200 es igual de limpio y descarta `acción`, `accept`, `afternoon`, que valen la pena. ⚠️ **Sólo aplica a packs con `rank_basis=frequency`**, el mismo guardia que ya usa la rotación de categorías: en uno de riqueza de página un rank de 150 no significa nada y el piso descartaría al azar. ⚠️ **Y lo que esto cierra de verdad**: una instalación recién hecha lleva sólo los núcleos del APK, así que hasta acá **el inicio no mostraba palabra del día en el primer arranque de cada usuario**. ⚠️ **Sin verificar en un reloj**: el piso se ajustó sobre tiradas simuladas, nunca sobre la pantalla — P-10 en `docs/preguntas-del-reloj.md` | `WordOfTheDayTest` (3 nuevos, **verificados por mutación** — el primero era **vacuo** y lo delató la sonda: se comparaba contra `RANK_FLOOR`, así que poner la constante en 0 movía el poste) · `SearchViewModelTest.unPackNUCLEO_SI_genera_palabra_del_dia` |
| D-241 | Las palancas de peso del índice FTS quedan **medidas y en espera**, no aplicadas, hasta que haya un número de arranque de un reloj | Decisión explícita: *«ninguna todavía: primero medir el arranque»*, y coincide con lo que §O-3 ya decía —*no se optimiza lo que no se mide*—. ⚠️ **Lo que sí cambió es que ahora hay números, y corrigen al roadmap en dos cosas.** **(1) §O-3 generaliza una medición del pack español a todos los packs, y el inglés tiene otra forma.** Medido con `dbstat` sobre los packs reales: en español `form` es el **42 %** y la conclusión *«el recorte obvio no existe, el 46 % está en `form`»* es cierta; en inglés `form` es el **6 %** y el peso está en `entry` **48 %**, `fts_def_data` **22 %**, `idx_entry_norm` **12 %** e `idx_entry_fuzzy` **8,8 %**. **(2) Las dos opciones abiertas de este archivo no valen lo mismo, ni de lejos.** Medido sobre texto real del pack inglés, 60.000 entradas reindexadas de las cuatro formas: `detail=none` ahorra **55,3 %** del índice —unos **38 MB del pack inglés, el 12 %**— y `columnsize=0` ahorra **3,6 %**. ⚠️ **Y la compatibilidad se verificó ejecutando la expresión MATCH real de la app, lo que corrigió una suposición mía**: asumí que `columnsize=0` rompía `ORDER BY rank` y **no lo rompe**. Lo que `detail=none` sí rompe son las **frases de varias palabras**, que la app no usa hoy porque `toMatchExpression` entrecomilla token por token — o sea que el costo real es **cerrar la búsqueda por frase exacta para siempre**. ⚠️ **Y el costo de `columnsize=0` es de este repo y no de FTS5**: borra `fts_def_docsize`, que es una de las siete invariantes que D-217 le agregó a `PackFile.open`. Nueve MB a cambio de un guardrail medido es el peor ratio de los tres. **Se reabre con el número de P-4** | — *(medición registrada; `docs/roadmap.md` §O-3 y P-4 en `docs/preguntas-del-reloj.md`)* |

| D-242 | Las **partes principales** de una palabra viajan en el payload con su ortografía, y la ficha las muestra **antes de las traducciones y con peso de acepción** | Pedido: *«que se muestren las formas de una palabra cuando estén disponibles»*. ⚠️ **El diagnóstico obvio —«`form` es el 42 % del pack y cuesta 0 bytes mostrarla»— es FALSO, y descubrirlo fue lo que más valió.** Esa tabla guarda `norm(forma)`: **`corrais`, no `corráis`**, porque su trabajo es ser una clave de búsqueda. Una ficha alimentada desde ahí estaría mal escrita, y en español eso se ve en cada acento. ⚠️ **Y las formas con su ortografía SÍ existen al construir**: `record.forms` las trae de la fuente y `build.py` las normalizaba y tiraba la original. Así que el canal nuevo no duplica un dato — recupera uno que se estaba perdiendo. ⚠️ **Son POCAS y elegidas: gerundio, participio, plural y femenino.** `correr` trae 137 formas en la fuente y **202 filas** en `form`; volcarlas sería ilegible en 234 dp y caro en bytes. Medido: de las 137 salen **dos** (`corriendo`, `corrido`); de `alto`, dos (`altos`, `alta`). Es lo que un diccionario impreso pone al lado del lema. ⚠️ **La clave viaja neutra (`ger`, `part`, `pl`, `fem`) y la etiqueta la pone la app**: guardar *«gerundio»* metería el idioma de la interfaz **dentro** del pack, y el mismo archivo lo comparten un usuario con la app en español y otro en inglés. Una clave desconocida muestra la forma **sin** etiqueta en vez de esconderla. ⚠️ **`impersonal` NO descalifica, y creerlo dejó a `correr` sin ninguna parte principal en la primera versión**: el español marca así **todas** sus formas no personales, y lo que separa `corriendo` de `habiendo corrido` no es una etiqueta sino que la segunda es **compuesta**. Medido contra el dump real, no razonado. ⚠️ **No son tocables**, a diferencia de sinónimos y traducciones: una forma flexionada no es un lema y no tiene ficha a la que ir (D-094). ⚠️ **Y no hace falta `schema_version` nuevo**: el payload es texto con tags y un pack viejo simplemente no lo trae — la ficha no muestra la sección y nada falla. Lo que sí hizo falta fue **registrar el tag en `verify_pack.py`**, que es lo que convierte «el lector ignora lo que no conoce» en un fallo visible cuando el builder escribe mal | `PayloadCodecTest` (5, **verificados por mutación**) · `test_payload.PartesPrincipalesTest` (6) · `test_source_kaikki.PartesPrincipalesTest` (7, uno escrito **porque una sonda descubrió que el filtro de compuestas no estaba cubierto**) · `verify_pack.TAGS_CONOCIDOS` · el toy pack lleva una entrada con acento |
| D-243 | El tap sobre una palabra de una glosa se resuelve **por cercanía**, con tres cotas | ⚠️ **Decidido contra mi recomendación, y queda dicho.** Evalué cinco opciones y recomendé la confirmación (B) porque una heurística de cercanía cambia *fallar visiblemente* por **fallar con confianza**: sin señal visual un acierto y un casi-acierto se sienten igual. Se eligió la E, así que el diseño contesta esa objeción donde puede, con tres cotas que son lo que los tests fijan: **(1) un acierto exacto nunca se pisa** —la heurística no corre mientras el dedo esté sobre la palabra, así que nada de lo que hoy funciona cambia—; **(2) un tap dentro de una línea sólo puede saltar a palabras de ESA línea**, que es justo el modo de falla que la objeción nombra; **(3) más allá del radio no pasa nada**, igual que hoy. **La causa es geometría y está medida**: el área táctil es el glifo, unos **40 × 14 dp** contra el mínimo de **48 × 48** de Android, y la altura es lo que está 3,4× por debajo. ⚠️ **El radio es una fracción de la altura de línea y no un valor en dp**, que es lo único que sobrevive al ajuste de tamaño de texto. ⚠️ **Lo que sigue sin resolver, dicho**: con dos enlaces pegados y un tap en el hueco, elige el más cercano y el usuario no recibe ninguna señal de que adivinó. Ése es el costo que la recomendación quería evitar y sigue en pie. ⚠️ **Y reemplaza `LinkAnnotation.Clickable`**, así que la medición de las cajas pasa a necesitar un `TextLayoutResult` — la decisión es pura y la cubre el gate; el dibujo necesita un dispositivo | `GlossTapTest` (11, **verificados por mutación** — y su primera corrida encontró **un bug real**: la guarda del radio corría antes de la contención, así que un tap exacto se perdía si el radio era inválido) |

| D-244 | El wrapper de Gradle **declara el `distributionSha256Sum`**, y el número se verifica contra el artefacto, no contra una página | El wrapper subió de 9.6.0 a 9.7.1 en una sesión paralela y el cambio **borró la línea**, así que el repo dejó de verificar el zip de Gradle que baja. ⚠️ **Es el control de cadena de suministro más barato que existe y el más fácil de perder**: se pierde solo, en un diff que además hace algo legítimo, y no rompe nada — el build sigue verde bajando lo que le manden. Repuesto con el valor publicado en `downloads.gradle.org`, y **verificado de dos formas**: el wrapper lo descargó entero en un `GRADLE_USER_HOME` limpio y lo validó (exit 0), y una sonda con un carácter cambiado falló con `Verification of Gradle distribution failed` imprimiendo el checksum **real** del artefacto, que coincide con el publicado. ⚠️ **La segunda mitad es la que vale**: leer un número de una página web y copiarlo no es verificarlo; hacer que la herramienta lo compruebe sobre los bytes descargados, sí | `gradle/wrapper/gradle-wrapper.properties`, **verificado por sonda** |
| D-245 | Este repo **se registra a sí mismo** en `.agents/tracking/carriers.md`, y el id se deriva reproduciendo la receta | ⚠️ **El bloqueo declarado el 2026-09-23 por la mañana era medio falso, y conviene que quede por qué.** Se habían dado dos motivos y sólo uno se sostuvo. El primero —*«`carriers.md` no es de los archivos que un carrier puede escribir»*— **es falso**: `prompt-harvest.md` §*What the local step must never do* prohíbe escribir **fuera** de `tracking/`, y `carriers.md` está dentro; lo que prohíbe además es subir una versión o recalcular un digest, y una fila no hace ninguna de las dos. Ese supuesto venía de una paráfrasis y no del texto, que es exactamente la clase de error que se evita leyendo la fuente. El segundo —*`bundle.py register` necesita `tomllib`, o sea Python 3.11+, y acá hay 3.9.6*— **es cierto**, pero impide usar la herramienta, no escribir la fila. ⚠️ **El id no se adivinó**: se reprodujo `bundle.py carrier_id` en 3.9 —`r-` más seis hex del sha256 del remoto normalizado a `host/owner/repo`— sobre `github.com/fabaindaiz/wearos-dictionary`, y da `r-a2f271`, el mismo que la sesión del 22 había anotado por su cuenta. Sin eso la fila sería una afirmación sin comprobar en el único archivo donde el dueño del bundle confía. Comprobado después que los tres digests siguen cuadrando: `tracking/` no entra en ellos, que es justo por lo que un carrier puede escribirlo | `.agents/tracking/carriers.md`; `audit_dictionary.check_bundle_digests` sigue en verde |

| D-246 | Un `Write` sobre uno de los cuatro archivos generados lo **bloquea un hook `PreToolUse`**, no una regla de permisos | Cierra el hueco que D-223 dejó nombrado y sin tapar. Las reglas `Write(ruta)` de `permissions.deny` **se aceptan y nunca se consultan**, así que se sacaron: los cuatro archivos quedaron protegidos contra `Edit` —el caso real, porque un agente los modifica— y **crear uno de cero no lo paraba nada**. ⚠️ **Y el daño de ese caso es el peor del repo**: `UnicodeRepertoire.kt` y `repertoire.txt` son el invariante central, y reescribirlos a mano crea dos fuentes de verdad que divergen **en silencio** — el síntoma es una palabra que falta, meses después. El hook sale **2** con un mensaje que dice qué herramienta regenera el archivo, que es lo que evita que el bloqueo se lea como un bug del harness. **Verificado por sonda, los cuatro y el control**: `UnicodeRepertoire.kt`, `repertoire.txt`, `payload-fixture.tsv` y `local.properties` salen 2; un `.kt` cualquiera sale 0. ⚠️ **Es una mejora de proceso y por eso se preguntó antes de construirla**, no se ejecutó sola | `.claude/settings.json` → `hooks.PreToolUse`, **verificado por sonda** |
| D-247 | Las dos fricciones que **fallan sin decirlo** viven en la skill `verify`, no en `CLAUDE.md` | `set -- $x` en fish y el `--` dentro de un comentario XML llevaban **dos y tres golpes**, que es el umbral que el principio 17 fija para promover una fricción. ⚠️ **La de fish es la que importa de las dos**: `set` asigna a una variable y `--` es fin de opciones, así que `$1` y `$2` quedan vacíos y **el comando sigue con argumentos faltantes sin error** — el bucle corre y produce basura que parece resultado. La del XML rompe el build con un mensaje que nombra el problema, así que se descubre sola y cuesta un minuto; entra igual porque es el mismo reflejo (`--` es como este repo escribe un inciso en todos lados). ⚠️ **Aterrizan en la skill y no en la raíz porque `CLAUDE.md` está en 200 de 200**, y por D-222 el puntero se dejó dentro de la línea que ya existía —sin sumar una— y la `description` de `verify` ya dispara con *«chequeá»* y *«¿esto funciona?»*, que es cuando se corre un comando así | La skill `verify`; `check_root_budget` (200 exactas) |


| D-248 | Spanish prose has a **per-area ceiling** the gate checks, and it drops when an area is translated | `CLAUDE.md` Working style says the repository is written in English from 2026-09-21 onward, with one documented exception --the UI strings-- and **the rule was not holding**: measured with one ruler against two trees, **5,281 to 6,966 lines in two days, +32 %**, written largely by sessions that had it loaded in context. By D-234 that is precisely the signal to **raise its rung**, not to restate it. WARNING: a ceiling and not a zero, because a zero would fail today over 11,894 lines and a check that cannot pass gets switched off. What it watches is not the debt: it is that the debt does not **grow**. WARNING: per area and not one total, because a total lets translating 200 lines in one module hide 200 new ones in another; each row drops on its own when its area is translated, and the advisory asks for it to be lowered -- that is the ratchet. WARNING: the half that makes it usable is excluding English prose that **quotes** Spanish: half a dozen documents say *"the app says «No hay ningún diccionario instalado»"* and a naive detector counts them. Measured: **6,966 without the exclusion against 6,798 with it**, about 170 false positives, nearly all in the `CLAUDE.md` files, which are already translated. WARNING: and a mistake of mine while writing it is kept as a comment -- the first version put the unaccented vowels into the character class and reported 2,068 where the ruler said 1,778. **The check did not say so** --a check does not know it is wrong-- what said so was **comparing its number against one I already had** from a separate script. WARNING: the changelog is its own row and was never in the roadmap's table: 4,180 lines, the largest area of all. Translating it backwards is worth little --it is the record of past sessions-- and what the ceiling buys there is that **new entries get written in English** | `audit_dictionary.check_spanish_prose_budget`, **verified by probe**: one Spanish line added to `dict-data` makes it fail |
| D-249 | `dict-data/src/main` is **entirely in English**, and it is the first step of the order the roadmap fixes | The first module of stage 3 --code before the roadmap, and module by module smallest first-- chosen because it is the smallest: **203 lines across two files**. It is a rewrite rather than a mechanical translation: this repository's prose carries measurements, names what went wrong and cites decisions, and running it through a translator would destroy exactly that. WARNING: and the ceiling drops to 0 in the same commit, which is what makes the check a ratchet -- without that the area keeps 203 lines of slack and the next session can refill it with nothing to warn. The row is left at 0 rather than deleted: a deleted row is a ceiling nobody watches. **6,595 lines remain**: `docs/roadmap.md` 2,314, `tools/**` 1,778, `app/src/main` 1,293, `dict-core/src/main` 587, `docs/decisions.md` 262 | `check_spanish_prose_budget` with `("dict-data/src/main/", 0)` |
| D-250 | New rows in `docs/decisions.md` and new entries in the changelog are written **in English from now on**, even while the rest of each document is Spanish | WARNING: **this was forced by D-248 biting its own author, within the hour.** D-248 and D-249 were first written in Spanish and the gate refused them: `docs/decisions.md` 263 against a ceiling of 262, `.claude/logs/` 4,209 against 4,180. That was the moment that decided whether the ratchet is a rule or decoration -- raising the ceiling the first time it bites makes every later ceiling advisory. WARNING: **and it resolves a tension against my own earlier argument.** The session of 2026-09-23 declined to translate its decision rows precisely because *"fifteen English rows leave a mixed document, which is worse than either state"*. That reasoning is right about a **one-off** translation and wrong about a **transition**: a document that is mixed on its way to English is the normal state of any staged migration, and refusing to start until it can be done whole is how a migration never starts. The cost is real and is accepted: `docs/decisions.md` reads in two languages until its own stage arrives | `check_spanish_prose_budget`, whose ceilings for those two areas are deliberately not raised |

## Tooling de Python

| # | Decisión | Por qué | Enforced in |
|---|---|---|---|
| D-045 | El builder no tiene dependencias de terceros: solo stdlib | Un pipeline que solo necesita `python3` se puede correr desde cualquier clone sin preparar nada | `pyproject.toml` → `dependencies = []` |
| D-046 | **El gate no depende de Hatch.** `./gradlew check` corre `python3 -m unittest` a secas | Si el gate necesitara Hatch, un clone limpio dejaría de verificarse solo. Hatch es la capa de desarrollo | `tools/build.gradle.kts` |
| D-047 | La matriz de versiones de Python es la razón principal de tener Hatch | El builder escribe claves que el reloj recalcula: que dé lo mismo en todo Python **es** el invariante central. Medido: 35 tests idénticos bajo Unicode 13.0 y 16.0 | `hatch run matrix:test` |
| D-048 | `requires-python = ">=3.9"`, abierto hacia arriba | 3.9 es el piso solo para **regenerar** el repertorio (exige Unicode 13.0.0). Construir packs funciona en cualquier versión moderna, y está medido | `pyproject.toml`; el guardián de `gen_repertoire.py` |

## Decisiones descartadas, con el número que las descartó

Están acá para que no se propongan de nuevo. Son de las filas más útiles del archivo.

| # | Se descartó | Por qué, con el número |
|---|---|---|
| D-036 | **Un canario comprimido** para validar el diccionario de payload | Se derivaba del propio diccionario, así que uno **truncado seguía validando**. Un mecanismo de integridad que se valida a sí mismo no valida nada. Reemplazado por el sha256 de D-008 |
| D-037 | **zstd** para el payload | Comprime más, pero costaba una librería nativa en el reloj además de la de SQLite, y una dependencia de pip en el builder. Ver D-007 |
| D-038 | **Play Asset Delivery** para distribuir packs | No tiene soporte documentado en Wear OS. No se basa la distribución en algo no verificable |
| D-039 | **Room para leer los packs** | `createFromFile()` copia el archivo al directorio de Room, duplicando decenas de MB. Room se usa solo para los datos de la app |
| D-040 | **OkHttp** para descargar packs | `HttpURLConnection` hace `Range` y progreso sin sumar un byte al APK, y ya se agrega ~1–1,5 MB por ABI de SQLite nativo. Se revisa si reintentos o TLS resultan insuficientes |
| D-041 | **Extraer un parser de SQL** de `schema.sql` para separar los índices | Un comentario con un punto y coma rompía el split. Se resolvió con dos archivos, `schema.sql` e `indexes.sql`. Un parser de SQL hecho a mano se rompe así de nuevo |
| D-049 | **La regla `UP031` de ruff** (usar f-strings en vez de `%`) | De los **89 hallazgos de la primera corrida, 69 eran esa sola regla**. Un linter que grita 69 veces por una preferencia se termina apagando entero. Además hay un motivo técnico: `gen_repertoire.py` emite una plantilla de Kotlin llena de llaves, que en un f-string habría que duplicar una por una |

## Decisiones abiertas

**No están decididas.** Si te encontrás resolviendo una de estas por tu cuenta, pará y preguntá.

| Tema | Qué hay que decidir | Por qué bloquea |
|---|---|---|
| **Qué se respalda de las preferencias** | `data_extraction_rules.xml` excluye sólo `packs/`, así que `historial`, `favoritos` y `pack_activo` **se respaldan y se transfieren**. ¿Se excluyen (una línea de XML, y un reloj nuevo empieza vacío) o se guarda `uid` junto a `entryId` y se resuelve por ahí? | **No es una preferencia, es un defecto**: `Visita` guarda `entryId`, que es la identidad **física**, y `schema.sql` dice que **no sobrevive a reconstruir el pack**. Restaurar un backup —o reconstruir un pack, que es rutina con `devpack.py`— deja el historial y las hasta 100 guardadas apuntando a **otras palabras, en silencio**. D-030 decidió *cómo* se configura el backup; que las preferencias quedaran adentro **no lo decidió nadie**. Toca D-102 y el formato en disco: ver `docs/roadmap.md` §El historial y las guardadas sobreviven a un backup |
| Granularidad de la composición | ¿El pack auxiliar se une a la **entrada** o a la **acepción**? Hoy `uid` es por entrada (D-055) | Un sinónimo es de una acepción, no de la palabra. Un join por acepción necesita un ordinal estable dentro de la entrada, y el ordinal se corre cuando la fuente agrega una acepción. **Verificado al construir el pack real (2026-09-17)**: los senses de kaikki.org **sí** traen `id` (`es-leonino-es-adj-toe7A5EW`), distinto por acepción. Pero el sufijo parece derivado del contenido, así que **editar una glosa probablemente lo cambia**: sirve como ordinal dentro de la entrada, no como identidad estable. Queda por medir contra dos dumps de fechas distintas |
| `detail=none` en `fts_def` | Achica el índice pero mata las consultas de frase | Con definiciones como contenido principal, buscar frases dentro puede ser *la* feature |
| `columnsize=0` en `fts_def` | Achica más, pero `SELECT COUNT(*)` pasa a dar error | Rompería una comprobación de `verify_pack.py` |
| Compresión por fila vs bloques de 50–64 kB | El dominio usa bloques (dictzip); nosotros por fila | Sin medición que lo decida a escala real |
| **El `sha256` del pack entero** | Hoy **no existe**: `payload_dict_sha256` cubre sólo el diccionario de compresión de 32 KB. ¿Lo declara el catálogo, o `verify_pack.py` emite un sidecar al publicar? | Sin él, una descarga truncada **abre sin error y devuelve menos palabras** — `PackFile.open` valida meta, que vive en las primeras páginas. Es el requisito nº 1 del instalador y no se puede resolver solo: ver `docs/roadmap.md` §Instalador |
| **Qué viaja por la red** | ¿El `.db` entero, o sólo lo no derivable (`entry` + `form`) reconstruyendo FTS e índices en el reloj? | En inglés el 45 % del archivo es derivable —134 MB de 295— pero reconstruirlo son minutos de CPU en una muñeca, y **el artefacto deja de ser el que `verify_pack.py` validó**. Hay que medir las dos cosas antes de elegir |
| ~~**R8 en release**~~ | **Cerrado por D-163: está encendido.** Lo que queda abierto es otra cosa y más chica: **verificarlo en dispositivo**, que sigue sin hacerse. ⚠️ Y el 2026-09-21 se midió por qué importa: el APK que se sideloadea es `DEBUGGABLE`, ART **no lo compila AOT**, y su dex son **43,8 MB en 12 archivos** contra **2,7 en uno** con R8 — 802 ms de verificación por arranque. El build type `benchmark` existe justo para probarlo sin keystore | Ver roadmap O-2 y `docs/bateria.md` |
| Startup profile | Reduce latencia de arranque a cambio de tamaño de APK | Ya sumamos ~1–1,5 MB por ABI de SQLite nativo. Necesita el número de O-1 |
